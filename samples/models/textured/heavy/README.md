# Heavy Textured Samples

The 38MB real nail texture models in `samples/models/textured/` are heavy
manual validation assets. Fast regression fallback configs must use the small
fixtures under `samples/models/textured/fixtures/` instead.
