# Stage 15 texture fixtures

| Fixture | Texture texels from left to right | Purpose |
|---|---|---|
| F-03 | `(255,255,255)`, `(254,255,255)`, `(254,254,254)`, `(128,128,128)` | Exact threshold boundary |
| F-04 | All `(255,255,255)` | Full white-carrier and legacy fail-closed paths |

Both textures use lossless PNG and nearest-neighbor sampling. They are production-test assets, not display previews.
