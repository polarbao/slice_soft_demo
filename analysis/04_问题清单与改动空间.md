# 04 问题清单与改动空间

> 每条含：**严重度 / 证据（A 级，可逐条复核）/ 影响 / 改动空间 / 成本 / 风险**。
> 严重度：🔴 高（已产生或必然产生错误）｜🟠 中（结构性风险，尚未爆）｜🟡 低（可理解性/效率）
> 成本：S ≤ 1 天 ｜ M ≤ 1 周 ｜ L ≤ 1 月 ｜ XL > 1 月
> ~~**本清单是分析结论，不是已授权的工作项。**~~ **⚠ 2026-09-17 更正**：
> 其中多条已获授权并完成（P0FIX 专项 10 条、TEXFAIL、F-09、F-34、F-41~F-50 等）。
> 逐条实际状态见文末「梯队状态复核（2026-09-17）」；**未标注状态的条目**仍是分析结论，
> 实施需按 `AGENTS.md` 第 2/7 条先取得用户裁定。

---

## A 类：契约一致性（5 条）

### F-01 🔴 部署清单与运行时自述的能力集已实际漂移

**证据**

| 位置 | `provides` 含 `slice.rgbwsvt`？ | `produces` 含 `p0.rgbwsvt.1`？ |
|---|---|---|
| `src/slicer_module/ModuleInfo.cpp:41`（运行时 `pm_module_info` 返回值） | ✅ 有 | ✅ 有 |
| `src/slicer_module/module.json.in:11-31`（部署清单 `module.json`） | ❌ **无** | ❌ **无** |

`ModuleInfo.cpp:41` 的字符串里明确含 `"slice.rgbwsvt"` 与 `{"contract":"p0.rgbwsvt.1","kind":"package"}`；`module.json.in` 的 `provides` 数组停在 15 项且 `produces` 只有 `p0.rgbwsv.2`。

且 `tests/stage14c_05/ModuleInfoTests.cpp:118-125` **正面断言**运行时自述里必须有这两项——说明 C++ 侧是刻意的，落下的是清单模板。

**影响**

- 任何按 `module.json` 做能力发现的消费者（打包/分发流程、未来的 PrintApp、第三方审计）都会认为本模块**不支持 RGBWSVT**。
- `scripts/PackageSlicerModule.ps1:260-269` 会把这份不完整的 `module.json` 原样打进分发包。
- 两份清单谁是真源，代码里没有答案。

**改动空间**：把 `module.json.in` 的 `provides` 补 `slice.rgbwsvt`、`produces` 补 `p0.rgbwsvt.1`。

**成本**：S（2 行）
**风险**：**极低**。已核实 `PackageSlicerModule.ps1:260-269` 只校验 `schema` / `spi` / `dll` / `subprocess.exe` / `buildConfig` / `version` 六个字段，**不校验 `provides` 或 `produces`**，所以补齐不会触发打包门禁。

---

### F-02 🔴 本该拦住 F-01 的对拍测试整条空转

**证据**

`CMakeLists.txt:2350-2356` 注册测试时的完整命令行：

```cmake
add_test(NAME ...
    COMMAND ${Python3_EXECUTABLE}
        ".../tests/stage14c_05/ValidateModuleInfoContracts.py"
        --repo "${CMAKE_CURRENT_SOURCE_DIR}")
```

**没有传 `--debug-probe` / `--release-probe`。** 而脚本第 270 行：

```python
moduleInfo = RunProbe(probePath) if probePath else expectedInfo
...
if moduleInfo != expectedInfo:
    raise AssertionError(f"{buildConfig} runtime module info drifted")
```

`probePath` 为 `None` → `moduleInfo = expectedInfo` → **`moduleInfo != expectedInfo` 恒为假**，「运行时自述漂移」这条断言**永远不会触发**。

雪上加霜：即使接上探针也会立刻红灯——脚本第 19-56 行的 `EXPECTED_PROVIDES` / `EXPECTED_WORKER` 常量本身也**没有** `slice.rgbwsvt`，与真实 DLL 不符。

剩下唯一真正执行的检查是 `ValidateCrossConsistency(expectedInfo, manifest)`：拿 Python 常量比 `module.json.in`。这两份**恰好都是旧的**，所以互相对得上 —— **两份陈旧产物互相印证，真实产物在外面独自漂移**。

**影响**：这是 F-01 能存在的直接原因。更严重的是它说明「有一个绿灯的对拍测试」不等于「这条不变量被守住了」。

**改动空间**

1. CTest 注册时接上探针：需要一个 `--print-json` 的模块信息探针目标（脚本的 `RunProbe` 已经预期了这个接口），把 Debug/Release 两个产物路径传进去。
2. 同步 `EXPECTED_PROVIDES` / `EXPECTED_WORKER`（否则第 1 步一接上就红）。
3. **把「探针缺失」从静默降级改成硬失败**——这是根治点。当前 `if probePath else expectedInfo` 这个写法本身就是缺陷模式：可选参数缺失时悄悄退化成自我比较。

**成本**：S–M（主要成本在做探针目标；若已有可复用目标则是 S）
**风险**：低。修复后可能暴露其他既有漂移，那是收益不是风险。

> 此条与既有经验一致：**对拍测试全绿不等于验过；接受它作安全网前先断言被测分支确实触发。**

---

### F-03 🟡 版本清单的兼容协议表同类漂移

**证据**：`version-manifest.json` `compatibility.contracts` = `["slicer-module.spi.v1", "file_contract_v1", "p0.rgbwsv.2"]` —— **缺 `p0.rgbwsvt.1`**。

**影响**：与 F-01 同类，影响面更小（这份清单主要用于版本治理展示）。

**改动空间**：补一项。
**成本**：S ｜ **风险**：低（需确认 `tests/version/ValidateVersionContracts.py` 是否对该数组有断言）

---

### F-04 🟠 宿主用裸子串判定模块能力

**证据**：`apps/slicer_ui_host_sim/HostSliceProtocolRoute.cpp:27`

```cpp
if (!moduleInfo.contains("slice.rgbwsvt"))
```

`moduleInfo` 是 `QByteArray`，装的是整份 `pm_module_info` JSON 文本。这是**对整份 JSON 做子串匹配**，不是解析 `provides` 数组。

**影响**：只要 `"slice.rgbwsvt"` 这 14 个字符出现在 JSON 的**任何位置**——未来的 `capabilities` 描述、错误示例、注释字段、甚至某个能力名的前缀——判定就会返回「支持」。反过来，如果模块把能力名换成等价的转义形式（`.` 表示点号，JSON 合法），判定会返回「不支持」。

这是典型的「用文本匹配代替结构解析」，在能力集会增长的系统里迟早出事。

**改动空间**：`QJsonDocument::fromJson(moduleInfo)` → 取 `provides` 数组 → 精确元素匹配。宿主已经在别处大量使用 `QJsonObject`，没有新依赖。

**成本**：S ｜ **风险**：极低

---

### F-05 🟠 公共能力 DTO 与私有 Worker 契约的版本节奏脱节

**证据**

| 契约 | 版本 | 是否含 `slice.rgbwsvt` |
|---|---|---|
| `contracts/file_contract_v1.md`（私有，DLL↔Worker） | major 1 / **minor 1** | ✅（schema `enum` 里有） |
| `contracts/slicer_capability_dtos.json`（公共，宿主↔DLL） | `contractVersion 1.14` | ❌ 全文 **0 次**出现 `rgbwsvt` |
| 同上 `protocolInvariants.workerOnly` | — | 只列 `["slice.rgbwsv", "geometry.repair"]` |

即：**私有契约已经支持并生产 RGBWSVT，公共契约完全没提这件事**，宿主只能靠 F-04 那个子串匹配去发现它。

**影响**：公共 DTO 是宿主开发者（包括未来的 PrintApp 团队）的唯一权威参考。一条已经在生产路径上跑的能力不在公共契约里，等于这条路径**没有对外契约**。

**改动空间**：在 DTO 里补 `slice.rgbwsvt` 能力条目 + `workerOnly` 补一项 + `protocolInvariants.packageSchema` 从单值改成集合（当前写死 `"p0.rgbwsv.2"`）。

**成本**：M（要同步 `.json` + `.md` + `tests/contracts/ValidateCapabilityDtos.py`）
**风险**：中。DTO 是冻结契约，变更按 `AGENTS.md` 属受控修订，**需要用户裁定并留决策文档**。
**注意**：`contracts/slicer_capability_dtos.{json,md}` 与 `tests/contracts/ValidateCapabilityDtos.py` **当前在工作树中已被修改未提交**，改动前须先确认那批改动的意图，避免冲突。

---

### F-21 🟠 一个对外错误码在契约里根本不存在

**证据**（2026-09-08 复核）

`PM-SLICER-VIEWDATA-SIMPLIFICATION` 被当作对外错误码返回，出现在至少 5 处：

```
src/slicer_core/api/viewdata/MeshSimplifier.cpp:156, 166, 304, 319
src/slicer_core/api/viewdata/SceneViewMeshBuilder.cpp:621
```

而 `contracts/slicer_error_codes.json` 里**没有声明它**（该文件共声明 19 个码，代码共使用 19 个码，恰好一进一出：多了 `VIEWDATA-SIMPLIFICATION`、少用了 `LAYOUT-0021`——后者实际在 `apps/slicer_worker/` 里使用，不是问题）。

它也**不符合** `file_contract_v1.result.schema.json:39` 的码格式：

```
^PM-SLICER-[A-Z]+-(?:[0-9]{4}|STALE|BUDGET)$
```

`SIMPLIFICATION` 既非四位数字，也非 `STALE`/`BUDGET`。

**影响**

- 宿主可以收到一个**契约里不存在的错误码**。宿主若按错误码表做分支处理（这是错误码存在的意义），这一条会落到 default 分支。
- 目前没有炸，因为 `scene.get_viewdata` 是进程内能力，不经过 `result.json`，所以格式正则管不到它。**但这是巧合而非设计**：如果将来 ViewData 相关工作被移到 Worker（并非不可想象——大场景 ViewData 生成本身就重），这条码会立刻违反结果 schema。

**改动空间**：二选一

1. 在 `slicer_error_codes.json` 声明它，并把码名改成符合正则的形式（如 `PM-SLICER-VIEWDATA-0080`）——需同步 5 处代码与相关测试。
2. 声明它并显式记录「非数字后缀码仅限进程内能力」这一例外（`BUDGET`、`STALE` 已经是这类例外，说明这个设计本来就允许语义后缀）。

倾向 **2**：`PM-SLICER-VIEWDATA-BUDGET` 已经在 DTO 里存在且同样是语义后缀，说明例外是既定风格；把 `SIMPLIFICATION` 补进去比改码名成本低、破坏面小。

**成本**：S ｜ **风险**：低
**根治办法**：这一条与 F-01/F-03/F-05 同源，应当并入 R-10 的能力/错误码单一真源，并加一条对拍——「代码里出现的每个 `PM-SLICER-*` 字面量都必须在契约中声明」。这条对拍脚本约 20 行，本轮已用一次性脚本验证过它能跑出结果。

---

## B 类：分层与拓扑（3 条）

### F-06 🟠 base/engine 分层靠 CMake 白名单而不是目录结构

**证据**：`cmake/SliceSoftCoreLayering.cmake` 全文。分类逻辑 = 6 条路径前缀 + 33 条精确文件名（`baseExactStems`）+ 1 条精确源文件 + 3 条反向例外（`engineExactSources`），其余归 engine。

**影响**

- 好的一面：**默认方向安全**——新文件默认落 engine，即默认不进 DLL。
- 坏的一面：把一个重计算文件放进 `src/slicer_core/api/`、`scene/`、`layout/`、`model/`、`importers/` 任一目录，它会**静默进 base 并被链进 DLL**，没有任何警告。分层保护完全依赖开发者知道这份名单存在。
- 名单本身**没有自检**：没有测试断言「`baseExactStems` 的每一条都对应真实文件」（改名/删文件后名单会静默失效），也没有断言「base 集合的包含闭包不触及 engine 目录」。
  > **2026-09-14 更正**：本条低估了既有门禁。`slicer_stage14b_layering_feasibility_test` 确实是一条分层门禁，但它检的是「`model.cpp` 是否越出冻结 base parser 依赖边界」，不检名单完整性。**且它当前是红的**——实测报 `model.cpp acquired a project dependency outside the frozen base parser boundary`，命中 `model/AssetReferencePath.h`、`ObjFaceParser.h`、`MtlMaterialParser.h`、`FrameGeometry.h`。准确说法是：分层自检存在但覆盖面不同，且这条已红的门禁无人处理。
- 现有的链接期门禁（`model_import_layering_probe`、`package_query_facade_14b02_base_link_test`）能兜住**符号越界**，但兜不住**误分类**——一个重文件被分到 base 只会让 DLL 变大变重，不会产生链接错误。

**改动空间**

- 方案 1（彻底）：把 base 文件物理搬进 `src/slicer_core/base/`，规则退化成一条前缀。代价是 400+ 文件的路径变更，会与所有在飞分支冲突。
- 方案 2（渐进，推荐）：保留当前名单，但**加一条自检测试**：断言 `baseExactStems` 每条都存在、断言 base 源集合里没有 `pipeline/` `engine/` `support/` `materials/` `raster/` 前缀。这是纯增测试，零风险。
- 方案 3：把名单从 CMake 挪到一份 JSON，CMake 与自检脚本共读，避免规则只有 CMake 能读。

**成本**：方案 2 = S ｜ 方案 3 = M ｜ 方案 1 = L（且冲突面大）
**风险**：方案 2 极低；方案 1 高（大规模路径变更）

---

### F-07 🟠 RIP 集成没有走 SPI，形成第二条不同形状的能力拓扑

**证据**

| 事实 | 位置 |
|---|---|
| SPI 声明「切片、RIP、未来扩展**都**通过本接口接入」 | `contracts/print_module_spi.h:5` |
| 实际由 Qt 宿主 `QProcess` 直接拉起 `rip_cli.exe` | `apps/slicer_ui_host_sim/HostRipJobController.cpp:865` |
| RIP 作业控制器 1 373 行，住在 UI 可执行文件里（全仓第 7 大文件） | 同上 |
| `src/rip_integration`（1 951 行）作为静态库直接链进宿主 exe | `apps/slicer_ui_host_sim/CMakeLists.txt` `target_link_libraries(... slicesoft_rip_integration ...)` |
| RIP 用自己的清单体系 `rip_module.json` 而非 `pm_module_info` | `HostRipJobController.cpp:235` |
| RIP 有独立 schema 与打包脚本，但**不实现 `pm_*`** | `contracts/slicesoft.rip.module.1.schema.json`、`scripts/PackageRipModule.ps1` |

**影响**

- 宿主里有**两套**：作业控制器、进度解析、取消逻辑、错误码映射、设置持久化、模块清单体系。
- 生产形态里 PrintApp 要集成 RIP 得再实现一次 QProcess 调度，而不是复用它已写好的 `pm_*` 装载器。**这直接抵消了做 SPI 的目的。**
- `RipOutputValidator`（820 行）等验证逻辑被绑死在 Qt 宿主里，无法独立发版或被非 Qt 宿主复用。

**改动空间**

- 目标形态：`rip_module.dll` 实现同一套 11 个 `pm_*`，内部 `CreateProcess` 调 `rip_cli.exe`（与 `slicer_module` 完全同构）；宿主只保留一份 `ModuleClient`。
- 收益：宿主 exe 减约 3 300 行；两条拓扑合一；RIP 可独立版本化；`src/rip_integration` 的验证逻辑变成可独立测试的库。
- 复杂点：RIP 侧是外部 SDK 二进制（`rip_project/` 被 `.gitignore` 排除，无源码，`status: LOCAL_ENGINEERING_ONLY`），边界要重新定义；且 `AGENTS.md` 记录 RIPFLOW E-01/E-02 外部分发与生产验收为 `BLOCKED_EXTERNAL`。

**成本**：L ｜ **风险**：中高。**需要用户立项授权**，且应等 RIPFLOW 外部阻塞状态明朗后再动。

---

### F-08 🟡 CLI 绕过 DLL/Worker，「生产必须经 Worker」在引擎边界不成立

**证据**

- `apps/slicer_cli/main.cpp:1-12` 直接 include `slicer_core/slicer.h`、`engine/ProductionSliceFacadeFactory.h`，链接 `slicer_core`（= engine）。
- `main.cpp:547` 直接 `slicer_core::run_slicer(options.config_path, runOptions)`。
- `MultiModelProductionService.cpp:1019-1022`：`productionAcceptance = contract.production ? "admitted" : "functional_fixture_admitted"` —— 准入标记由 effective config 的 `production` 布尔决定，**与调用者是 Worker 还是 CLI 无关**。

**影响**：模块边界上的三道防线（DLL 不链引擎 / `backend` 只接受 `worker` / DTO `noInProcessFallback`）都是硬的，但**引擎边界是软的**——CLI 拿一份 `production=true` 的配置就能产出标记 `admitted` 的包。`file_contract_v1.md` §1 用文字声明了「CLI 包仍是候选」，但代码里没有对应的强制。

**改动空间**

- 方案 1（记账，推荐）：在 `docs/slice` 明确写下 CLI 通道的准入语义与它与 Worker 通道的差异，把「靠共识」变成「有文档」。
- 方案 2（强制）：给包写入器加一个「产出者身份」字段，CLI 产出的包只能是候选。代价是要改包契约（p0.rgbwsv.2 是冻结契约），成本远高于收益。

**成本**：方案 1 = S ｜ **风险**：低
**判定**：CLI 是内部开发者工具，不分发给终端用户，方案 1 足够。**不建议封堵。**

---

## C 类：巨型文件与内聚（3 条）

### F-09 🔴 `slicer.cpp` 5 888 行是唯一的技术债主体

**证据**（2026-09-08 复核）：`wc -l src/slicer_core/slicer.cpp` = **5 888**（09-06 时为 6 071；MEMFLOW 的三次「G2 同步下沉」提交把它拉回了一部分）。结构见 `02_模块级评估.md` §5.5 的逐段拆解。要点：

- 匿名 namespace 占 66–4117 行（**4 051 行，占 69%**），内含 25 个结构体 + 数十个自由函数，**全部无法被单元测试直接触达**。
- 对外只有 2 个 `run_slicer` 重载。
- 至少 7 个不相关关注点混在一起，其中手写 CRC32/adler32/zlib/PNG 编码（约 100 行，535–655 行）**与切片语义完全无关**。
- 它是四个在飞专项（MATOPQ / MO-04 / MATVOL / TRANSFER）的共同修改点。

**影响**

- 单元测试不可达 → 验证只能靠 golden 包整体对拍 → 定位一个语义 bug 的成本极高。
- 变更热点 + 巨型文件 = 合并冲突的常驻来源。
- 编译单元巨大，任何改动触发全文件重编（`/bigobj` 已经必须开）。

**改动空间**：见 `06_` R-12。核心原则是**小步搬 + 每步 golden 对拍**，绝不一次性重构。第一刀应当切在与切片语义无关的部分（PNG/zlib 编码 → `src/slicer_core/output/png/`），因为它零语义风险且能立刻验证方法论是否成立。

**成本**：XL（整体）｜ 单步 S–M
**风险**：**高**。这是生产路径核心。任何一步都必须有 golden 零漂移证据。

---

### F-10 🟠 单一 `SliceConfig` 承载全部行为，引擎入口是一个文件路径

**证据**：`config.h` 488 行 **39 个结构体**（`grep -c "struct " config.h` = 39），全部聚合进 `SliceConfig`；`config.cpp` 1 485 行；引擎公开入口 `slicer.h:137,145` 是 `run_slicer(const std::filesystem::path& config_path)`。

**影响**

- 「哪些配置项影响哪个车道」无法从类型判断，只能靠 `DiagnosticEffectiveConfig`（855 行）事后解释。
- 每个新专项都往同一个 `SliceConfig` 挂结构体，配置面只增不减。
- 任何配置变更都触碰 `config.cpp`，而 `config.cpp` 已被 G2 豁免——**豁免变成常态**。

**改动空间**：按车道把 `SliceConfig` 拆成子配置（`LegacyConfig` / `GlobalShellConfig` / `MatvolConfig` / `TransferConfig` / `SamplingConfig`），入口改成接受结构体而非路径（保留文件路径重载作为兼容）。

**成本**：L ｜ **风险**：中高（`config.cpp` 的加载与校验逻辑是所有 golden 的前置）
**优先级**：低于 F-09。配置膨胀主要代价是可理解性，不产生正确性风险。

---

### F-11 🟡 G2 行数豁免无上限且理由文本已过期

**证据**：`scripts/SourceSizeGuardConfig.json` 的 `slicer.cpp` 条目：

```json
"expiresWhen": "版本重构时统一清理 slicer.cpp（现 5645 行）"
```

实际当前 **5 888 行** —— 仍比豁免理由文本里的数字多 **243 行**，且豁免条目**不带行数上限**，`expiresWhen` 是一个没有触发条件的自然语言描述。

同一份配置里 `slicer_cli/main.cpp` 与 `config.cpp` 也有 G2 豁免，同样无上限。

另据 `AGENTS.md` 记录：**仓库全扫描未进 CTest，只注册 `--self-test`**——那部分违规确实是静默债。

> **2026-09-14 更正**：但「全是静默债」不准确。宿主侧的行数门禁 `slicer_stage14e02_qt_host_boundary_test` **已注册进 CTest 且当前是红的**：实测报 `host source exceeds 500 lines and is not in the debt ledger: HostMainWindow.cpp (516)`。现状是「仓库全扫描静默、宿主侧已红」，不是全部静默。

**影响**：门禁在形式上存在，在实际上不产生阻力。「豁免」变成了「永久许可」。

**改动空间**

1. 给每个豁免条目加 `maxLines`（例如冻结在授予时的行数），超过即红。
2. 把仓库全扫描注册进 CTest（`AGENTS.md` 已记录这是已知缺口）。
3. 把 `expiresWhen` 从自然语言改成可判定条件。

**成本**：S（配置 + 脚本小改）｜ **风险**：低，但会**立刻暴露 12 处既有 ERROR**（`AGENTS.md` MEMFLOW 段已逐条列出），需要用户先裁定这批债怎么处理。
> 相关经验：**门禁放宽必须先出授权文档留痕；别的专项的债不要在这里替它承接。** 收紧门禁同理——先定责再收紧。

---

## D 类：进程与协议实现（5 条）

### F-12 🟠 Worker 日志与进度在 DLL 内无界累积

**证据**：`WorkerClient.h` 的 `WorkerRunResult` 含四个无上限容器：
`progressEvents` / `timingEvents` / `stdoutLogLines` / `stderrLogLines`。
写入点：`WorkerProtocol.cpp:280`（每个进度事件 `push_back`）、`:303`（每个计时事件）、`:306`（每条普通 stdout 行）、`:213,321`（每条 stderr 行）。**全程无淘汰。**

**影响**：1 429 层逐层进度 ⇒ 至少 1 429 个 `WorkerProgressEvent`（每个含 `std::string phase`），加上引擎的普通日志。单次作业不至于爆内存，但这是**结构上没有上界**的增长，且累积在**宿主进程**里（DLL 在宿主地址空间）。

**改动空间**：环形缓冲 —— 进度保留「首 N 条 + 最近 N 条 + 溢出计数」，日志保留最近 N KB + 溢出计数。改动局限在 `WorkerProtocol.cpp` 与结构定义，**不触碰任何契约**（契约只规定了行语法，没规定模块要保留全部行）。

**成本**：S ｜ **风险**：低（需确认没有测试依赖「全部日志行都在」）

---

### F-13 🟡 `maxConcurrentJobs = 1`，进程隔离的并行红利未使用

**证据**：`ModuleInfo.cpp:41` `"maxConcurrentJobs":1`；`WorkerJobService.cpp:695-704` 遍历现有作业，发现同模块有非终态作业即返回 `PM-SLICER-RESOURCE-0041 / maxConcurrentJobs=1`。

**影响**：私有作业目录已按 `pid/module-id/job-id` 三级隔离，进程模型本就支持并行，但被主动关死。对 22 实例批量生产是硬上限。

**改动空间**：把上限做成 `pm_create` 的 `options_json` 参数——**SPI 早就预留了这个入口**（`print_module_spi.h` 的 `pm_create(const char* options_json)`，且 `pm_module_info` 已把 `maxConcurrentJobs` 声明为宿主可读字段）。

**成本**：M ｜ **风险**：**中高**。N 个并发 Worker × 62 GB 的内存画像必须先解决，否则并发只是把 OOM 变快。
**建议**：等 MEMFLOW 有界窗口（目标 134 MB）落地并接生产路径后再评估。**现在不动。**

---

### F-14 🟡 10 ms 轮询循环占用一个专用线程

**证据**：`WorkerClient.cpp:26` `PollIntervalMs = 10`；`:326` `while (WaitForSingleObject(process, PollIntervalMs) == WAIT_TIMEOUT)` 循环内做管道排空与取消判定。每个作业一个 `std::thread`（`WorkerJobService.cpp:707`）。

**影响**：并发上限是 1，所以最多一个线程在 10 ms 轮询——**实际影响可忽略**。但如果 F-13 放开并发，N 个作业 = N 个轮询线程。

**改动空间**：改成 `WaitForMultipleObjects` + 管道事件驱动。**当前不值得做**，仅在放开并发时一并评估。

**成本**：M ｜ **风险**：中（重写已验证的进程管理代码，收益不明）
**判定**：**不做。** 记录在案即可。

---

### F-15 🟡 三段文件序列化 + 模型重复解析

**证据**：链路见 `01_架构全景与证据基线.md` §2。三次序列化：① 宿主 → DLL 的 ABI JSON 字符串；② DLL 写 `request.json`；③ Worker 物化 effective config 文件供引擎 `run_slicer(config_path)` 读回。模型文件在 DLL（`model.import` / viewdata / collision）与 Worker（切片）各解析一次。

**影响**：控制流的序列化开销可忽略（JSON 很小）。真正的成本是**模型重复解析**——同一个大模型两份三角网同时在内存里。

**改动空间**：见 `03_` §5.1。**前置条件是先测量**。

**成本**：M–L ｜ **风险**：中
**判定**：**不测不动。**

---

### F-20 🟡 合同协商每个作业都重跑，契约允许的缓存未使用

**证据**：`WorkerJobService.cpp:465-470` 在每个作业线程里无条件构造 `WorkerContractNegotiator` 并 `Negotiate`，全文件无缓存字段。所以**一个作业 = 两次进程启动**（`--contract-info` + `--spi-request`）。

而 `contracts/file_contract_v1.md` §2 原文是：「模块必须在向某个 Worker 身份提交**第一个**作业之前执行 `--contract-info`」——**契约明确预期按 Worker 身份缓存**。

**影响**：每作业多一次进程创建 + 一次 JSON 解析。对 30 分钟的切片作业可忽略；对 `geometry.preflight.full` 这类可能只跑几百毫秒的作业，占比可能显著。

**改动空间**：按「Worker 可执行文件绝对路径 + 文件 mtime + size」缓存协商结果于模块实例生命周期。改动局限在 `WorkerJobService`，**不触碰任何契约文件**（契约本就允许）。

**成本**：S ｜ **风险**：低（缓存键含 mtime+size，Worker 被替换时自动失效）

---

## E 类：工程与验证（4 条）

### F-16 🔴 无 CI

**证据**：仓库无 `.github/workflows` 目录。`AGENTS.md` 记录「CI ⏸ 用户 2026-08-10 裁决【暂缓】，清单保留不开工」，任务卡 `docs/codex_task/current/TASKS_CI_冻结面工程保护任务清单.md`。

**影响**：234 项回归（B 级）全靠人工本机跑。`AGENTS.md` 里那两条醒目警告本身就是没有 CI 的症状：

> 「构建与回归必须分开判定退出码：`cmake --build ... | tail` 之类的管道会用管道末端命令的退出码掩盖真实的构建失败，而失败的配置会让随后的 ctest 对陈旧二进制报 PASS。」
> 「仓库根下的 `build/` 是陈旧目录……它残留的 CTestTestfile 仍能让 ctest 跑起来并对陈旧二进制报出与基线不符的结果。」

**这两个陷阱在 CI 里根本不会存在**（干净 checkout + 单一构建目录 + 退出码即结论）。

**改动空间**：`AGENTS.md` 明确记录 CI 的两个前置是「等解除暂缓 + 定 runner」。**这是产品/资源决策，不是技术决策，不能自行开工。**

**成本**：L ｜ **风险**：低（纯增）
**判定**：**待用户裁定。** 本报告只记录代价。

> 相关经验：**验证要用产出过基线的构建目录；换了目录才崩就先怀疑目录。** 这条经验之所以必要，正是因为没有 CI 强制单一构建环境。

---

### F-17 🟠 无测试框架，148 个可执行目标各写一份 `main`

**证据**：`CMakeLists.txt` 全文无 `gtest` / `Catch` / `doctest` 字样。抽样 `tests/unit/multimodel_scene_contract/Main.cpp:13-22` 是手写的：

```cpp
bool ExpectTrue(const bool condition, const std::string& message)
{
    if (!condition) { std::cerr << "FAIL " << message << '\n'; return false; }
    return true;
}
```

这个 helper（或其变体）在 185 个测试文件里被重复实现。

**影响**

- **失败时不报实参**：`AGENTS.md` 记录「断言实参求值顺序致失败不报原因，全仓 85 处；已证【非】机械改动，批量改写试过并回退」。失败只告诉你哪条断言挂了，不告诉你实际值是多少——**对一个用 golden 对拍做主要验证手段的项目，这是很贵的**。
- 每个测试是一个独立可执行目标 → 148 个链接单元 → 构建时间与 CMake 体积（F-18）。
- 无自动发现、无过滤、无参数化、无 fixture 复用。

**改动空间**

- 引入 header-only 框架（doctest 单头文件最轻，Catch2 v3 需构建）。
- **不要动老用例**：新用例走框架，老用例保持原样自然衰减。强行批量改写已被证明是错的（`AGENTS.md` 记录 PC-12 试过并回退）。
- 中间方案：抽一个共享的 `tests/support/Expect.h` 提供带实参打印的宏，新老用例都可以逐步替换单个断言——**改动粒度是单条断言而不是整个文件**，风险最低。

**成本**：M（共享 header）～ L（引入框架并迁移）
**风险**：低（纯增，不改产品代码）
**推荐**：先做共享 `Expect.h`，解决 85 处「失败不报原因」的实际痛点；框架迁移单独立项。

---

### F-18 🟠 单文件 3 315 行 CMake 承载 173 个目标

**证据**（2026-09-14 复核）：`CMakeLists.txt` **3 315** 行；`add_executable` **163** 个、`add_library` 10 个、`add_test` **198** 个（另有 **44** 个在 `apps/slicer_ui_host_sim/CMakeLists.txt`）。09-06 时分别是 3 113 / 148 / 183 / 40——**八天净增 15 个可执行目标、19 条测试、202 行 CMake**。

**影响**

- 任何目标改动都触碰同一个文件 → 常驻合并冲突点。`AGENTS.md` 已记录 MEMFLOW 分支的 merge 冲突面「仅 `CMakeLists.txt` 与 TASKS_16 两处」——`CMakeLists.txt` 就是那个「两处」之一。
- 173 个目标里绝大多数是测试与开发工具；**产品目标只有 5 个**（`slicer_module` / `slicer_worker` / `slicer_ui_host_sim` / `slicer_host_sim` / `slicer_cli`）。产品与工装混在一个文件里，看不出哪些是必须构建的。

**改动空间**

1. 按域拆分：`cmake/targets/core.cmake` / `module.cmake` / `worker.cmake` / `tests.cmake` / `tools.cmake`，根文件只做 `include()`。
2. 测试目标批量化：绝大多数测试目标形状一致（一个 `Main.cpp` + 链一个库 + `/W4 /WX` + `add_test`），可以用清单 + `foreach` 生成，**把 163 个手写块压成一份清单**。这条收益最大。
3. 加一个 `SLICESOFT_BUILD_TOOLS` 开关，默认关掉 12 个演示/矩阵工具。

**成本**：M ｜ **风险**：中（CMake 重构会短暂动摇构建；必须在一个干净构建目录里逐步验证，且**不能在有在飞分支时做**——冲突面正是要解决的问题本身）

---

### F-19 🟡 仓库根 8 个 `build*` 目录，其中一个已知危险

**证据**：`build/`、`build-12c-ui/`、`build-codex-memory-opt/`、`build-guide-copy-check/`、`build-guide-copy-ninja/`、`build-openvdb-09p/`、`build-rip-audit/`、`build-slicesoft/`、`build-slicesoft-ninja/`、`build-slicesoft-nmake/`（共 10 个，`.gitignore` 已排除 `build/` 与 `build-*/`）。

`AGENTS.md` 明确警告 `build/` 陈旧（无 vcpkg toolchain、`meshoptimizer_DIR-NOTFOUND`、不含任何 matvol 目标），且**其残留的 CTestTestfile 仍能让 ctest 跑起来并对陈旧二进制报出与基线不符的结果**。

**影响**：正确的构建目录只有一个（`build-slicesoft/main`），其余 9 个是过期实验残留，其中至少 1 个会主动误导验证结论。

**改动空间**：清理陈旧目录（**属破坏性操作，须用户确认**）；或至少在 `build/` 里放一个 `STALE_DO_NOT_USE.md` 并删掉其 `CTestTestfile.cmake` 让 ctest 直接失败而不是假 PASS。

**成本**：S ｜ **风险**：低，但**删除需用户明确授权**（`AGENTS.md` 第 6/7 条）

> 相关经验：**并行会话共享同一工作树**——别的构建会编译未提交改动，也会静默盖掉交叉提交。多构建目录并存会放大这个问题。

---

## F 类：目录与资产冗余（2026-09-15 实测增补）

用户 2026-09-15 提出「`contracts`、`samples/models` 与 `model`、`samples/configs` 是否有无效或重复区域」。**实测了，结论是三条里两条否定原假设、一条成立。** 先记否定的两条，避免后人重复排查：

### 已排除：`contracts/` 无冗余 ✅

39 个文件**全部有引用，零孤儿**（全仓 grep 交叉核对，排除构建与产物目录）。**不需要清理。**

### 已排除：`samples/configs` 无重复配置 ✅

130 个配置**零逐字节重复**。存在 28 组「JSON 键结构完全相同」的分组（最大一组 13 份，`openvdb/surface_shell_*`），但那是**同形状不同取值的参数化测试矩阵**，是刻意设计，不是该删的冗余。**不需要清理。**

---

### F-37 🟡 `model/` 与 `samples/models/` 是两棵并行资产树

**证据**（2026-09-15 实测）

```
model/           346 文件 / 765.6 MB   （.obj 123, .png 122, .mtl 82, .stl 8）
samples/models/   90 文件 / 347.1 MB   （.obj 41, .mtl 23, .3mf 12, .png 10）
逐字节完全相同的文件：9 个
```

且**命名体系完全不同**，同一份资产在两处叫不同名字：

```
model/obj/meigui_fudiao/02.obj        ==  samples/models/rose.obj
model/obj/titian_fudiao/dmz.obj       ==  samples/models/0.33.obj
model/3mf/meigui_fudiao/02.3mf        ==  samples/models/3mf/02.3mf
model/obj/meigui_fudiao/dise(2).png   ==  samples/models/relief/dise(2)_1.png
model/obj/reality/finger_suoguo/1-2.png == samples/models/1-2.png
```

**影响**：合计约 1.1 GB 的两套资产树，9 个文件重复只是表象；真正的代价是**同一份模型有两个身份**——写测试时不知道该引哪棵树，且 `model/` 里的目录名（`meigui_fudiao` / `titian_fudiao`）承载语义而 `samples/models/` 的扁平名（`rose.obj` / `0.33.obj`）不承载。

**改动空间**：需要先回答一个产品问题——`samples/` 是「随仓库分发的最小样例集」还是「测试 fixture」？两者定位不同，合并方式也不同。**在这个问题回答之前不应动手**，否则只是把混乱从两处搬到一处。

**成本**：S（仅去重 9 个文件）～ M（统一身份与命名）｜ **风险**：中（大量测试与脚本按路径引用资产，改路径会广泛触发）

---

### F-38 🟡 RIP 三族 schema 各有三代并存，v1/v2 源码不消费

**证据**（2026-09-15 实测，源码级消费点统计）

| schema 族 | v1 | v2 | v3 |
| --- | --- | --- | --- |
| `slicesoft.rip.diagnostic.*` | 源码 0 处 | 源码 0 处 | **3 处** |
| `slicesoft.rip.result.*` | 源码 0 处 | 源码 0 处 | **3 处** |
| `slicesoft.rip.settings.*` | **2 处** | **2 处** | **3 处** |

`settings` 三代都在消费是**合理的**——`7e129b9a`「墨量配置接入双 RIP 模式并迁移旧设置合同」需要读旧版做迁移。

但 `diagnostic` 与 `result` 的 v1/v2 **源码一处不消费**。

**影响**：轻微。它们不产生正确性风险，只是让 `contracts/` 看起来比实际更复杂，且后人无法从目录判断哪一代是现役。

**改动空间**：**不能凭「源码没引用」就删**——它们可能仍用于读取历史产物或外部 RIP 交付物。正确做法是请 RIPFLOW 专项确认去留，或在文件内加一行 `"status": "superseded"` 标注现役代次。

**成本**：S ｜ **风险**：低（若确认为死档）/ 中（若外部仍在用）
**前置**：需 RIPFLOW 专项确认，本轮不自行处置。

---

### 顺带量到（不立 F 编号）

`runtime/` 有 **16 947 个文件、50.3 GB**（6 726 个 `.tiff` + 3 711 个 `.tif` + 1 626 个 `.json` + 1 308 个 `.obj`），是 `.gitignore` 的运行产物堆积。**不影响源码**，但在 C 盘常年紧张的环境下会变成真问题。清理属破坏性操作，须用户明确授权。

---

## G 类：仓库目录架构（2026-09-15 实测增补）

用户 2026-09-15 提问「当前切片软件目录中架构是否合理，是否可进行优化」。**判断前先把两件事分开：已跟踪结构才是架构，磁盘上的未跟踪目录是工作副本卫生。**

```
已跟踪一级目录   17 个
磁盘未跟踪目录   18 个 / 162 GB
```

### 已排除：产品面目录架构合理 ✅

```
src/        产品源码（slicer_core / slicer_module / rip_integration / third_party）
apps/       可执行目标        contracts/  契约
cmake/      构建模块          tests/      测试
scripts/    工具              docs/       文档
licenses/   法务              model/ samples/  资产
rip_module/ 外部 SDK 配置
```

标准且清晰。**`contracts/` 独立成一级目录是这个项目最对的结构决策之一**——它让契约成为一等公民，而不是散落在 `include/` 或 `docs/` 里，也是 `01_`~`03_` 能把契约当独立分析对象的前提。

**结论：产品面架构不需要优化，不建议动。** 下面两条都在外围。

---

### F-39 🟡 AI 工具脚手架在根目录蔓延到 9 个条目

**证据**（2026-09-15 实测）

| 已跟踪 | 跟踪文件数 | 最后活动 |
| --- | ---: | --- |
| `.agents/` | 35 | 2026-09-07（活跃） |
| `.codex/` | 9 | 2026-08-24 |
| `ai_workspace/` | 59 | **2026-07-28（停滞）** |
| `ai-project-template/` | 32 | **2026-07-01（停滞）** |
| `.agents.7z` | 归档 4.6 KB | 2026-07-01 |

外加未跟踪的 `.claude/`、`.dish/`、`.qodo/`、`.specstory/`（34 MB / 107 文件）。

**值得单独说的一点**：`ai-project-template/` 装的是 `.agents/docs` 的**通用子集**（11 个文档 vs `.agents/` 的 15 个，少掉的是 `context-handoff.md`、`r0-r1-roadmap.md`、`skill-migration-map.md`、`SLICE_AI_SKILL_MASTER.md` 四个项目专属文档）。

所以它**不是**副本，而是一份**给其它项目用的可复用模板**——却住在本项目的仓库里。这是定位问题，不是冗余问题。

**影响**：轻微，不产生正确性风险。代价是根目录的信噪比：一个新人 `ls` 仓库根，看到的 9 个 AI 相关条目多于产品源码目录（`src/` + `apps/` 共 2 个）。

**改动空间**：`ai-project-template/` 迁出到独立仓库；`ai_workspace/` 与 `.agents.7z` 确认去留；其余 AI 配置目录考虑收敛到单一父目录。**均属破坏性操作，需用户单独授权，本轮不动手。**

**成本**：S ｜ **风险**：低（但需确认各目录是否仍被外部流程引用）

---

### F-40 🟡 两个陈旧的根级已跟踪文件

**证据**

```
AGENTS.md.backup-20260604-140819   1769 字节   2026-06-04   （现 AGENTS.md 已 32 KB）
.agents.7z                         4626 字节   2026-07-01   （.agents/ 有 35 个跟踪文件）
```

前者是三个多月前的备份，内容规模不到现版本的 6%，已无参考价值；后者是同名目录的归档。

**影响**：纯噪音，合计 6.4 KB。

**改动空间**：删除。**属破坏性操作，需用户明确授权。**

**成本**：S ｜ **风险**：极低

---

### F-19 升级：工作副本污染的量化

原 F-19 只记了「仓库根 10 个 `build*` 目录，其中 `build/` 已知陈旧」。2026-09-15 实测量化：

| 性质 | 文件数 | 体积 |
| --- | ---: | ---: |
| **产物**（`output/` `runtime/` 9 个 `build*/`） | **149 673** | **162.6 GB** |
| 资产（`model/` `samples/`） | 568 | 1 113.0 MB |
| 测试 | 1 413 | 37.8 MB |
| 文档 | 1 169 | 11.6 MB |
| **产品源码**（`src/` `apps/`） | **751** | **5.4 MB** |

```
output/           81 036 文件 /  82.0 GB   ← 单目录最大
runtime/          17 242 文件 /  51.6 GB
9 个 build* 目录  约 51 000 文件 /  29.0 GB   ← 只有 build-slicesoft 是规范的
```

**产品源码与构建产物的体积比约 1 : 30 000。**

这不改变任何架构判断——它们全部 `.gitignore` 排除、不入库。但在 `AGENTS.md` 已记录「C 盘常年紧张」「陈旧 `build/` 会让 ctest 对陈旧二进制报 PASS」的前提下，**这是会实际咬人的卫生问题**，不只是好看不好看。

**改动空间**：清理陈旧 `build*` 与历史 `output/`。**破坏性操作，须用户明确授权并逐目录确认。**

---

### F-41 🟠 两条 hostux 测试的超时余量只有 2.4 倍，机器有负载即假失败

**证据**（2026-09-15 实测）

| 测试 | 超时上限 | 基线耗时 | 余量 | 负载下实测 |
| --- | ---: | ---: | ---: | --- |
| `hostux_preset_after_import` | 180 s | 76.63 s | **2.35×** | 170.48 s **FAIL** |
| `hostux_restored_preset_after_import` | 180 s | 74.82 s | **2.41×** | 180.03 s **TIMEOUT** |

同一时段全量回归总耗时 2306 s vs 基线 1336 s（慢 73%），机器上 `verysync`（CPU 9960）、两个 `ChatGPT`、`Feishu` 在抢资源。定向单独复跑两条**均 PASS**（142.00 s / 108.67 s）——功能无问题，纯粹是时间不够。

**影响**：这两条会在任何有后台负载的机器上随机变红，而红灯长得像真回归（功能断言其实已全部走完，输出里能看到 `PRESET_AFTER_IMPORT ready=1`、`MULTILAYER_WORKSPACE_RESTORE_PASS`）。**每一次都要花一轮定向复跑才能定责**，这是持续的排查税。

**先例**：`AGENTS.md` 记录 `hd02_real_asset_matrix` 正因同样原因把 TIMEOUT 由 900 s 抬到 1800 s（实测 555/628 s 原仅 1.43~1.62 倍余量）。**这两条是同一问题的未处理实例。**

**2026-09-15 补充：第三次复现，且揭示 ctest 超时之外还有一道内部期限。**

| 运行场景 | 耗时 | 结果 |
| --- | ---: | --- |
| 基线（全量回归内） | 74.82 s | PASS |
| P0-04 全量回归内 | 180.03 s | TIMEOUT |
| P0-04 定向单跑 | 108.67 s | PASS |
| P0-03 全量回归内 | **154.02 s** | **FAIL（输出里无任何 FAIL 行）** |
| P0-03 定向单跑 | 117.21 s | PASS |

第四行是新信息：**154 s 就判 Failed，而不是撞 ctest 的 180 s 超时**，且输出里进度标记（`MULTILAYER_WORKSPACE_RESTORE_PASS`、`PRESET_AFTER_IMPORT ready=1`、`PRE_SLICE ready=1`）全部正常打完、没有任何失败信息。

说明**测试自身还有一道内部期限**，位置在 117 s 与 154 s 之间，比 ctest 的 180 s 更早触发。所以单抬 ctest 的 TIMEOUT **不足以修掉这条**——必须同时定位并抬高测试内部的那道期限，否则抬完还会在 154 s 附近以"无信息失败"的形式复发。

这也让排查成本更高：无失败信息的红灯比超时更难定责，每次都得靠定向复跑对比才能排除。

**改动空间**：① 把两条的 ctest TIMEOUT 由 180 s 抬到 600 s（约 8 倍余量，与 hd02 抬后的比例相当）；② **同时定位测试内部那道 117~154 s 的期限并一并抬高**——只做 ① 会让失败从「超时」变成「无信息失败」，排查更难。

**成本**：S ｜ **风险**：极低（抬高超时只会让真实挂起等更久，不会掩盖功能失败——功能失败是断言红，不是超时）

---

### F-42 🟠 构建版本计数器让「定向构建 + 定向验证」不可用，每次验证都被迫全量重建

**证据**：本轮独立复现 **三次**，每次都表现为"看起来像真回归的红灯"。

| # | 场景 | 失败测试 | 报的原因 | 真实原因 |
| --- | --- | --- | --- | --- |
| 1 | P0-02 只重建 `slicer_module` | `stage14c05_module_info_tests`、`slicesoft_version_contract_test` | `0.2.465-dev != 0.2.467-dev` | 测试 exe 未重建 |
| 2 | P0-04 证伪时只重建探针目标 | `stage14d03_worker_contract_unit_tests` | `real worker reports the manifest-derived engine version` | 二进制时间戳 10:48 vs 13:31 |
| 3 | 产物清理后只重建三个目标 | `slicesoft_version_contract_test` | `slicer_cli --version drifted` | `slicer_cli` 未重建 |

三次全部在**全量重建后自愈**，语义无任何问题。

**机理**：`SLICESOFT_SLICER_IMPLEMENTATION_VERSION` 带自增构建计数（如 `0.2.467-dev`），被**编译进**每个链接它的二进制。任何一次部分构建都会推进计数，使已重建与未重建的产物报出不同版本，而多处契约测试正是比对这个版本串。

**影响（这才是要紧的）**

1. **定向验证事实上不可用**：改一个文件后想只跑相关测试，会随机撞上版本红灯。每次验证都被迫全量重建——本仓库 Debug 全量约 **10 分钟**，而定向构建约 1 分钟。
2. **红灯有伪装性**：报的是 `engine version`、`--version drifted` 这类像真回归的措辞，不是"你没重建"。我第一次就误判了，靠比对二进制时间戳才定责。
3. **叠加 F-41**：负载假失败 + 版本假失败两类噪音同时存在时，一次全量回归的红灯需要逐条定向复跑才能定责，排查成本显著。

**改动空间**

- 方案 A：让版本计数只在**显式发版**时推进，日常构建用稳定占位（如 `0.2.0-dev`）。根治，但要确认没有流程依赖每次构建都换号。
- 方案 B：把版本一致性检查从"逐二进制比对"改为"只校验与构建清单一致"，允许同一次构建内的产物共享版本。
- 方案 C（最低成本）：在 `AGENTS.md` 与任务卡模板里写明「**任何回归验证前必须全量重建**」，把它从隐性陷阱变成显性纪律。

**成本**：C = S ｜ B = M ｜ A = M（需确认发版流程）
**风险**：A/B 触碰版本治理（有 `DOC_DECISION_VERSION` 在管），需裁定；C 无风险。

**建议先做 C**——三次误判都源于"不知道有这条规则"，写下来即可消除大部分成本。

## 汇总表

| ID | 严重度 | 标题 | 成本 | 风险 | 建议 |
|---|---|---|---|---|---|
| F-01 | 🔴 | 部署清单缺 rgbwsvt 能力与包契约 | S | 极低 | ✅ **已修复**（P0-02，commit 768a0de6） |
| F-02 | 🔴 | 模块自述对拍测试整条空转 | S–M | 低 | ✅ **已修复**（P0-04，commit 4596a771；静默降级分支已删除而非补参数） |
| F-03 | 🟡 | 版本清单缺 p0.rgbwsvt.1 | S | 低 | ✅ **已修复**（P0-02，随 F-01 一起） |
| F-04 | 🟠 | 宿主用裸子串判定能力 | S | 极低 | ✅ **已修复**（P0-05，commit 0a261fbd） |
| F-05 | 🟠 | 公共 DTO 未声明 rgbwsvt | M | 中 | ✅ **已修复**（P0-02；与 F-01 硬耦合，实际改动 10 文件而非原估 6） |
| F-06 | ✅ | 分层靠 CMake 白名单 | XS（实际） | 极低 | **已闭环（用户 2026-09-18 授权）**：两条 base 层依赖并入冻结名单并写明新判据（新增 base 层单元且 model.cpp 净增 ≤5 行）；门禁自 09-07 起持续红，现转绿，已知基线失败 5 项 → 4 项。顺带修了它被漏更新两次的根因——失败信息补上登记指引 |
| F-07 | 🟠 | RIP 未走 SPI，两条拓扑 | L | 中高 | 立项；等 RIPFLOW 外部阻塞明朗 |
| F-08 | 🟡 | CLI 绕过 Worker 准入 | S | 低 | 记账即可，不封堵 |
| F-09 | 🔴 | slicer.cpp ~~5 888 行~~ **现 1961 行** | XL | 高 | 🟢 **搬运完成但未完全交付**：八步 5888 → 1961 行，`run_slicer` 1736 行未动、占比 88.5%，八次字节级基线全通过；**R-12 验证 Gate 第 4 条（每个搬出函数补单测）未达成，新增单测为零** |
| F-10 | 🟢 | 单一 SliceConfig 39 结构体 | ~~L~~ **改做 S** | 中高 | **已换方案落地（2026-09-18）**：实测 25 个成员 / 42 个消费文件、重度仅 5 个，拆 5 条车道对谁都没帮助（详见文末专节）。改做配置消费门禁 `ValidateConfigConsumption.py`：守「任何成员都要有消费者」与重度文件棘轮，已注册两条 ctest。**原方案的 92 文件拆分不做** |
| F-11 | 🟡 | G2 豁免无上限、~~理由过期~~**过期条件无法判定** | S | 低 | 🟢 **可独立修的一半已完成**：豁免现每次运行都以 NOTICE 报出（此前完全不可见），并新增可选 `expiresOn`（ISO 日期，过期即 ERROR）。~~**被 F-09 门住的一半仍挂起**~~ **⚠ 2026-09-17：`slicer.cpp` 那条豁免已撤销**——G2 是增长门禁，该文件降到 1961 行后不再需要增长许可，撤销后其增长五行即报红；`config.cpp` 与 `main.cpp` 未动过，那两条仍必要 |
| F-12 | 🟠 | Worker 日志无界累积 | S | 低 | ✅ **已修复**（P0-03，commit 6299f0d0；四容器受限且末段恒为最新） |
| F-13 | 🟡 | maxConcurrentJobs=1 | M | 中高 | 等 MEMFLOW 落地 |
| F-14 | 🟡 | 10 ms 轮询线程 | M | 中 | **不做**，记录在案 |
| F-15 | 🟡 | 三段序列化 + 模型重复解析 | M–L | 中 | **不测不动** |
| F-20 | 🟡 | 合同协商每作业重跑 | S | 低 | ✅ **已修复**（P0-06，commit b918e0ba） |
| F-21 | 🟠 | 一个对外错误码在契约里不存在 | S | 低 | ✅ **已修复**（P0-02；R-10 单一事实源仍未做） |
| F-37 | 🟡 | `model/` 与 `samples/models/` 两棵并行资产树（1.1 GB，9 文件逐字节重复，命名体系不一） | S~M | 中 | 先回答 samples 定位问题再动 |
| F-38 | 🟡 | RIP 三族 schema 各三代并存，diagnostic/result 的 v1/v2 源码不消费 | S | 低~中 | **需 RIPFLOW 确认去留**，不自行删 |
| F-39 | 🟡 | AI 脚手架占 9 个根级条目；`ai-project-template/` 是给别的项目用的模板却住在本仓库 | S | 低 | 归档；清理需单独授权 |
| F-40 | 🟡 | 两个陈旧根级跟踪文件（`AGENTS.md.backup-*`、`.agents.7z`，合计 6.4 KB） | S | 极低 | 归档；删除需单独授权 |
| F-41 | 🟠 | 两条 hostux 测试超时余量仅 2.4 倍，有负载即假失败，每次需定向复跑定责 | S | 极低 | ✅ **已修复**（T1-02）：内部期限**已定位**在 `HostUxSceneSmoke.h` 的 120s/130s，落在预测的 117–154s 窗口内；改为 480s/520s，ctest 180s→600s，并让超时分支打印自述原因 |
| F-42 | 🟠 | ~~构建~~**提交**触发版本递增；重配监视失效使部分构建产生伪装成真回归的版本红灯（复现 3 次） | S | 低 | ✅ **已根治**（T1-01）：原描述的因果有误（递增靠提交不靠构建）。根因是 `CMAKE_CONFIGURE_DEPENDS` 监视了不变的 `.git/HEAD` 与无法监视的**目录** `.git/refs`；改监视三个真实文件。纪律另存 `AGENTS.md` 8b |
| F-43 | 🟠 | 配置来源面分散成五种写法且无统一登记，契约变更须同时改到，按键名 grep 只能找到其中一部分 | M→S | 低 | ✅ **已处置，但修法与原计划不同**：原写「其余四面各加门禁」——实测后改为**汇聚点强制 + 按需清点工具**，理由见下 |
| F-44 | 🟠 | 单测未捕获异常挂死而非快速失败，坏 fixture 每个吃满 ctest 超时 | S~M | 低 | ✅ **暴露面已清零**：97 个无保护 main 里只有 **13 个**会消费配置/模型/包（真正可能抛异常），已迁移 12 个（余 1 个为 13 行纯链接检查）。其余 84 个是纯逻辑测试，迁移属空转。边界：`catch(...)` 接不住 Windows 结构化异常 |
| F-46 | 🟠 | `.gitignore` 的 `tests/*` 白名单漏掉 25/35 个测试目录，新增文件被静默忽略（已因此丢过 2 个脚本） | S | 极低 | ✅ **已修复**（commit `9235806d`）：受陷目录 25/35 → **1/36**，余下 `fixtures` 是刻意设计。放行前逐目录复核：23 个零未跟踪，仅 `stage14d_03` 有构建产物，由 `tests/*/out/` 单独挡住。**刻意未用 `*.obj`**——`model/obj/` 下全是正在跟踪的模型 |
| F-45 | 🟠 | `slicer.config.1` 归一化静默丢弃白名单外的全部键，`background` 与 `geometrySampling` 经 v1 无法表达且无任何提示 | S | 低 | 🟢 **静默已消除**（`RejectUnknownKeys`）：未知键改为报错并点名该键与所在层级。**补全映射未做**——`background` / `geometrySampling` 经 v1 仍无法表达，那是行为变更须单独裁定 |
| F-16 | 🔴 | ~~无 CI~~ **CI 已立项、用户已裁定暂缓** | L | 低 | ⚠ **二次更正**：`AGENTS.md` 记着「CI ⏸ 用户 2026-08-10 裁决**暂缓**，清单保留不开工」——不是「待裁定」而是**已裁定**。卡（v1.2 七项）已备齐，阻塞点 C-A-01 仍在且被 SHA-256 钉住。**除非用户改变裁定，否则不开工** |
| F-17 | 🟠 | 无测试框架、85 处断言不报原因 | M | 低 | 🟢 **已提供手段**（T1-03 `tests/support/Expect.h`）：断言宏打印表达式原文与双方实参。**余 97 个测试 main 未迁移**——批量改写已试过并回退（PC-12），应随手逐个改 |
| F-18 | 🟠 | 单文件 ~~3 315~~ **曾达 3 440** 行 CMake / 171 目标 | M | 中 | 🟢 **R-08 已落地（2026-09-17）**：120 个形状一致的目标转清单式，行数降至 **2 895（−545）**；`ctest -N` 测试名集合 268 零差异。余 51 个手写（多源 / 多链接 / 带 include 目录），**按域拆分成 cmake/targets/\*.cmake 尚未做** |
| F-19 | 🟡 | 8 个 build 目录，1 个已知危险 | S | 低 | **删除需授权** |

---

## 补充章节的问题登记（F-22..F-36）

2026-09-08 增补三章，问题沿用同一编号体系，不在本表展开：

| 章节 | 编号 | 主题 |
|---|---|---|
| `07_切片域算法与数据结构评估.md` | F-22..F-27 | Dense 整栈默认路径未根治（🔴 F-22 → ✅ **已修复**，P0-07 commit bccec8c7：默认值不再可静默落入）、无 z 分桶、无活动边表、非流形 fail-soft、逐层重分配、通道极性语义分散 |
| `08_依赖耦合_测试覆盖_并发模型.md` | F-28..F-32 | `(root)` 耦合枢纽、27 对双向依赖、覆盖密度失衡（🟠 F-30）、`material`/`materials` 命名、并发正确性未验证 |
| `09_输入健壮性与安全面.md` | F-33..F-36 | JSON 解析无深度上限（🔴 F-33 → ✅ **已修复**，P0-01 commit 554847f3：RAII 深度守卫上限 64）、导入无规模上限、进程内能力无配额、`catch(...)` 覆盖面未记账 |

**全部 36 条里，按「成本 × 影响」应当最先做的三条** —— 截至 2026-09-15 **三条均已完成**：

1. ✅ **F-33**（JSON 深度上限，约 10 行）—— 畸形配置可杀死宿主打印软件，代价与后果完全不成比例
   → P0-01，RAII 深度守卫上限 64，commit `554847f3`
2. ✅ **F-22**（`slicing_mode` 默认值指向未根治的 Dense 路径，加闸 S 级）—— 已被 MEMFLOW 解决的问题仍可从默认路径复现
   → P0-07，改为必填，commit `bccec8c7`。**实际代价远超原估的 S 级**：四轮才收敛，详见 F-43
3. ✅ **F-02**（让模块自述对拍测试真跑起来）—— 原因见 `06_` 结尾：症状易修，「绿灯但什么都没测」才是病
   → P0-04，删除静默降级分支并接真实探针，commit `4596a771`

### P0FIX 专项已修复清单（10 条）

| 编号 | 任务 | commit |
|---|---|---|
| F-33 | P0-01 JSON 解析深度上限 | `554847f3` |
| F-01 / F-03 / F-05 / F-21 | P0-02 能力集受控修订 | `768a0de6` |
| F-12 | P0-03 Worker 日志与进度上界 | `6299f0d0` |
| F-02 | P0-04 模块自述对拍测试接探针 | `4596a771` |
| F-04 | P0-05 宿主结构化能力判定 | `0a261fbd` |
| F-20 | P0-06 合同协商按 Worker 身份缓存 | `b918e0ba` |
| F-22 | P0-07 `slicingMode` 改为必填 | `bccec8c7` |

**专项期间新立 6 条**：F-41、F-42（验证纪律类）、F-43~F-46（P0-07 实施时撞出）。
净增 6 条不等于变差——这 6 条本来就存在，只是此前无人撞到。


---

## F-43 / F-44：P0-07 实施过程中暴露的两条（2026-09-15）

这两条不是静态审读看出来的，是 P0-07（`slicingMode` 改必填）**实施时被撞出来的**，故证据即为当时的实测。

### F-43 配置来源面分散成五种写法，且无统一登记

`load_slice_config` 有 **38 个调用方**。喂给它的配置由五种互不相同的方式产生：

| # | 来源面 | 规模 | 本轮是否需改 |
|---|---|---|---|
| 1 | `samples/configs/**.json` | 124 份 | 是（补齐 35 份） |
| 2 | 测试源码里的 C++ 字符串字面量 | 9 处 / 7 文件 | 是 |
| 3 | 程序化构造 `Json::Object`（如 `WorkerPreflightExecutorTests::MakeProfile`） | 若干 | 否（本就声明） |
| 4 | 纯 C 宿主 `HostRequestBuilder.c`（规范形式 + 紧凑形式**双份**） | 3 个 builder | 否（本就声明） |
| 5 | 引擎侧回读调用方传入的 Profile 路径 | 5 处 | 否（契约上由调用方负责） |

**为什么这构成问题**：契约一变，五面都要跟上，而**没有任何东西告诉你共有几面**。按键名 grep 还会漏——`\"modelPath\"`（C 字符串字面量形态）只命中 10 个文件，去掉转义限定后是 41 个，差的那 31 个里就有第 3 类写法（`{"modelPath", ...}` 用的是普通引号）。

**实际代价**：P0-07 的范围判断错了三次，共四轮才收敛。

| 轮次 | 判据 | 怎么发现不够的 | 补齐 |
|---|---|---|---|
| 一 | 盘上 `.json`，要 `input.modelPath` **且** `output.packageDir` | 回归第 69 项 `experimental_config` 变红 | 34 份 |
| 二 | + 源码内字符串字面量 | 全量复核 124 份配置时发现漏网 | 9 处 |
| 三 | 放宽为只看 `input.modelPath` | 全量回归点名 5 项 | 1 份 |
| 四 | **谁写出配置文件** | —（收敛） | 5 处 |

前三轮错在同一处：盯着**谁加载**配置（`load_slice_config` 的调用方）。但测试可以造好配置
交给上层入口，由它内部加载——按调用方枚举必然漏掉这一类。正确判据是**谁写出配置**。
四轮全部靠红灯发现，没有一轮是靠事先枚举发现的。

**已落的缓解**：`slicing_mode_declared_contract_test` 覆盖第 1 面（124 份全查，含「发现数为 0 即判空转」的自检）。**第 2~5 面仍然只能靠人**。

**若要根治**：让配置只有一个生成器（或至少让每一面都有自己的门禁）。属 M 级，不在 P0 范围。

**方法论结论（须与 F-44 合看）**：第四轮时做过一次判据放宽后的静态枚举，得到 **87 个可疑文件**，
而回归实测只有 **5 个**真坏——静态枚举在错判据上欠拟合、在对判据上过拟合 17 倍，两头都不准。
**回归本身才是准确的枚举器**，唯一让它不可用的因素是 F-44 的挂死：坏 fixture 吃满超时，
且带着已知坏 fixture 跑完的失败集失去归因价值。

所以两条的正确关系是 **F-44 是 F-43 昂贵的原因**。把未捕获异常改成快速失败之后，
「改完跑一轮回归、按它点名的修、重复到收敛」就是最省的做法，不必事先把五个来源面枚举干净。
先修 F-44，F-43 的代价随之塌缩。本轮因为挂死而转向静态枚举，是被迫的、且代价高于收益。

### F-44 单测未捕获异常挂死而非快速失败

`output_resolution_config_unit_tests` 在 P0-07 首轮回归中启动后 **CPU 仅 0.078 秒却一直不退出**，卡在第 71/249 项——未捕获的 C++ 异常没有让进程快速失败，而是被 Windows 错误报告接管，停在那里等 ctest 超时。

**影响的是排期决策而非功能**：如果一次改动打坏了 N 个共享 fixture，回归不是「快速红 N 次」，而是额外 N × 超时（本仓部分项超时设到 180 秒）。这直接否定了「改完就跑全量、让回归替我找范围」这一策略——对 F-43 那种分散来源面，事先静态枚举是**必须**的而不是可选的。

**识别方法**：`Get-Process <name> | Select Name,Id,CPU,StartTime`，CPU 接近 0 而已启动数分钟即为挂住，应直接掐掉——带着已知坏 fixture 跑完的失败集没有归因价值。


### F-45 `slicer.config.1` 归一化静默丢弃白名单外的全部键

`NormalizeConfigJson` 对 `schema: "slicer.config.1"` 的配置调用 `NormalizeSlicerConfig1`
（`src/slicer_core/config/ConfigMigration.cpp:18-61`），它**按白名单逐键搬运**：顶层 11 个键、
`pipeline.slicingMode`、`geometry` 下 3 个、`materials` 下 8 个。白名单以外的一切**静默丢弃**——
不报错、不告警。`DetectConfigSchemaKind` 只校验 schema 的**名字**，全仓没有未知键拒绝。

**两条可验证的后果**：

1. **有些设置经 v1 根本无法表达。** `background`（`config.cpp:248`）与 `geometrySampling`
   （`config.cpp:676`）都从 legacy root 读取，但两者都不在白名单里。也就是说同一个功能，
   legacy 配置能配、v1 配置配了等于没配，而作者拿不到任何信号。
   `background.value` 决定空白像素取值，`geometrySampling.strategy` 是 relief 候选策略的闸门，
   都不是无关紧要的项。

2. **仓库自己的 v1 样例就踩了。** `samples/configs/schema_v1/slicer_config_v1_basic.json`
   声明了顶层 `diagnostics`、`materials.textureApplication`、`materials.varnishGeometry`，
   三块均不在白名单中，全部被丢弃。

**本轮是怎么撞上的**：P0-07 给 v1 样例补 `slicingMode` 时误加在顶层。文件里**看得见**该键、
门禁（当时只查顶层）报绿，而引擎在归一化后仍判「未声明」并拒绝——错误信息与文件内容直接矛盾。
这正是该缺陷最难查的形态：证据摆在眼前却与行为不符。

**已落的缓解**：`slicing_mode_declared_contract_test` 按 schema 分档指认正确位置，
并**反向**拒绝「v1 配置带被忽略的顶层 slicingMode」。但这只覆盖 `slicingMode` 一个键。

**建议**：先给 `NormalizeSlicerConfig1` 加未知键拒绝（S 级，把静默变成报错），
再决定要不要补全映射（M 级）。补全映射之前，未知键拒绝会让第 2 条里那三块立即变红——
那是真实债务浮出水面，不是新增回归。


### F-46 `.gitignore` 的 `tests/` 白名单漏掉 25/35 个目录，新增文件被静默忽略

`.gitignore` 第 18 行 `tests/*` 忽略 `tests/` 下一切，随后用 `!` 逐个放回：
`golden`、`unit`、`fixtures/frame_geometry`、`hostflow`、`ripflow`、`rip_integration`、
`stage16`、`matvol`、`matvol_t`、`stage14e_02`、`version/*.py`。

**为什么平时看不出来**：已跟踪的文件不受 `.gitignore` 影响。所以未放行目录里的现存文件
照常在库、照常提交修改，一切正常——**只有新增文件会被静默忽略**，而且 `git status`
默认不显示被忽略的文件，加了也不会有任何提示。

**实测规模**（`git ls-files` + `git check-ignore` 逐目录探测）：

```
tests/ 下含已跟踪文件的一级子目录     35 个
其中新增文件会被静默忽略             25 个
这些目录现有已跟踪文件合计           95 个（不受影响，故问题不可见）
```

被漏的包括 `contracts`、`integration`、`fixtures`，以及 `stage14b_02` 到 `stage14e_04d`
的全部 22 个阶段目录。

**已经造成的实际损失**：`tests/contracts/` 下有两个文件只存在于磁盘、从未进库——
`RunSceneFacade14B03Tests.ps1` 与 `ValidateSceneFacade14B03.py`，由 PRESET 专项的任务卡引用。
它们未被 CTest 注册，所以没有表现为红灯，只是**悄悄丢了**。

**最坏形态**：若新增的被忽略文件**被 CMakeLists 注册为测试**，作者本地全绿、提交后
其他人必红，且红灯信息是「文件不存在」而非任何有意义的诊断。本次 P0-07 的
`ValidateSlicingModeDeclared.py` 差一点就是这个形态——脚本已写好、CTest 已注册，
提交前才发现它在忽略名单里。

**本次处理**：只解开 `tests/contracts/`（放在 `!tests/unit/**` 之后，且位置在
`__pycache__/` 规则之前，故 `__pycache__` 仍被忽略）。上述两个孤儿脚本因此从
**隐形**变为 `??` **可见但未跟踪**，去留由 PRESET 专项自行决定——不在本专项代为入库。

**其余 24 个目录未动**：解开它们会一并暴露各目录下的未跟踪内容，需要逐个确认是产物还是源码，
属另一件事。建议单独立项，并在放行后跑一次 `git status` 复核扫进来的是什么。


---

## 下一阶段优先级（2026-09-15 排定）

P0 层已全部完成（见上方 ✅ 标注与 `README.md` §1.7）。以下是**剩余全部未完成项**的排序。
完整理由与时序图见 `06_改进路线图与验证方案.md` 的「时序建议」。

排序依据是**两条互不相同的轴**，混在一起排会得出错的顺序：

- **用户可见正确性** —— 现在就在产出错误结果的。
- **开发速度杠杆** —— 本身不产错，但让此后每一次改动都变贵的。

| 梯队 | 项 | 成本 | 为什么在这一档 |
|---|---|---|---|
| ~~一~~ 🟢 | **F-17 + F-44（同一修法）** | S + 持续 | **手段已就位**（T1-03）：`tests/support/Expect.h` + 两个试点迁移并证伪通过。~~余 97 个测试 main 待随手迁移~~ **⚠ 2026-09-17：F-44 暴露面已清零；余 84 个是纯逻辑测试，迁移属空转，剩余收益接近零** |
| ~~一~~ ✅ | F-42 | S | **已根治**（T1-01），非仅写纪律：监视项改为三个真实文件，端到端验证提交后自动重配、版本 +1 |
| ~~一~~ ✅ | F-41 | S | **已修复**（T1-02）：内部期限定位于 `HostUxSceneSmoke.h` 120s/130s → 480s/520s；ctest 180s→600s；超时分支现打印自述原因 |
| **二** | **TEXFAIL 卡 TF-00..TF-03** | M | **当前唯一一条正在产出错误结果的问题**——缺贴图静默降级为灰模型，用户拿到的是能切但材质错的模型 |
| **二** | F-46 解开其余 24 个目录 | S | 极低风险；已因此丢过 2 个脚本 |
| **二** | F-45 v1 加未知键拒绝 | S | 把整类静默误配变成报错 |
| **二** | F-43 其余四面加门禁 | M | **代价随 F-44 修复而塌缩，勿先于 F-44 动** |
| **三** | F-16 CI | L | 卡已备齐，阻塞点已定位但被 SHA-256 钉住；**待用户裁定** |
| **三** | F-06 修 5 处越界依赖 | M | 门禁已在红；原估的 S 级不成立 |
| **三** | F-11 / R-09 行数门禁收紧 | S | 会暴露 12 处既有 ERROR，先定责 |
| **三** | F-21 余项 / R-10 能力注册表 | M | 需受控修订裁定 |
| **四** | F-09 `slicer.cpp` 拆解 | XL | **必须先有第一梯队的安全网**：8 步每步独立验证，否则每步都付一次 F-42 + F-44 的税 |
| **四** | F-10 配置面重组 | L | 排在 F-09 之后 |
| **四** | F-07 RIP 归位 SPI | L | RIPFLOW 外部阻塞 |

**明确不做 / 需授权**：F-14（不做，已记录）、F-15（不测不动）、F-13（依赖 MEMFLOW 且其合入处于暂缓裁定）、F-19 与 F-37~F-40（破坏性操作，需单独授权）。

> **2026-09-15 进展**：第一梯队三条已全部处理（专项卡 `TASKS_TIER1_验证工装税消除专项.md`）。
> F-41 / F-42 为完全修复；F-17 / F-44 是**手段就位、迁移待续**——收益随迁移进度兑现，
> 故第二梯队的 F-43 减负也只兑现了一部分。

### 一个反直觉的结论

第一梯队三条**没有一条在修 bug**，用户也看不见它们。但本轮把代价实测出来了：
不做它们，第二至第四梯队的每一条都要多花约一倍时间，而第四梯队的 F-09
（整个清单里最大的一笔债）在没有安全网的情况下根本不该开工。

所以顺序不是「先修严重的」，而是**先把测量与验证的工具修好，再去修它们能帮你验证的东西**。


### F-45 / F-46 处理记录（2026-09-16）

#### F-45：把静默丢弃改成报错

`NormalizeSlicerConfig1` 新增 `RejectUnknownKeys`，顶层与 `pipeline` / `geometry` / `materials`
三个容器各自按白名单校验，未知键抛出并**点名该键与所在层级**。

**爆炸半径先静态界定**（F-44 未兑现，97 个测试 main 遇异常仍会挂死，不能靠回归点名）：
全仓 `schema=slicer.config.1` 的配置**只有 1 份**，其中 3 个键会被拒——
`diagnostics`、`materials.textureApplication`、`materials.varnishGeometry`。
逐个实测确认三者在 `src/` 下均无读取点（`diagnostics` 的命中全是在**写**报告），
是**死键**：承诺了永不发生的行为。已从样例中删除（9 行删除，其余逐键不变）。

**刻意不做的**：不把这三个键补进映射。它们当前什么都不做，补进映射会让既有配置
**突然生效**——那是行为变更，须单独裁定，不在本条范围内。故 F-45 标 🟢 而非 ✅：
静默消除了，但「`background` / `geometrySampling` 经 v1 无法表达」这一半仍在。

**六项证伪全过**：正例仍可加载并推进到配置之后；顶层未知键、`materials` 层未知键、
`pipeline` 下拼错的键名各自被拒且消息可读。其中拼错键名一项最能说明价值——
此前 `slicingMdoe` 会被静默忽略，然后配置以「slicingMode must be declared explicitly」
失败，报错指向的位置与真正的错误完全不同。

全量回归 250 项 / 7 失败 / 2183.54 s，失败集与基线逐条一致、零新增零消失。

#### F-46：放行其余 23 个目录

受陷目录由 **25/35 降为 1/36**。放行前逐目录实测：23 个零未跟踪文件；
仅 `stage14d_03` 有 16 个，全在 `tests/stage14d_03/out/` 下且全是构建产物，
由新增的 `tests/*/out/` 规则单独挡住（该规则须放在放行规则**之后**才生效）。

`tests/fixtures` 刻意不放行：既有规则「只跟踪 `frame_geometry`」是设计而非疏漏。

**一处刻意的克制**：没有用 `*.obj` 挡构建产物。`model/obj/` 下全是正在跟踪的三维模型，
那条规则会让以后新增的模型被静默忽略——等于把 F-46 换个地方再犯一次。


### F-06 第三梯队准备结论（2026-09-16）：它不是技术工作

趁 TF-01 回归的等待窗口做的只读准备，结论推翻了我此前**两次**估计（先 S 后 M）。

门禁 `tests/contracts/ValidateStage14BLayeringFeasibility.py:164-173` 冻结了
`model.cpp` 的期望 include 清单：

```python
expectedModelIncludes = [
    "src/slicer_core/model.h",
    "src/slicer_core/model/ObjFaceParser.h",
    "src/slicer_core/model/MtlMaterialParser.h",
]
```

实际有 5 项，**差的恰好是两个**：`model/AssetReferencePath.h`(27 行) 与
`model/FrameGeometry.h`(31 行)。

**同一处早有正确做法的先例。** 该文件 :159-163 记着 MATOPQ 专项加
`MtlMaterialParser.h` 时的授权留痕：同层（`BASE_PREFIXES` 含 `src/slicer_core/model/`）、
同目录、同命名空间、同角色，未产生 base → engine 边，且**model.cpp 因此由 1982 缩减至 1941 行**
——属抽取方向而非依赖累积，授权见 `DOC_DECISION_MATOPQ_*` 第 12 节。

**另外两个头没有这样的留痕**（git log 与 DOC_DECISION 逐份核对）：

| 头文件 | 引入提交 | 专项 | 有 DOC_DECISION 提到分层门禁？ |
|---|---|---|---|
| `model/AssetReferencePath.h` | `d85c6190` | UNIPATH | **无该专项的决策文档** |
| `model/FrameGeometry.h` | `e7628f7b` | FRAME | **无该专项的决策文档** |
| `model/MtlMaterialParser.h` | —— | MATOPQ | **有**（第 12 节） |

而且 `model.cpp` 至今仍是 **1941 行**——与 MATOPQ 注释里引用的数字相同，
说明这两次添加**没有伴随抽取**，MATOPQ 用来论证正当性的那条理由在它们身上不成立。

**结论**：这条红灯不是「5 处越界依赖待修」，而是**两个专项各欠一笔留痕债**。
正确处置是各自补授权（或撤回），不是由后来者把门禁放宽。
按 `AGENTS.md` 的门禁纪律与既有教训：**别的专项的债不要替它承接**。

**本专项不做**。建议把它退回 UNIPATH 与 FRAME 各自处置，或由用户统一裁定一次性补齐。

### F-11 / F-16 的准备状态（同一窗口）

- **F-11**：行数门禁脚本已定位为 `scripts/ValidateSourceSizeGuard.py`。
  收紧前需先对「会暴露的 12 处既有 ERROR」逐条定责——那是**另一批专项的债**，
  与 F-06 同构，宜一并裁定。
- **F-16**：阻塞点 C-A-01 仍在（`samples/configs/material_process/obj_mtl_texture_rgb_varnish.json`
  第 127 行绝对路径，全仓仅此一处），且该配置被 `HostTransferProfileTests.cpp:84` 钉了 SHA-256，
  改它须同步更新钉值。属 CI 专项范围，需用户裁定是否开工。


### F-44 定向迁移收口（2026-09-16）

**没有迁移 97 个，只迁移了 12 个。** 理由是迁移的价值正比于「该测试会不会真的撞上抛异常的
代码路径」——实测过的挂死案例（`output_resolution_config_unit_tests`）发生在改配置解析时。

按「是否消费配置/模型/包」把 97 个无保护 main 分层：

| 分类 | 数量 | 处置 |
|---|---|---|
| 会消费配置/模型/包（真正可能抛） | **13** | 迁移 12 个（余 1 个是 13 行纯链接检查，包装它是噪音） |
| 纯逻辑/纯字符串等 | 84 | **不迁移**——抛异常概率低，迁移属空转 |

**改法刻意只用最小保护**：把原 `int main(...)` 改名 `RunGuardedBody(...)`，末尾追加新 `main`
调用 `slicesoft_test::GuardedMain`。**不动花括号、不碰任何断言**。

这一点与 `AGENTS.md` PC-12 的关系值得写明：PC-12 的原文是
「**断言实参求值顺序**致失败不报原因…批量改写试过并回退」——它针对的是**改写断言**。
给 `main` 套一层捕获既不触碰断言也不改用例结构，**不在该禁止范围内**。
完整的 `RunCases` 迁移（同时买到 F-17 的实参打印）才属于 PC-12 的范围，仍按「随手逐个改」推进。

**证伪**：向一个已迁移的测试注入未捕获异常——**0.03 秒**快速失败并打印
`FAIL transformed model preflight uncaught exception=…`，而不是挂住吃满 180 秒以上的超时。
五项判定全过（快速失败 / 退出码非零 / 打印 uncaught / 带 what() / 点名套件）。

**CMake 侧**：12 个目标里 9 个缺仓根 include 路径。逐个补会散落十余处，
且我第一次尝试用脚本解析多行 `add_executable` 块时**写坏了 CMakeLists**（已撤回）。
改用一条目录作用域的 `include_directories(${CMAKE_CURRENT_SOURCE_DIR})`，
与既有显式写法等价且幂等。

**过程中踩到的**：被打断的构建留下了损坏的 `.obj`，导致 `LNK1163: COMDAT 节选择无效`。
删掉那 4 个 obj 后重建即恢复。与既有教训 LNK1104 同族——**构建被打断会留下坏产物**。

全量回归 250 项 / 5 失败 / 1153.30 s，新增失败 0 条。


### F-11 定责结论（2026-09-16）：一半可独立修，一半被 F-09 门住

**先更正两个数字。** 原条目写「会暴露 12 处既有 ERROR」——实测门禁**当前退出码 0**，
输出的 74 条全是 WARNING（26 条 G4 实现/头文件比例 + 48 条 G5 空壳文件），**零 ERROR**。
「12 处」不知源自何处，此处以实测为准。

#### 可独立修的一半：过期条件无法判定

`scripts/SourceSizeGuardConfig.json` 的豁免条目用的字段是 `expiresWhen`，
而它的值是**散文而非日期**：

```
"expiresWhen": "版本重构时统一清理 main.cpp 与其他历史巨型文件"
```

`ValidateSourceSizeGuard.py:72` 只检查该字段**是否存在**
（`allowlist entry lacks reason/expiry`），`IsAllowed`（:134-137）**从不读它**。
结果是**所有豁免实际上永久有效**，无论上面写着什么。

这一条与 F-09 无关，可单独修（改成可判定的形式，例如日期或显式的「解除条件已达成」开关）。
属 **S 级**。

#### 被 F-09 门住的一半：三条 G2 豁免

现有三条豁免全部是 2026-09-01 用户授权、全部针对 G2（历史文件增长）：

| 路径 | `expiresWhen` 原文 |
|---|---|
| `apps/slicer_cli/main.cpp` | 版本重构时统一清理 main.cpp 与其他历史巨型文件 |
| `src/slicer_core/config.cpp` | 版本重构时统一清理 config.cpp 与其他历史巨型文件 |
| `src/slicer_core/slicer.cpp` | 版本重构时统一清理 slicer.cpp（**现 5645 行**） |

第三条记的是 5645 行。**注意这不是增长**——详见下方更正。

**三条的失效条件字面上就是 F-09。** 所以「收紧 G2」不是可以与 F-09 并行的独立任务；
F-09 完成后这部分自动消解。立项方案见
`docs/slice/DOC/DOC_PREP_F09_slicer_cpp拆解立项方案.md`。

> **⚠ 2026-09-16 更正（本条先前写错，已撤回）**：我曾写「slicer.cpp 自 2026-09-01 授权以来
> 又长了 243 行」。**该论断不成立。** 实测：该文件含空行 5889 行、非空行 5628 行，
> 而 `Measure-Object -Line` 的结果正是 5628——豁免里记的 **5645 是非空行口径**，
> 与 5889 只是同一文件的两种量法之差（261 个空行），不是增长。
> 按同一口径比较，该文件自授权以来**缩了 17 行**（5645 → 5628，MEMFLOW 专项所致）；
> git 历史也显示它自 2026-09-08 起稳定在 5889（含空行），且相较 09-05 的 5981 是下降的。
>
> **这直接影响优先级论据**：不能再用「文件在长大、越拖越贵」来支持 F-09 优先。
> 该论据已撤回，优先级改按「F-09 解锁 F-11」与「安全网刚建好」两条成立的理由判断。
> 教训与本工作流早前那次同源：**行数必须单一口径**，混用 `Measure-Object -Line`
> 与含空行计数会凭空造出几百行的差。

#### 那 74 条 WARNING 不在本专项承接范围

与 F-06 同一口径：它们是其他专项的既有债。把 WARNING 提成 ERROR 等于替它们承接，
应各自处置或由用户统一裁定。

### 第三梯队可开工性汇总（2026-09-16）

| 项 | 可否由本专项开工 | 原因 |
|---|---|---|
| F-06 | ✅ | UNIPATH 与 FRAME 的留痕债已由 P0FIX 代办（两个专项均无活的归属方） |
| F-16 | ✗ | `AGENTS.md`：用户 2026-08-10 裁决**暂缓** |
| F-11 | 一半可（改判定）／另一半 **2026-09-17 已随 F-09 完成而解除**（`slicer.cpp` 豁免已撤销） | 见上 |
| R-10 | ✓ | 能力集实测钉在 **29 处非文档位置**（无一在 `slicer.cpp`）；与 F-09 **代码不交叉但验证交叉**（共享工作树与构建目录），故**不可并行**，见 `DOC_PREP_F09` §9 |


### F-43 处置结论（2026-09-16）：原计划的修法是错的

原处置写「给其余四个来源面各加门禁」。开工前先量了两件事，结论是**那个修法不该做**。

#### 一、汇聚点唯一，分面门禁是冗余

实测 `SliceConfig` 的产生只有 `load_slice_config` **一个命名入口**，
定义只在 `src/slicer_core/config.cpp` 一处（全仓扫描 `src/`、`apps/` 的 `.cpp`/`.h`，
未发现 `ParseSliceConfig` / `MakeSliceConfig` 等其他构造入口）。

五种来源面无论怎么造配置，**最终都要过这一关**。而两道强制已经在这一关里：

| 强制 | 位置 | 来自 |
|---|---|---|
| `slicingMode` 缺失即抛 | `config.cpp` | P0-07 |
| v1 未知键即抛（顶层 + 三个容器共 4 处调用） | `ConfigMigration.cpp` | F-45 |

**在汇聚点强制严格优于给五个面分别加门禁**——后者既冗余，又必然漏（本专项已两次
因按键名 grep 漏掉整类写法）。

#### 二、冻结清单会变成噪音

若按原计划做「登记 + 冻结」，实测需登记的文件**近百个**（造配置 66 + 只读 26）。
冻结这种规模的清单，结果是它常年变红、没人维护——**比没有门禁更糟**，
与 F-06 那条红了整个专项期的门禁同一下场。

#### 三、剩下的是可发现性，给工具不给门禁

F-43 真正没被解决的是那句「**没有任何东西告诉你共有几面**」。这是可发现性问题。
新增 `scripts/InventorySliceConfigSurfaces.py`：列出五种来源面各自的判据与强制位置、
核验汇聚点前提、报出当前规模。**按需运行，刻意不注册为 ctest**——它是回答问题的工具，
不是需要维护的门禁。

**其中最要紧的是「汇聚点核验」**：上面整套论证都建立在「汇聚点唯一且两道强制在位」上。
若哪天该前提失效而无人察觉，这份结论就会悄悄变成错的。故工具会在前提失效时**退出码非零并点名**。

**三向证伪通过**：删掉 F-45 的函数定义 → 报警点名；**只删一个调用点**（函数还在，
覆盖面缺一块）→ 报出「只剩 3 处」；停用 P0-07 的必填抛出 → 报出「缺失时不再抛异常」。

> **首版判据是空转的，靠证伪才发现。** 最初用裸子串 `"RejectUnknownKeys" not in text`
> 判断该强制是否还在；证伪时把它改名为 `RejectUnknownKeysDISABLED`——**子串仍在，
> 检查照样通过**。已改为结构化判据（要求函数定义 + 至少 4 处调用点）。
> 与 F-02「绿灯但什么都没测」同一族。


### F-11 可独立修的一半：完成（2026-09-16）

**改了什么**（`scripts/ValidateSourceSizeGuard.py`）：

1. **豁免变可见。** 新增 `CollectAllowlistNotices`，每次运行把每条豁免连同其解除条件
   打成 `NOTICE:` 行，排在 74 条 G4/G5 警告**之前**——写在末尾等于继续被淹没。
   此前这三条豁免在日常输出里**完全不出现**，没有任何一行提醒它们还在。

2. **新增可判定的到期。** 可选字段 `expiresOn`（ISO 日期）：过期即 **ERROR** 并使
   退出码非零，必须复核后延期或删除，不能默默继续豁免。格式非法在读配置时即失败。

3. **措辞更正。** 原错误信息写 `lacks reason/expiry`，而 `expiresWhen` 的值是散文、
   从不与任何东西比较——那句话暗示了一种并不存在的时间到期。改为 `lacks reason/expiresWhen`。

**刻意不做的**：没有给现有三条补 `expiresOn`。它们的解除条件是「版本重构时统一清理」，
即 **F-09 完成**，不是某个时间点；硬套日期只会制造一个到期就被顺手延后的形式，
把「无法判定」换成「假装可判定」。那三条的真正处置见
`docs/slice/DOC/DOC_PREP_F09_slicer_cpp拆解立项方案.md` §6。

**五项证伪通过**：基线通过；过期 `expiresOn=2020-01-01` → ERROR 且退出码非零；
未过期 `2099-12-31` → 放行且 NOTICE 显示日期；非法格式「版本重构时」→ 读配置即失败；
还原后恢复。

**对回归无影响**：注册的 ctest `slicer_source_size_guard_self_test` 只跑 `--self-test`，
不经过新增路径；已单独跑过，通过。

### F-47 🟡 PNG/PPM 预览编码器有两套独立手写实现，8 个函数一一对应

**发现经过（2026-09-16）**：F-09 第 1 步测绘「预览/图像编码簇该搬到哪」时，
按关键字 `crc32|IHDR` 搜全仓，只命中 `slicer.cpp` 与 `src/third_party/miniz/`。
第二套之所以搜不到，是因为它整套用了另一组命名（`UpdateCrc32`、`CalculateChunkCrc32`）——
**这正是它能长期并存而无人察觉的原因**：两套都不引用对方，也没有共同的关键字。

**实测对应关系**（左：`src/slicer_core/slicer.cpp` 匿名命名空间；
右：`src/slicer_core/output/rgbwsv/RgbwsvPackageWriter.cpp` + `RgbwsvPreviewIo.h`）：

| slicer.cpp | 行 | RgbwsvPackageWriter.cpp / RgbwsvPreviewIo.h | 行 |
|---|---|---|---|
| `append_be_u32` | 452 | `AppendBigEndianUint32` | 500 |
| `crc32_update` | 459 | `UpdateCrc32` | 510 |
| `crc32_chunk` | 468 | `CalculateChunkCrc32` | 523 |
| `adler32` | 479 | `CalculateAdler32` | — |
| `zlib_store_blocks` | 489 | `EncodeStoredZlibBlocks` | 551 |
| `write_png_chunk` | 513 | `WritePngChunk` | ~599 |
| `write_png` | 529 | `WritePng` | PreviewIo:33 |
| `write_ppm` | 434 | `WritePpm` | PreviewIo:3 |

**两套的输出字节相同**：同一 PNG 签名、同一 IHDR（`8,2,0,0,0` = 8 位 RGB、无隔行）、
同样的纵向翻转、同样的 stored-zlib IDAT、同样的 IEND；PPM 两边都是 `P6` + 纵向翻转。
差异**只在调用约定与边界处理**，不在编码本身：

- 取消令牌：只有 `RgbwsvPreviewIo` 侧有（`ThrowIfCancellationRequested`）
- 缓冲区尺寸校验与 `create_directories`：只有 `slicer.cpp` 侧有
- 参数形状：`slicer.cpp` 收 `GridSpec`，另一侧收显式 `widthPx/heightPx`

**为什么值得记**：手写 PNG 编码是非平凡格式代码（CRC 表、Adler32、zlib 块边界，
且 stored 块有 65535 字节上限）。两套并存意味着任何一处修复都必须**记得**改另一处，
而代码里没有任何东西会提示第二处的存在——与 F-01/F-02 是同一形状的问题。
仓库里已经有 `src/third_party/miniz/`，而两套都没用它。

**附带发现**：`RgbwsvPreviewIo.h` 只有 `#pragma once`，**没有任何 `#include`**，
其中的函数也不是 `inline`。它只能被 `RgbwsvPackageWriter.cpp` 在特定位置（在那些助手
定义之后）文本式包含才能编译——是代码片段而不是头文件。换个包含点或多包含一次即报错。

**不在 F-09 第 1 步处理**：第 1 步的契约是**逐字节不变的纯搬运**。合并两套编码器会改变
调用方可见的行为（取消语义、错误处理、是否建目录），属于行为变更，必须另行立项与验证。
第 1 步只做一件与之相关的事：把 `slicer.cpp` 这一簇搬到 `output/` 下、与 `RgbwsvPreviewIo`
相邻，**让这处重复变得可见**。

### F-48 🟡 `compose_retained_layer` 是死代码，72 行从未被调用

**发现经过（2026-09-16）**：F-09 第 5 步搬运时核对调用点，发现它全仓**只有定义、
没有任何调用点**（`grep -rn compose_retained_layer src/ apps/ tests/` 仅命中
`slicer.cpp` 的定义行）。

它不被字节级基线覆盖（没有执行路径），也不被任何测试触及——**编译进二进制但永不执行**。

**未在第 5 步删除**：该步的契约是「只移位、不改行为」，删除代码不属于移位。
已随簇搬入 `materials/SliceMaterialTexture.cpp` 的匿名命名空间，单独立条处置。

**处置前要确认的一件事**：它与 `material/RetainedMaterialLayerComposer.cpp` 的
`ComposeRetainedMaterialLayer` 是否为同一功能的两份实现（名字与签名都相近）。
若是，删除前应确认现役的那份确实覆盖了它的全部行为；若否，则要查清它是
未接线的半成品还是已废弃的旧路径。

### F-47 增补：第三处「被注释承认」的重复实现

`src/slicer_core/material/RetainedMaterialLayerComposer.cpp:622` 的注释原文写着
与 `slicer.cpp` 的 `TextureColumnMatchesOwner`「语义**逐字一致**，两处必须同步修改」。

与 F-47 的 PNG/PPM 编码器不同，这一处**作者是知情的**并留了注释——但机制上仍然
只有注释在约束，没有任何东西会在两处漂移时报错。F-09 第 5 步后，
`TextureColumnMatchesOwner` 已随簇移至 `materials/SliceMaterialTexture.cpp`，
那条注释里的「slicer.cpp 的」指向已过期（本次未改该注释，属 F-47 的处置范围）。

### F-49 🟡 进度回调与阶段计时有两份相同实现，分属两条管线

**发现经过（2026-09-16）**：F-09 第 6 步搬运时核对外部引用，发现
`ElapsedMsSince` / `NotifyProgress` / `ShouldNotifyLayerProgress` 在
`src/slicer_core/pipeline/OpenVdbCandidatePipeline.cpp` 也各有一份，
定义在**该文件自己的匿名命名空间**里（时钟别名叫 `PipelineClock` 而非 `SlicerClock`）。

两份实现同名、同签名、同逻辑：取消检查 → 回调空判 → `std::clamp(percent, 0, 100)`
→ 附带 `ElapsedMsSince(runStart)`；层进度的节流判据同为「首层、末层、
或每 `max(1, (layerCount+99)/100)` 层报一次」。

**为什么一直没被发现**：两者都在匿名命名空间里，互不可见也互不引用，
且名字相同——按名字搜会得到两个结果，但两处都「看起来像自己文件的私有助手」。

**这是本专项记录的第四处重复实现**，前三处见 F-47（PNG/PPM 编码器 8 个函数对、
`TextureColumnMatchesOwner` 与 `RetainedTextureColumnMatchesOwner`）。

**未在第 6 步合并**：合并会改变 `OpenVdbCandidatePipeline` 的可见行为面
（它当前用的是自己那份），不属于「只移位」。第 6 步把 slicer.cpp 那份落到
`src/slicer_core/pipeline/SliceProgressNotifier.{h,cpp}`——**刻意放在 pipeline/ 下，
就是为了让这次合并有个现成的家**：届时删掉 OpenVdb 侧的私有拷贝、改为包含该头即可。

### F-50 🟠 行数门禁实质上从不运行，且只看最后一次提交

**发现经过（2026-09-17）**：F-09 收口后复盘本专项自身的产出时撞到的。

两个缺陷叠加，使这道门禁的实际保护面接近于零：

1. **注册的 ctest 只跑 `--self-test`**（`CMakeLists.txt`）。自测只验证「规则函数在构造的假数据上会报」，
   **从不接触仓库真实文件**。实质检查只能靠人手动敲命令——而没人会主动去敲。
2. **实质检查的 base 是 `HEAD^`**（`ResolveBaseRef`）。G1/G2/G3 都是**变更**判据，
   所以**两次提交之前引入的东西永远看不见**。

**实证就在本专项自己的产出上**：F-09 八步新增的三个文件超过 G1 阈值（新源文件 500 行），
一次都没被拦过——

```
src/slicer_core/materials/SliceMaterialTexture.cpp     1392 行
src/slicer_core/support/SliceSupportGeneration.cpp      554 行
src/slicer_core/geometry/ReliefHeightfieldSampler.cpp   535 行
```

这与 F-02「绿灯但什么都没测」是同一形状：门禁存在、测试存在、日常全绿，
而被保护的不变量从未被真正检查过。

**顺带推翻了 R-09 的一个估算**：`analysis/06` 的 R-09 写「收紧后会立刻暴露 **12 处**既有 ERROR，
这批债必须先逐条定责」。全仓实测是 **132 个**超限文件（源 112 / 头 20；
`tests/` 45、`src/` 63、`apps/` 24），**差 11 倍**。在 132 条的规模上逐条定责不可行，
因此 R-09 原方案（冻结清单 + 先定责）已不可执行。

**处置：改用棘轮，不冻结清单。**

新增 `--census` 模式：全仓清点超限文件数，与 `censusBaseline`（记录值 132）比对，
**只在数量上升时报红**。它回答的唯一问题是「有没有新增」。

- 确定性——只读工作树状态，不依赖 git 历史，因此可以安全注册为 ctest
  （已注册 `slicer_source_size_census_test`，测试总数 251 → 252）
- 不替任何专项承接既有的 132 条债，也不需要先定责就能立刻生效
- **刻意不冻结清单**：F-43 已论证过，冻结近百文件的清单会常年变红、没人维护，
  比没有门禁更糟；132 个只会更糟

**已知边界**：棘轮是防回涨的，不是完备证明——「加一个又删一个」会互相抵消。
要完备就得回到冻结清单，而那条路已被 F-43 的实测否掉。

**四点证伪通过**：新增 501 行源文件 → 报红并点名「由 132 增至 133」；缩到 499 行 → 恢复；
未跟踪的 900 行文件 → 不计入（清点只看 `git ls-files`）；还原后恢复。

### F-49 更正（2026-09-17）：原登记的三处说法有两处不成立

F-49 原文写「两份实现**同名、同签名、同逻辑**」，并给出处置「删掉 OpenVdb 侧的私有拷贝、
改为包含该头即可」。动手前逐字比对，**这两句都不成立**。

**只有两个函数真的相同**（去掉时钟别名名字差异后逐字节一致）：

| 函数 | 行数 | 判定 |
|---|---|---|
| `ElapsedMsSince` | 4 | 逐字节相同 ✅ 已合并 |
| `ShouldNotifyLayerProgress` | 10 | 逐字节相同 ✅ 已合并 |
| `NotifyProgress` | 25 | **三处不同** ❌ 不合并 |

`NotifyProgress` 的三处差异：

| | 共享单元 | OpenVdb 侧 |
|---|---|---|
| 参数类型 | `SliceRunOptions` | `OpenVdbCandidatePipelineOptions` |
| 取消检查 | 有，`cancellation_requested` 为真时抛异常 | **完全没有** |
| 百分比 | `std::clamp(percent, 0, 100)` | 原样透传 |

而 `OpenVdbCandidatePipelineOptions` **根本没有 `cancellation_requested` 字段**——
合并需要改产品类型，属行为变更，不是去重。

**取消能力的缺失与可达性一致，不是缺口**：`run_slicer` 对 OpenVdb 候选配置**显式抛异常**
（要求走 `--openvdb-candidate-slice`），所以可取消的 facade / DLL 路径**到不了**这条管线；
它只能从 `apps/slicer_cli/main.cpp` 进入，而 CLI 本就没有取消令牌
（取消是在 `ProductionSliceFacadeFactory.cpp:337` 接的）。

**因此 F-49 的实际规模远小于原登记**：真正的重复只有 14 行平凡代码，
其中只有 `ShouldNotifyLayerProgress` 含逻辑（层进度节流公式），是唯一可能漂移的部分。
已合并这两个；`NotifyProgress` 保留本地实现并在原处加注说明为何不合并。

**这条更正本身是个教训**：F-49 是我在第 6 步搬运时「顺手」记的，
凭的是两边函数名与形状看着一样，**没有逐字比对**。同一份登记里的 F-47
（PNG/PPM 编码器）是逐函数比对过的，结论至今成立；F-49 没有，于是错了两处。

### F-34 处置（2026-09-17）：导入文件尺寸上限已落地

**为什么先做这条**：F-33（JSON 解析深度上限）当初被本清单评为「全部 36 条里性价比最高」，
而 F-34 是**同一威胁模型的另一半**且一直没做。`model.import` 是 `in_process` 能力——
解析与分配都发生在**宿主打印软件的地址空间**，此前没有任何规模上限。

**闸门位置**：`load_model_report(const ModelLoadConfig&, path)`（`model.cpp`）。
实测它是**唯一汇聚点**：另一个 `SliceConfig` 重载只是适配器，转调它；
全仓 15+ 个调用点无一绕开。这是 F-43 得出的经验——在汇聚点强制，优于逐个导入器加。

闸门下在 `exists` 检查之后、`detect_format` 与任何 `load_*` 之前，
即**读任何字节、分配任何内存之前**。

**阈值 512 MiB 的依据**：仓库内 190 个模型文件，最大 `meigui_fudiao/04.obj` **39.96 MB**。
512 MiB 是它的约 13 倍。它挡的是量级错误的输入，不是正常的大模型。

**刻意不做环境变量开关**：引擎侧实测没有读环境变量的先例，
给一个要嵌进宿主进程的库加隐藏的全局输入是坏味道。
将来若确有合法的超大模型需求，应走配置契约加字段，而不是加后门。

**新增单测** `tests/unit/model_import_size_limit`（测试数 252 → 253），
刻意建成 `json_parse_depth`（F-33）的姊妹用例。判定被抽成可单测的纯函数
`EnsureModelFileWithinLimit`，因此不需要一个 512 MiB 的样本进仓库。

**两层证伪都做了**：

1. **单测证伪**：常量改成 256 MiB → 报红并点名；判定改成永不抛 → 报红；还原后恢复。
2. **接线证伪**（单测证明不了这一层）：把调用处上限临时降到 1 KB，
   跑真实模型（1173 字节）→ 被拒，且错误经由正规预检码 `E_12E_PREFLIGHT_IMPORT_INVALID`
   上报，宿主拿到的是结构化诊断而非崩溃；还原后恢复。

   > **第一次接线证伪是无效的，记在这里**：我挑了跑得最快的基线用例，
   > 却没查它的模型多大——`internal_void_frame.obj` 只有 1.15 KB，
   > 远低于当时设的 1 MiB 测试上限，于是「通过」了，而我差点据此认为闸门没接上。
   > 证伪用例的**判别力**必须先验证：劣化必须真的落在被测分支上。

**未覆盖的边界**：只挡文件尺寸，不挡「小文件解压/细分成巨量三角面」
（如高压缩比的 3MF）。那属于 F-35「进程内能力整体没有资源配额概念」，仍未做。

## 梯队状态复核（2026-09-17）

「下一阶段优先级」表排定于 2026-09-15，此后两轮实施使多条状态失效。下表是**实测复核**，
不是重排优先级。

| 梯队 | 项 | 实际状态 |
|---|---|---|
| 一 | F-42 / F-41 | ✅ 已根治 / 已修复 |
| 一 | F-17 + F-44 | 🟢 F-44 暴露面清零（13 个真有风险的迁了 12）；**F-17 余 84 个是纯逻辑测试，迁移属空转，收益已基本兑现完** |
| 二 | TEXFAIL TF-00..03 | ✅ 全部 COMPLETE，基线失败集少 2 条 |
| 二 | F-46 / F-43 | ✅ 已修复 / 已处置（修法与原计划不同） |
| 二 | F-45 | 🟢 静默已消除；补全映射属行为变更未做 |
| 三 | F-16 CI | ⏸ 用户已裁定暂缓，本轮再次确认搁置 |
| 三 | F-06 | ⚠ 非技术工作，属 UNIPATH 与 FRAME 两专项的留痕债，不由本专项承接 |
| 三 | F-11 | 🟢 **两半都有进展**：可见性与 `expiresOn` 已做；被 F-09 门住的一半，`slicer.cpp` 那条豁免**已撤销**（见下） |
| 三 | R-09 行数门禁收紧 | ❌ **原方案已不可执行**，见 F-50：实测 132 条而非估计的 12 条 |
| 三 | F-21 / R-10 | ✅ 已修复 / 🟢 交叉校验已做，代码生成未做 |
| 四 | F-09 | 🟢 **搬运完成但未完全交付**：八步 5888 → 1961 行，八次字节级基线全通过；**R-12 验证 Gate 第 4 条（每个搬出函数补单测）未达成，新增单测为零** |
| 四 | F-10 / F-07 | 未动 / RIPFLOW 外部阻塞 |

### 本轮新增并已处置的四条

| 编号 | 状态 |
|---|---|
| F-34 导入无规模上限 | ✅ 已处置，含可单测判定与两层证伪 |
| F-48 死代码 | ✅ 已删除 73 行 |
| F-49 进度助手重复 | ✅ 合并 14 行真重复；**原登记三处说法有两处不成立，已更正** |
| F-50 门禁空转 | ✅ 已补清点棘轮并注册 ctest |
| F-47 PNG/PPM 两套编码器 | 🟡 **重估后建议不做合并**：两侧均被字节级基线覆盖，原风险叙述不成立，详见文末专节 |

### 两条被本轮实测推翻的既有论断

1. **「F-11 被 F-09 门住的一半仍挂起」不再成立。** G2 是**增长**门禁，豁免的作用只是允许增长；
   `slicer.cpp` 从 5888 降到 1961 后不再需要，已撤销并证伪（增长五行即报红）。
   `config.cpp`(1501) 与 `main.cpp`(1075) 未动过，那两条仍必要。
2. **R-09 的「12 处既有 ERROR」估算差 11 倍。** 全仓实测 132 个超阈值文件
   （源 112 / 头 20；`tests/` 45、`src/` 63、`apps/` 24）。逐条定责在这个规模上不可行。

### 收益率最高的未做项（2026-09-17 重估）

1. **F-35 进程内能力整体无资源配额** —— F-34 只挡了文件尺寸，挡不住「小文件细分成巨量三角面」。
   它是 F-34 的上位问题，且 `slicer_capability_dtos.json` 里只有 ViewData 一条能力有预算语义。
2. **F-47 两套 PNG/PPM 编码器** —— 8 个函数对，四处重复里唯一未动的一处。
3. **F-09 的单测缺口** —— 八个新编译单元共 4596 行，目前零单测。R-12 说这是搬迁的目的。
4. **F-16 CI** —— 它是 F-50 的真正解药（门禁没有自动运行的地方，根因就是没有 CI），但已被裁定暂缓。

### F-51 🟠 `ReadFileBytes` 有三份实现且全部是无界整文件读取

**发现经过（2026-09-17）**：按收益排序推进 F-35（进程内能力无资源配额）时，
清点 12 个 `in_process` 能力的分配路径所撞到的。

`slicer_capability_dtos.json` 里 16 个能力中 **12 个是 `in_process`**——
它们的解析与分配都发生在**宿主打印软件的地址空间**，而其中只有 `scene.get_viewdata`
一条有预算语义（`viewDataRules.budgetExhaustion`）。这是 F-35 的本体。

清点时发现同名函数 `ReadFileBytes` 在三处各有一份：

| 文件 | 行 | 打不开时的行为 |
|---|---|---|
| `api/implementation/ModelFacadeImplementation.cpp` | 42 | 抛 `std::runtime_error` |
| `api/implementation/PackageQueryFacadePackage.cpp` | 118 | 返回空串 |
| `output/rgbwsvt/RgbwsvtPackageReader.cpp` | 32 | 调 `Fail(ManifestMissing, ...)` |

**三者的错误处理各不相同，因此不能机械合并**——这一点是动手前逐字比对确认的
（F-49 的教训：上次没比对就写了「三者同逻辑」，错了两处）。

**但三者相同的恰恰是问题所在**：读取部分全都是

```cpp
std::string{std::istreambuf_iterator<char>{input}, std::istreambuf_iterator<char>{}}
```

——**没有任何尺寸上限**。`ModelFacadeImplementation.cpp:202` 用它读**整个模型文件**算 SHA-256；
另两处读包清单。F-34 已经给 `load_model_report` 加了 512 MiB 闸门，
但这条 SHA-256 路径**绕开了它**，同一个大文件仍可从这里进宿主地址空间。

**这是本专项记录的第五处重复实现**，前四处见 F-47（PNG/PPM 编码器、
`TextureColumnMatchesOwner`）、F-49（进度助手）。共同形状依旧：
几份代码互不引用，漂移时没有任何机制会报错。

**处置方向**：不合并三个函数（错误处理必须各自保留），而是抽出**共享的有界读取**，
三处各自保留自己的「打不开」分支。属实现侧修法，**不触碰契约**。

**F-35 的另一半仍需裁定**：把「进程内能力的资源配额」写进 DTO
（与 `sliceBackend.noInProcessFallback` 同级）属 `contractVersion 1.14` 的受控修订，
需用户裁定，不在实现侧修法范围内。

### F-35 第一层处置（2026-09-17）：进程内整文件读取已加界

按「先做实现侧、攒够证据再决定是否写进契约」的顺序，先做第一层。

**新增** `src/slicer_core/system/BoundedFileRead.{h,cpp}`（分层实测为 `base`，
与三个调用方同层），提供 `ReadOpenFileBounded` 与可单测的纯判定 `EnsureReadWithinLimit`。

**刻意只接管「读取」这一段**：三处 `ReadFileBytes` 在「打不开」时的行为各不相同且
各自都正确（抛 `runtime_error` / 返回空串 / 报 `ManifestMissing`），合并它们会改变
可见行为（F-49 的教训）。而三者相同的恰恰是缺上限的读取部分。

**用流本身量尺寸而非 `filesystem::file_size`**：量的就是即将读的这个流，
不存在「查的是路径、读的是另一个东西」的缝隙。

**新增单测** `tests/unit/in_process_read_limit`（测试数 253 → 254），其中一条钉住的是
**两处上限必须同值**——模型导入闸门（F-34）与进程内读取闸门读的是同一批文件，
取值一旦分叉就又回到「一扇锁了一扇没锁」，而没有任何东西会报错。

**三点证伪通过**：判定改成永不抛 → 报红；两处上限改成不同值 → 报红并点名；
把读取函数改成永远抛 → 包读取测试由全绿转红（证明闸门确实长在读取路径上），还原后恢复。

**一处自伤**：第一次接线证伪的 `finally` 只重建了两个 target，导致包读取测试的二进制
**还留着探针代码**，我拿旧二进制跑出红灯并一度误判为「自己改坏了」。
这正是本仓记过的「测旧二进制」陷阱——证伪脚本的还原步骤必须重建**全部**被测 target。

**字节级基线**：PASS，11 个用例 / 738 个产物逐字节一致。

**第二层（清点其余 11 个 `in_process` 能力）与第三层（写进 DTO）仍未做**，
后者属受控修订需裁定。

### F-52 🔴 TIFF 解码按文件头声明的尺寸分配，分配先于校验

**发现经过（2026-09-17）**：F-35 第二层清点 12 个 `in_process` 能力的分配路径时找到的。

`src/slicer_core/tiff_io.cpp`：

```cpp
std::size_t DecodedByteCount(const TiffReadResult& result)
{
    return static_cast<std::size_t>(result.spec.width)
        * result.spec.height * result.spec.samples_per_pixel;   // 三者全部来自文件头
}
void InitializeReadPixels(TiffReadResult& result, const bool retainPixels)
{
    ...
    result.pixels.assign(DecodedByteCount(result), 255U);        // 直接分配【并填充】
}
```

**分配先于校验。** 数据量与声明尺寸是否相符，要到 275–282 行读条带时才对账；
而到那时内存已经分配并填充完毕。现存的唯一尺寸检查（200–210 行）问的是
「文件能否一次读入」——那是**文件**大小，不是**声明**尺寸。

**后果**：一个几 KB、头里声称 100000×100000×6 的 TIFF，会让宿主立即尝试分配并填充约 60 GB。
`std::bad_alloc` 虽可捕获，但 `assign` 会**填充**，即真实提交页面——在触发 `bad_alloc` 之前
就可能把宿主拖进交换或被 OOM 结束。

**可达性已确认**：`RgbwsvtPackageReader` 走这条读取，而它是 `package.render_layer_preview`
与 `package.get_layer_descriptor` 的后端，两者都是 `in_process` 能力；
`tiff_io.cpp:360` 处 `retainPixels` 无条件为 `true`。

**与 F-33/F-34 同族**：畸形输入代价极低、后果落在宿主地址空间、修法都是在分配前加一道对账。
F-33 已修（JSON 深度）、F-34 已修（模型文件尺寸）、F-51 已修（整文件读取），
**这一条是同族里唯一仍然敞开的**，且严重度最高——前三条挡的是「文件真的很大」，
这一条挡的是「文件很小但声称自己很大」。

**修法方向**（实现侧，不触碰契约）：在 `InitializeReadPixels` 之前用**实际文件字节数**
对账声明尺寸——解码后的像素数不可能凭空多于文件所能承载的下限；
超出即抛可捕获异常。上限常量与 `kMaxInProcessReadBytes` 同源。

**另一条顺带记下的**：`package.render_layer_preview` 的 `request.max_width_px`
是在缓冲按包声明尺寸分配完之后才做降采样（`ResizePreview` 是最后一步），
**它是降采样参数，不是准入闸门**。这与 F-35 原文说的「只有 ViewData 有预算语义」
是同一回事的具体形态。

### 证伪脚本的三条纪律（2026-09-17，三次自伤后固化）

本轮写证伪脚本踩了三次坑，每次都让我一度误判结论。三条都不是关于被测代码的，
而是关于**证伪脚本自身**的——它和被测对象一样需要被怀疑。

1. **还原步骤必须重建【全部】被测 target。** F-35 那次 `finally` 只建了两个，
   包读取测试的二进制还留着探针，我拿旧二进制跑出红灯并一度断定「自己改坏了」。
2. **基线测量之前也必须先构建。** F-52 这次脚本开头直接跑基线，用的是上一条命令
   留下的劣化二进制，于是「基线通过」莫名失败。
3. **要匹配断言文本就必须加 `--output-on-failure`。** 否则 ctest 不打印断言内容，
   字符串判据必然落空——而退出码其实是对的，看上去就像「劣化没被抓到」。

共同形状与 F-02「绿灯但什么都没测」一致，只是发生在**证伪脚本**这一层：
判据看起来在跑，实际根本没触及它要判的东西。

### F-35 第二层清点结论（2026-09-17）：不可信输入面已全部设闸

逐条走完 12 个 `in_process` 能力的分配路径，得到一个此前没写清楚的关键区分：

**这些能力的不可信输入是【它们读的文件】，不是请求 JSON。** 请求由宿主自己构造，
与「攻击者塞一个畸形文件进来」不是同一威胁。原 F-35 把两者混在一起谈，
所以显得面很大；按输入来源切开后，需要设闸的其实只有文件读取这一条链。

| 能力 | 不可信输入 | 状态 |
|---|---|---|
| `model.import` | 模型文件 | ✅ F-34 文件尺寸上限 |
| `model.get_metadata` / `model.release` | 同上（已加载的模型） | ✅ 受 F-34 约束 |
| `package.verify` / `get_summary` / `get_layer_descriptor` / `read_report` | 包清单与报告 | ✅ F-51 有界读取 |
| `package.render_layer_preview` | TIFF 层 | ✅ F-52 声明尺寸对账 |
| `scene.get_viewdata` | 已加载场景 | ✅ 原有 `max_bytes` 预算实测有效 |
| `scene.apply_operation` / `get_snapshot` / `geometry.collision` | 已加载场景 | ✅ 受上述闸门间接约束 |

**`ViewData` 的预算是真的**：`TexturedSceneViewDataProvider` 里有候选生成与逐级降级，
并以 `<= request.max_bytes` 实判，与 DTO 的 `budgetExhaustion` 声明一致。

**`render_layer_preview` 的 `max_width_px` 不是预算**：它在缓冲按包声明尺寸分配完之后
才做降采样（`ResizePreview` 是最后一步），是降采样参数而非准入闸门。
真正挡住它的是 F-52——在 TIFF 解码分配点上。

**`scene.apply_operation` 的 `operations` 是数组**，理论上可以很长，但它由宿主构造，
不属于文件类不可信输入；且每个操作对既有实例是 O(1)，不按请求数批量创建实例
（实测无 duplicate/clone/repeat 类操作）。

**结论**：F-35 的实现侧（第一、二层）**已经做完**。剩下的第三层是把配额写进 DTO，
属 `contractVersion 1.14` 受控修订，需用户裁定——而现在有了实测的闸门位置与取值依据，
真要写进协议时不再是拍脑袋。

### 验证口径调整（2026-09-17）：默认改为「字节级基线 + 快集」

**实测依据**（final6 完整分项）：全量 248 项 1754 秒，其中**最慢 8 项占 1320 秒（75%）**，
其余 240 项仅 434 秒。

**而本轮全量回归真正抓到的两条回归都是亚秒级、都不在那 8 项里**：
`slicer_source_size_census_test`（0.41 s）与 `slicer_stage14b_target_graph_test`（0.16 s）。
5 项基线失败也全在 1.2 秒内，失败集合比对在快集里照样成立。

故默认验证改为 `output/p0fix/run_fast.sh`：全量构建 + 字节级基线 + 排除最慢 8 项的 ctest，
约 6 分钟，覆盖 247/255 项。

**仍须跑全量的情形**（写在脚本注释里）：改动触及那 8 项覆盖的域——
hostflow/hostux 宿主流程、frame 画幅、slice_executor、contact_leveling、matvol 生产写包；
以及合入前的里程碑。

**这条判据的正当性依赖「本次改动与那 8 项不相交」这个判断，每次都要重做**，
不能一劳永逸——所以脚本写的是判据，不是「以后都用快集」。

### F-52 的回归状态（如实记录，未跑完）

F-52 提交后的全量回归跑到 **252/255 被主动中止**：5 项失败已全部出现且与基线集合一致，
剩余 3 项为 hostflow（`real_asset_matrix` 单项 625 秒、`drag_interaction`、`scene_refresh`）。

**不记为「全量回归通过」。** 中止的依据是：这 3 项在 final6 全部通过；F-52 的闸门只在
「解码字节数 > 文件字节数 × 128」时触发，而真实资产比值为 1.0–1.2，不可能触发；
字节级基线（含真实切片与写包读包）已通过。缺口由 F-47 的全量回归补齐。

**代价**：若此后这 3 项中有一条变红，无法直接归因于 F-52 还是 F-47，须二分一次。

### F-47 重估并降级（2026-09-17）：原风险叙述已不成立

F-47 原文写「两份代码互不引用，漂移时没有任何机制会报错」。**这句话现在不成立。**

**实测**（分别劣化两侧的 CRC 多项式后跑字节级基线）：

| 编码器 | 劣化后 |
|---|---|
| `RgbwsvPackageWriter`（写包侧） | `cms_tex` 单例即变红 |
| `LayerPreviewWriter`（预览单元） | 全量 11 例变红，71 处差异 |

**两侧都被字节级基线逐字节覆盖**，任一侧的改动只要改变产物就会被抓——基线就是那个「会报错的机制」。

**逐字比对也确认核心算法等价**：`crc32_update` 与 `UpdateCrc32` 同一算法、同一多项式
`0xedb88320U`、同样的 mask 写法，差异仅是格式与循环变量名（`i` vs `bit`）。

**合并的代价反而是实打实的**：三处行为差异方向相反——取消令牌只在写包侧，
缓冲尺寸校验与建目录只在预览侧。合并要逐条决定给缺的那侧补什么，每条都是行为变更。

**故降级为 🟡 并建议不做合并**：收益已被基线吃掉大半，残留风险只剩
「给一侧打的补丁所影响的输入形态不在基线 11 个用例里」。真要处置，
加一条对拍用例（同一像素喂两侧、比对 PNG 字节）比合并更划算，且零行为变更。

**一处过程自伤**：本次先用单个用例 `cms_tex` 做劣化，得出「预览单元未被覆盖」的错误结论，
跑全量 11 例才发现是用例选错——`cms_tex` 的预览来自写包侧，根本碰不到预览单元。
这是本轮第四次因**证伪用例判别力不足**而下错结论，已并入本文的《证伪脚本的三条纪律》。

### F-09 单测缺口（R-12 Gate 4）：七批落地，覆盖 42/56 个公开入口

八个搬出单元共 4596 行、公开入口 56 个，此前新增单测为零。第一批 `tests/unit/slicer_extracted_units`
覆盖 **11 条用例 / 5 个单元**，只挑【无需重型夹具】的入口——那正是搬出来之后才第一次可单测的部分；
需要 `GridSpec` / `SliceConfig` / `ModelReport` 的入口留待后续批次，它们得先有夹具。

**证伪**：同时劣化三处（`layers/layer_` 位宽 6→5、层判据偏移 −0.5→−0.4、进度节流 `% interval`→`% 1`），
三条对应用例全部转红，还原后重建通过。断言有判别力，不是空转。

**写用例过程中纠正了自己的一处误解**：`first_layer_at_or_above_z` / `last_layer_at_or_below_z`
算的是 `ceil/floor(z/t − 0.5)`，判据是**层中心**（层 k 的中心在 `(k+0.5)·t`）而非层边界。
我按「层边界」写断言，在 z=0.3、t=0.1 上得到 3 与 2 不等而一度怀疑是缺陷。**代码没问题，是用例的前提错了。**
顺带一条取值纪律：`0.3/0.1` 在浮点里是 `2.9999999999999996`，拿它做「精确边界」断言测的是浮点误差
而非本函数的约定；改用 `1.25/0.5`（两侧二进制精确）才测得到真东西。

**这正是 R-12 说「搬出来不加测试则债没减」的实证**：搬迁本身没让我读懂这两个函数，写断言才让我读懂。

**批次 2（同文件追加）**：再覆盖 9 个入口 / 12 条用例——`ElapsedMsSince`、
`ResetSupportGenerationStats`、`ApplyOuterVarnishSupportPriority`、`LiftModelForSupportBase`、
`update_layer_channel_stats`、`merge_channel_stats`、`merge_semantic_stats`、
`bbox_to_json`、`channel_stats_array_to_json`。这批要手工造结构体，但都是带默认值的聚合体，
不需要真实模型或配置文件。证伪同样四处扰动、四条转红。

**写批次 2 又纠正了一处误解，而且这次更值钱**：我断言 `ResetSupportGenerationStats`
应当把计数全部清零，撞红后才发现它**刻意只清 8 个字段、留着 island 系列**。
理由在唯一调用方——`CalculateSupportGenerationStats` 先 Reset 再逐层 Accumulate，
而 Accumulate 只重算那 8 个；island 统计是在 `generate_support_masks` 的连通域分析里算的，
清掉就没人补。**「顺手把 Reset 改整齐」会让报告里的岛统计恒为 0：不崩、不报错、产物照出，只是数字没了。**
用例已改成钉住这条分界（该清的清、该留的留），并把理由写在断言旁边。

这是**两批里第二次由写单测纠正我对代码的误解**（前一次是层判据的中心 vs 边界）。
R-12 说「搬出来不加测试则债没减」——实证是：搬迁本身一次也没让我读懂这些函数，写断言让我读懂了两次。

**批次 3（另起目标 `tests/unit/slicer_relief_sampler`）**：覆盖 `ReliefHeightfieldSampler`
的 4 个纯函数入口 / 6 条用例——它是八个单元里唯一此前零覆盖的一个。四处扰动、四条转红。

**为什么另起目标而不是续写**：`slicer_extracted_units/Main.cpp` 已 399 行，续写会越过 G1 的
500 行阈值。G1 只判「新文件」，提交之后再长就看不见了——但钻这个自己刚记录下来的门禁盲点，
和替 R-06 悄悄放宽白名单是同一件事，故按仓库「一目录一目标」的惯例拆开。
代价是多一个 CMake 目标块（R-08 的账），换的是不在门禁上做手脚。

**批次 3 一次即绿**，与前两批各撞红一次形成对照。差别只有一个：这次是**先读实现、再写预期**。
前两批是先按「这函数听起来该怎样」写断言。两轮构建的代价买到的教训就这一句。

本批钉住的两条契约值得单记：`compute_mask_column_ranges` 记的是首末命中之间的
**跨度而非集合**（像素在第 0、2 层命中而第 1 层空，区间仍是 [0,2]，中间的洞被吞）；
`compute_first_model_layers` 与它必须给出同一个最低层，用例里做了交叉断言——
只改其中一个是最可能发生的漂移，而产物上看不出来。

**批次 4（`tests/unit/slicer_mask_rasterizer`）**：掩膜栅格化单元剩余 4 个入口 / 9 条用例。
这是前四批里第一次碰**核心逻辑**而非纯函数——模型采样、外光油壳、上表面支撑边界。
五处扰动、五条转红，同样一次即绿。

**本批钉住的三条契约**：

1. `sample_model_masks` 里 `z = (层号 + 0.5) × 层厚`——**这就是 `first_layer_at_or_above_z`
   那个 `−0.5` 的来源**。批次 1 我从函数形状反推出「判据是层中心」，这里在采样侧得到二次确认。
2. **包围盒之外的层仍要占位**（全零但存在）。写成 `continue` 不 push 不是少一层，
   而是此后每一层的掩膜下标都对错了。
3. **封闭空腔不上光油**——`BuildExternalEmptyMask` 存在的全部理由。用例造了一个
   5×5 实心块正中挖空一格的掩膜：那一格「不是模型、且紧邻模型」，但不与外部连通。
   去掉那道判定后产物**仍然有光油**，只是刷在了打印不到的封闭腔里，肉眼与基线都不一定看得出。

**另记一笔 R-08 的账**：批次 3、4 各自另起了 CMake 目标（共 3 个），因为续写会撞 G1 的 500 行。
这是明知故犯地给 R-08 的目标块又加了 3 块——但另一条路是钻 G1「只判新文件」的盲点，
两害相权取其轻，且在此留痕。

**批次 5（`ReportJsonCases.cpp`，并入已有目标）**：`SliceReportJson` 其余 10 个序列化器 /
11 条用例。六处扰动、六条转红。

**这批换了组织方式，顺带把批次 3、4 欠 R-08 的账还了一部分**：新用例不再另起 CMake 目标，
而是各自成一个编译单元、暴露一个 `int RunXxxCases()`，由 `Main.cpp` 汇总返回码
（用 `|` 不用 `||`，好让前一批红了后一批仍跑完）。这样既不撞 G1 的 500 行，也不再加目标块，
**而且不需要动 `tests/support/Expect.h`**——那个头有 20 个文件在包含。批次 6 起沿用。

**这批的断言重点是键名而非数值**：报告 JSON 由下游按键名读取，改一个键名不崩、不报错，
只会让下游读到「缺字段」并退回默认值，而报告文件看上去仍然完整。证伪的第一处扰动
就是把 `textureSurfacePixels` 改成蛇形——正是这种改法。

**另有两条钉住了容易写错的判定**：连通性汇总里 `enabled` 是跨层的【或】、而计数只算
「开着**且**分量数为正」的层（两条判据不同，合并写会错）；`layerWithMaxComponentCount`
记的是 `layer.layer_index` **而非向量下标**——用下标写也能跑、数值往往也对，
只有诊断向量不是从第 0 层连续排起时才分叉，而那时报告会指错层。用例刻意用了 70/71/72 的层号。

**批次 6（`SupportStatsCases.cpp`）**：支撑放置策略与统计三件套，3 个入口 / 8 条用例，
六处扰动、六条转红。它与批次 2 配成一对：批次 2 钉住 `ResetSupportGenerationStats`
「只清会被重算的 8 个、留着 island 系列」，本批钉住另一半——`Accumulate` 把**层级计数在入口清零**、
而 **result 级计数是累加**的，`Calculate` 则是「先 Reset 再逐层 Accumulate」。
三条合起来才说清这组函数为什么这样分工；单看任何一个都像写漏了。

**本批最要紧的一条**：`ResolveSupportPlacementPolicy` 在 `placement_explicit` 为假时走 legacy 路径，
此时 `upper_enabled` 被**强制关掉**。丢掉那一行不崩不报错，只会让**所有老配置静默打开上表面支撑**——
产物里多出一层没人要的支撑。另钉了一条「未知 mode 两个开关都应为假」，防的是静默退回默认行为。

**批次 7（`PolicyResolverCases.cpp`）**：四个「在多来源之间定优先级」的取值入口，
4 个入口 / 9 条用例，七处扰动、七条转红。这四个出错的方式完全相同——
**不崩、不报错，只是选了另一个来源，而产物看上去完全合理**，所以断言全压在优先级与条件结构上：

- `ResolveProfileDefaultModelFillMaterial` 的**四级优先链**
  （process_profile > material_policy > material_channel > Rgb 兜底），另钉「通道 enabled 为真
  但 mode 是 disabled 时不算数」——只看 enabled 会把显式关掉的配置当成生效。
- `ResolveSurfaceVarnishValue` 的**三条件与**，逐条验缺任一即回落。
- `should_write_preview` 的**首末层无条件写**。少这条则间隔不整除时末层预览消失，
  而末层恰恰是最常被人工核对的一层。
- `NotifyProgress` 的**取消判定在回调判定之前**。调换之后，没设进度回调的调用方就再也取消不掉——
  而那正是批处理场景，最需要能中途停下。这条钉的是**顺序**，不是功能。

**一条证伪纪律的应用**：刻意不扰动 `std::max(1, preview.interval)` → `preview.interval`。
整数除零在 Windows 是结构化异常，`Expect.h` 开头写明 `catch(...)` 接不住——
那样得到的不是「红」，是一次无信息的进程崩溃。

**累计与剩余**：56 个公开入口已覆盖 **42 个**（66 条用例 / 35 处扰动全部转红），余 **14 个**：
materials 8、support 2、CrossSection 2、preview 1、relief 1。
剩下的集中在 `generate_support_masks`、`compose_layer`、`sample_model_masks`、
`prepare_texture_runtime`、`build_*_columns` 这类要吃 `GridSpec` + `SliceConfig` + 掩膜的入口。

**顺带修正一处我自己的高估**：`SliceConfig` 并不重——它是 25 个带默认值子结构的聚合体，
现有单测直接 `SliceConfig config;` 就能用；`GridSpec` 同样是全默认值的聚合体。
真正要动手造的只有小尺寸掩膜与三角形（4×4×3 量级即可），成本比「先立夹具」的说法低。
批次 3 按此推进。

### R-06 查清但**不处置**：这是别的专项的债，且放宽标准要先降一档

路线图把 R-06 写成「修 model.cpp 的 5 处越界依赖」。**查实后这个描述不准确。**

门禁的真实形态是 `ValidateStage14BLayeringFeasibility.py` 里一份**冻结的三项白名单**做**全等比较**，
不是 base→engine 边检查。边检查（`unexpectedEdges`）**是绿的**——两个新头
`model/AssetReferencePath.h`、`model/FrameGeometry.h` 都在 `src/slicer_core/model/` 下、
经 `AssignLayer` 判定同为 **base 层**，没产生任何跨层边。红的只是「名单没同步」。

**这两个头不是本分支引入的**：

| 头 | 引入提交 | 日期 | model.cpp 净变化 |
|---|---|---|---|
| `model/FrameGeometry.h` | `e7628f7b` feat(frame) | 2026-09-07 | +4 行 |
| `model/AssetReferencePath.h` | `d85c6190` fix(unipath) | 2026-09-08 | 0 行 |

两个专项各自新增了 base 层单元并让 model.cpp 调用，但都没同步这份名单，门禁自 09-07 起持续红。

**为什么我不顺手把名单补上**：白名单里 `MtlMaterialParser.h` 那条留了先例，
它的判据写得很明确——「model.cpp **因此由 1982 缩减至 1941 行**，属期望的抽取方向而非累积」，
且附了用户 2026-08-31 的授权与决策文档链接。**这两条不满足那个判据**：model.cpp 一条 +4、一条 0，
都没缩。要放行就得**发明一条比先例更弱的新理由**——而这份门禁存在的意义正是拦住这种悄悄降标准。
叠加两点：放宽门禁须先留授权痕迹；别的专项的债不该由本专项在合入时替它承接。

**本分支与此无关的证据**：`git show 579f89f0 -- src/slicer_core/model.cpp` 只有 +31 行且
**零 include 增删**——那 31 行是 F-34 的取值理由注释（为让 model.h 从 202 行回到 200 阈值内而移入）。

**待拍板（二选一，都很快）**：
1. 认可「新增 base 层单元并由 model.cpp 调用」是合法方向，授权把两条并入名单并写明新判据；
2. 判定 model.cpp 不应再新增依赖，退回 FRAME / UNIPATH 两个专项各自处置。

在拍板前，这条继续记作 5 项已知基线失败之一，不计入本分支回归。

### R-13 / F-10 配置面重组：技术前置已解除，但应与 R-08 合用同一个窗口（2026-09-17）

路线图给 R-13 的**唯一**前置是「`slicer.cpp` 还是 5888 行时动配置，等于同时动两个高风险面」。
F-09 完成后 `slicer.cpp` 是 1961 行，**那个前置已不成立**。所以结论不是「不能做」。

拦住它的是两件此前没量过的事：

| 实测项 | 数字 |
|---|---|
| 引用 `SliceConfig` 的文件 | **92**（src 51 / apps 8 / tests 33） |
| `run_slicer(` 调用点 | 16 |
| `config.cpp` 行数 | 1461（所有 golden 的前置，且已被 G2 豁免） |

**为什么现在不开工**：

1. **改动面比 R-08 大一个量级**。R-08 只动一个 `CMakeLists.txt`，却被明文要求
   「必须在没有在飞分支的窗口做」，理由是合并冲突；R-13 要动 92 个文件，同一条理由只会更强。
2. ~~**当前有三条分支在飞**：`codex/feature-logging-dump`、`codex/feature-host-ui`，
   加上本分支待合入 `product/packaged-slicer`。~~

> **⚠ 同日更正：第 2 条是事实错误，已撤回。**
> 实测 `git merge-base --is-ancestor`：`codex/feature-logging-dump` 与 `codex/feature-host-ui`
> **都已经是 `product/packaged-slicer` 的祖先**，即早已合入，不是在飞分支。
> 我当时只凭「分支还在 + 提交日期近」就判成在飞，没验祖先关系——这是本轮第三次
> **凭形状推断而未取证**（前两次是层判据与统计重置）。
>
> **结论随之改变**：本分支合入 product 之后，仓库里确实没有在飞分支，
> **R-08 与 R-13 的前置条件届时成立**。第 1 条（92 个文件的改动面）仍然有效，
> 它说明的是「这件事要在干净窗口里一次做完」，而不是「不能做」。

**安全网的边界要说清**：`SliceConfig` 拆分是纯类型重构、不改行为，
11 例 / 738 产物的字节级基线会抓住任何产物漂移——这一点比 R-08 有利。
但基线覆盖的是**切片产物**，`config.cpp` 的加载与校验路径它盖不到，宿主与 CLI 的配置面同理。

**建议（按更正后的事实）**：先把本分支合入 `product/packaged-slicer`，
合入即形成干净窗口，随后在同一窗口内依次做 R-08（一个 CMakeLists）与 R-13（92 个文件）。
顺序是 R-08 在前：它把测试目标收拢成清单驱动，R-13 随后要改的测试文件数会少一截。

**现在就能做且零冲突的前置**：路线图提了 5 条车道
（`LegacyConfig` / `GlobalShellConfig` / `MatvolConfig` / `TransferConfig` / `SamplingConfig`），
但**没有把 39 个结构体分派到车道上**——而那个分派正是最容易分错、分错代价最大的部分。
它是纯分析、不碰代码，适合在等窗口期间先出并评审，避免开工后在压力下临时决定归属。

### R-08 已落地（2026-09-17）：测试目标清单化，CMakeLists 减 545 行

合入 product 后窗口成立，当日完成。

| 项 | 改动前 | 改动后 |
|---|---|---|
| `CMakeLists.txt` 行数 | 3440 | **2895（减 545，−16%）** |
| 清单式调用 | 0 | **120** |
| 仍手写的 `add_executable` | 171 | 51 |
| `ctest -N` 测试名 | 268 | **268（零差异）** |

新增 `cmake/SliceSoftTestTargets.cmake`，提供
`slicesoft_add_test_executable(NAME/SRC/LIB [SOURCE_DIR_DEFINE] [STRICT_WARNINGS])`。

**一个设计决定与路线图不同：函数【不注册 add_test】。** 三条实测理由，任一条单独成立即足够：

1. `add_test` 并非都是简单形式。`add_test(NAME unicode_path_contract_tests COMMAND
   unicode_path_contract_tests ${CMAKE_SOURCE_DIR})` 带额外参数——函数若也注册就是**重复注册**。
2. 21 条 `set_tests_properties` 引用具体测试名，顺序敏感。
3. **`add_test` 行本来就是一行，从来不是冲突源**；6~8 行的 `add_executable` 块才是。
   把它挪进函数不减冲突面，只增加改变 ctest 执行顺序的风险。

因此采用**原地替换**：目标定义顺序不变，`set_target_properties` / `add_dependencies` 的引用照旧成立，
`add_test` 174 行**逐行未动**。

**实施中修了自己三处错，都是「先猜形态、没先量」：**

| # | 错法 | 后果 | 发现方式 |
|---|---|---|---|
| 1 | 按行匹配 `target_*` 语句 | 只转出 90 个 | 实测 `target_compile_definitions` 是**多行 47 / 单行 2** |
| 2 | 归一化没吃掉闭括号前空格 | 又漏 30 个 | 打印实际归一化结果，见到 `..."${CMAKE_CURRENT_SOURCE_DIR}" )` |
| 3 | `STRICT_WARNINGS` 是死代码 | 21 个目标白留 3 行样板 | 转换后 `grep -c STRICT_WARNINGS` 得 0 |

第 3 条的根因是 `/W4 /WX` 一律包在 `if(MSVC)` 里，块扫描遇 `if(` 即停。**三处都没造成设置丢失**——
每一步都用「`/W4 /WX` 字面 + 关键字总数」「`SLICESOFT_SOURCE_DIR` 总数」对账，54/54 与 45/45 始终相等。

**验证**：`ctest -N` 的 268 个测试名集合在两遍改动后都**完全相同**（这是「没有目标被静默丢掉」的直接证据），
配以全量重建与全量回归的失败集合比对。

### ctest 耗时的实测分布与提速路径（2026-09-17）

用户问「ctest 太慢，换 GTest 能否提升」。**答案是不能，实测省不到 2%。**

全量 248 项累计 1754 秒，分布极度偏斜：

| 耗时区间 | 项数 | 合计 | 占时长 |
|---|---|---|---|
| < 1 秒 | **196（79%）** | 35 秒 | **2.0%** |
| 1–30 秒 | 41 | 236 秒 | 13.5% |
| 30–120 秒 | 8 | 608 秒 | 34.7% |
| > 120 秒 | **3** | 875 秒 | **49.9%** |

`hostflow_hd02_real_asset_matrix` **一项占 36%**（625 秒）。GTest 是断言与夹具框架，
唯一与速度相关的作用是让多个用例共享进程、省进程启动开销——而那 196 个亚秒测试**合计才 35 秒**，
就算归零也只省 2%。时间全在真实工作里：真切片、写真包、起 worker 进程、宿主 UI 仿真。
（仓库对此另有立场：`AGENTS.md` PC-12 记录「批量改写试过并回退」，R-07 因此选了共享断言头而非上框架。）

**真正的杠杆是并行——而此前 ctest 完全没用 `-j`**，本机 18 逻辑核心。

**并行度不是越大越好，这是算得出来的**：墙钟下界是 `max(最长单项, 总时长/j)`。
串行 1729 秒、最长单项 625 秒，于是 j=3 就已触底，j=4 只留装箱余量，j=8 与 j=4 同样是 625 秒下界。
**想再快必须先拆掉 `hostflow_hd02_real_asset_matrix`，而不是加大 j。**

**已知风险两类**（引入并行时先怀疑这两条）：

1. **超时余量本来就薄**。F-41 记录过「两条 hostux 测试超时余量仅 2.4 倍，有负载即假失败」。
   本轮已复现一次同类现象：`slicer_stage14d07_r2_engine_conformance_test` 在带负载的运行里
   **耗时 35.76 秒并失败**，单独重跑 **7.59 秒通过**——慢 4.7 倍，是负载相关的假失败而非回归
   （该目标是多源文件、R-08 根本没转换它，且同日 18:07 的全量回归里它是通过的）。
2. **66 处用 `temp_directory_path()` 且文件名固定**（含本专项新增的
   `slicesoft_f35_probe.bin`、`slicesoft_f09_b5`）。并行后同名即串。

仓库已给 6 项标了 `RUN_SERIAL`（`stage14e01_c_host_end_to_end`、`hostflow_ha03_c_end_to_end`、
`stage14f02/f03/f04/f05`），说明前人想过并行，地基是有的。

**实测（2026-09-17，268 项全量）**：串行 1729 秒 → **`-j4` 964 秒，1.79×**，
失败集合仍是那 5 项已知、零新增，字节级基线 PASS。**并行是安全的**，可作为默认。

但 1.79× 也就到头了——**真正的天花板是那 931 秒的 `RUN_SERIAL`**。若把
`hostflow_hd02_real_asset_matrix` 拆开并解除其独占标记，模型给出 j=4 约 669 秒（2.6×）、
j=8 约 488 秒（3.6×）。**所以拆它不是「之后可以做」，而是「必须先做」**：不拆的话 j 加到 8 也只有 1.7×。
那 21 项独占测试都是 Qt 宿主仿真（`hostflow_*` / `hostux_*`），独占很可能有真实理由
（共享 offscreen Qt 平台、共享 evidence 目录、起同一个 module DLL），要逐项查而不是批量去标记。

**更正：R-08 没有「第二期」。** 我曾据 `apps/slicer_ui_host_sim/CMakeLists.txt` 有 2095 行
就推断它是自然的下一期——**实测推翻**：那里的 28 个目标**全部是多源文件**（每个 2 到 141 个源），
单源单库的一个都没有，2095 行是源文件清单本身而非样板。**R-08 到此已真正完成。**
（这是同一类毛病的第五次：按类比推断而未先量。）

**一处测算自伤**：我最初只 grep 了根 `CMakeLists.txt` 的 `RUN_SERIAL`（5 处），据此算出
「`-j` 可得 2.8×」。实际独占测试有 **21 项**——大半定义在
`apps/slicer_ui_host_sim/CMakeLists.txt`（2095 行，**R-08 没碰过**，是自然的第二期）。
只看根文件就下结论，这是本轮第四次「凭局部推断全局」。

**另修一处自伤**：`run_fast.sh` / `run_full.sh` 此前都没加 `--output-on-failure`，
导致上述那次失败**没留下断言文本**，只能靠重跑定性。这正是本文《证伪脚本的三条纪律》的第二条，
本轮又犯一次，已给两个脚本补上。

### ctest 提速：结论与收口（2026-09-17）

三条路径量完之后，**收益已经拿到，本条到此为止**：

| 手段 | 效果 | 状态 |
|---|---|---|
| 快集（排除最慢 8 项） | 常规路径约 **8×** | 早已在用 |
| **`-j4`** | 全量 **1.79×**（1729 → 964 秒） | **已验证并设为两个脚本的默认** |
| 拆 `hostflow_hd02_real_asset_matrix` | 全量再约 20% | **不做**，理由见下 |
| 换 GTest | 约 **2%** | 不做 |

**为什么不拆 `hostflow_hd02`**（它占 21 项独占测试 931 秒中的 625 秒、即 67%）：

1. **拆不动的部分是大头**。它的结构是「逐资产循环 31 次导入 → 聚合场景再导入前 22 个可渲染资产」，
   合计约 53 次导入。聚合段天然不可分（场景实例预算 22 是未回签的产品输入，不得抬高），
   所以分片只能把下界从 625 秒降到聚合段的约 250 秒。
2. **它是别的专项三次重固化过的冻结契约**（2026-08-11、09-03、09-16），每次都有事故留痕；
   分片还要给每片单独的期望计数，脆。这是 RENDER/TEXFAIL 的资产，不该由本专项动。
3. **它早已在快集的排除名单里**——日常验证一分钱不花，只在里程碑全量时出现，
   而全量现在是 964 秒，对里程碑闸门可接受。

**去掉其余 20 项的 `RUN_SERIAL` 也不划算**：它们合计仅 306 秒，其中 15 项加起来才 17.7 秒；
且都是共享 evidence 目录的 Qt 宿主仿真，独占很可能有真实理由。

**一处反直觉的实测**：该用例的逐资产 3D 刷新只占 49.2 秒 / 625 秒（**8%**），
其余 92% 在「每资产打开 module DLL + 解析 OBJ」上（Debug 构建，含 15 万顶点级模型）。
**所以想从渲染侧优化它是找错了地方**——这条实测值得留着，免得下次有人照直觉去优化刷新。

### R-13 前置分析：按车道拆分 `SliceConfig` 的收益不成立（2026-09-17）

窗口已开（本分支已合入产品线），动手前先做了路线图要求的「车道归属」分析。
**结论是这个方案不该照原样执行。**

**测量一：谁读 `SliceConfig` 的哪个成员**（25 个顶层成员 × `src/` + `apps/` 全扫）

消费并不按车道聚集——`pipeline` 与 `reports` 两个域几乎读遍了所有成员。
路线图设想的 `LegacyConfig` / `GlobalShellConfig` / `MatvolConfig` / `TransferConfig` /
`SamplingConfig` 五条车道，在实际消费图上找不到对应的边界。

**测量二：每个文件读多少个不同成员**（决定性的一条）

> **⚠ 2026-09-18 更正：下表的数字用的是宽松匹配器，偏高。**
> 当时按裸 `.成员` 匹配得到 99 个消费文件，但 `layer.support`、`result.support`、
> `instance.*` 根本不是 `SliceConfig`，是同名成员的别的结构体。
> 改用 `config.<成员>` 等别名精确匹配后实测是 **42 个消费文件**（虚高 2.4 倍），
> 读 >=10 个成员的是 **5 个**而非 6 个。
> **结论方向不变、反而更强**：42 个消费文件里只有 5 个重度，拆 5 条车道更加无从获益。
> 宽松匹配器在这里不是「更保险」，是错的。

| 读到的成员数 | 文件数（宽松匹配器，已知偏高） |
|---|---|
| 1 个 | **56（57%）** |
| 2–3 个 | 26 |
| 4–9 个 | 11 |
| 10 个以上 | **6** |

99 个消费文件里 **83% 只读 1–3 个成员**。读 10 个以上的只有 6 个：
`config.cpp`（25，它是加载器、天然如此）、`slicer.cpp`（22）、
`GlobalSurfaceShellProductionPipeline.cpp`（14）、`SliceMaterialTexture.cpp`（11）、
`RetainedMaterialLayerComposer.cpp`（11）、`MultiModelProductionService.cpp`（10）。

**所以拆分对谁都没帮助**：56 个单成员文件本来就只碰一处；那 6 个重度文件拆完仍需大部分车道。
而代价是改 92 个文件（src 51 / apps 8 / tests 33）。**收益与代价严重不匹配。**

**F-10 的真实诉求另有便宜解法。** F-10 抱怨的是
「『哪些配置项影响哪个车道』无法从类型判断，只能靠 `DiagnosticEffectiveConfig`（855 行）事后解释」——
那是一个**可理解性**问题，不是耦合问题。既然 83% 的文件只读 1–3 个成员，
这张「配置成员 → 消费者」的映射表本身就是答案，**生成它的成本是 S、零行为变更**，
而且可以注册成门禁：新增配置项若没有消费者，或消费面突然扩散，立刻可见。
本节两次测量用的就是这套方法，脚本现成。

**待拍板（三选一）**：
1. **改做「配置消费图」**（推荐）：S 成本、零风险，直接回答 F-10 的诉求；
2. 仍按原方案拆 5 条车道：L 成本、92 个文件，但上面的数据说明收益不在那里；
3. 搁置 R-13，F-10 维持 🟠。

在拍板前不动 `config.h`。

### 四项待拍板的处置（用户 2026-09-18 授权，按收益率排序执行）

排序依据是实测代价与实测收益，不是严重度。

| 排名 | 项 | 代价 | 收益 | 处置 |
|---|---|---|---|---|
| 1 | **R-06** | XS | 高 | ✅ **已办** |
| 2 | **R-13 方案 a** | S | 中高 | ✅ **已办** |
| 3 | F-35 第三层 | S 改动 / M 协调 | 低 | ⏳ 等触发：下次契约修订 |
| 4 | F-45 补全映射 | M | 低（现在） | ⏳ 等触发：v1 迁移立项 |

#### R-06 已办：分层门禁转绿，已知基线失败 5 项 → 4 项

**一个新查到的事实改变了答案**：原先登记的「二选一」里，第 2 项（退回 FRAME / UNIPATH 各自处置）
**不是真选项**——`AGENTS.md` 里 FRAME 是 `COMPLETE`、UNIPATH **连专项条目都没有**，
而两个提交早已在 product 线上。**没有活的归属方，退回等于让门禁永久红着**，
而常红的门禁不再具备判别力。

**放行判据（与 MATOPQ 那条并列，不是取代）**：允许「新增 base 层单元、由 model.cpp 调用」的依赖，
条件是 **model.cpp 净增不超过 5 行**——即逻辑放到了外面而不是长在里面。
MATOPQ 的原判据（model.cpp 因抽取而缩减）是本判据的**特例**。两条的实测依据已写进门禁源码注释。

**顺带修了它被漏更新两次的根因**：失败信息此前只说「越界了」，
**没写该去哪儿登记**——FRAME 与 UNIPATH 都因此漏更新。现在信息里含实际清单、期望清单、
登记指引与这段事故史。证伪：加一个 base 层但未登记的头，门禁转红且指引出现。

#### R-13 已办：改做配置消费门禁，未按原方案拆车道

新增 `scripts/ValidateConfigConsumption.py` + `scripts/ConfigConsumptionBaseline.json`，
注册两条 ctest（`slicer_config_consumption_self_test` 与 `slicer_config_consumption_test`），
测试数 268 → 270。守两件事：

- **G-C1 任何 `SliceConfig` 顶层成员都必须有消费者**（`config.cpp` 之外）。
  零消费者意味着「用户配得上、但什么都不影响」——已经收下却无人读取的死旋钮。
  实测当前 25 个成员全部有消费者，门禁起点为绿。
- **G-C2 读 >=10 个成员的文件数不得超过基线 5**（棘轮）。盯的是消费面扩散，
  不冻结清单、不替任何专项承接既有那 5 个。

**匹配器的边界写进了脚本头部**：按 `config.<成员>` 等别名做文本匹配、不做 C++ 解析，
方向**偏窄**——匹配到必为真消费，匹配不到可能是经别名访问。因此 G-C1 配了允许清单，
但登记必须写明经哪个别名，且脚本会把**过期的允许清单条目**也报成错误（免得它变成消音器）。

**证伪**：自检 6 条判定用例全过；另对真实仓库加一个没人读的配置成员，G-C1 转红并同时给出
「补别名」与「登记允许清单」两条正确处置路径。

#### F-35 第三层：等下次契约修订，不单独立项

实现侧（第一、二层）已完成、闸门已在生效，**没有正确性缺口**——第三层是把已生效的配额
写进公共 DTO（`contractVersion 1.14`）。收益是「宿主从契约读到上限，而不是撞错误才知道」，
但改的是宿主↔DLL 公共契约，协调成本大于代码量。**触发条件：下次本来就要动
`contractVersion` 时一并带上**，边际成本接近零。

#### F-45 补全映射：等 v1 迁移立项

静默丢弃那部分已处置（未知键改报错），剩下的是「v1 配置表达不了
`background` 与 `geometrySampling`」。**实测 v1 采用率极低**：`samples/configs` 下
130 个配置里只有 4 个声明 schema、**其中仅 1 个是 `slicer.config.1`**。

**但要记一笔：用到 `background` 的有 79 个配置**（都是 legacy，能配）。
它们想迁到 v1 就必须先有这个映射——**所以它是迁移的前置，不是可以无限推的事**。
**触发条件：v1 迁移真正立项时，它排在第一位。**

### 并行缺口同样存在于 IDE 侧（2026-09-18）

用户问「VSCode 的编译环境是否也用并行」。实测结论分两半：

**构建侧本来就是并行的**：`CMakePresets.json` 的 6 个 buildPreset 全是 `"jobs": 16`，
`.vscode/settings.json` 里 `"cmake.parallelJobs": 16`。这一半不用改。

**测试侧从来没并行过，而且比「没加 -j」更糟——`testPresets` 是 0 个。**
而 `settings.json` 里 `"cmake.useCMakePresets": "always"`，
所以 VSCode CMake Tools 的「Run Tests」**根本走不通**，谈不上并行；
`tasks.json` 里唯一那条 ctest 任务（`SliceSoft: Advanced - CTest Legacy Debug`）也没有 `-j`，
而且它指向的是 legacy 的 `build` 目录，主构建目录连一条测试任务都没有。

**已补**：`CMakePresets.json` 新增两个 testPreset——`slicesoft-debug-fast`（排除最慢 8 项）
与 `slicesoft-debug-full`，均 `execution.jobs = 4`、`outputOnFailure = true`；
`tasks.json` 那条 ctest 任务加上 `-j 4`。
实测 `ctest --preset slicesoft-debug-fast -N` 得 **262** 项、`-full` 得 **270** 项，
差 8 正是排除名单，过滤器精确。

**为什么测试用 4 而构建用 16——这不是不一致，理由已写进 preset 的 `description` 字段**：
构建可以吃满核心；而 ctest 有 21 项 `RUN_SERIAL`（独占运行、并行时相加），
实测占全量 1754 秒中的 931 秒。墙钟下界是「独占之和 + 可并行部分/j」，
j=4 已接近触底，再加大收益极小却会放大 F-41 记的「超时余量仅 2.4 倍、有负载即假失败」。
**把理由留在文件里是刻意的**，否则下一个人会把 4 「对齐」成 16。

### 一次归因错误的更正：孤儿 ctest 进程（2026-09-18）

前文把 `slicer_stage14d07_r2_engine_conformance_test` 那次「耗时 35.76 秒并失败、
单独重跑 7.59 秒通过」归因为「MSBuild 节点负载」。**归因错了。**

真凶是一个孤儿 ctest：2026-09-17 我用 `TaskStop` 中止了一次后台快集，
但**它只杀了外层 shell，ctest 子进程活了 15.8 小时**——次日才从
`Total Test time = 56762 秒 / 191 项失败 / 大量 Exit code 0xc0000142` 看出来。
期间我每次构建都在替换它正在加载的 DLL。

**它不影响已交付的结论**（那几轮全量与快集都各自报出恰好 5 项已知失败、基线逐字节 PASS），
但它污染了中间那一次定向测试，并让我下了一个错的归因。

**纪律补一条**：`TaskStop` 之后必须确认子进程真的没了（`tasklist | grep -ic ctest` 应为 0）。
见到 `0xc0000142` 或某项耗时突然翻几倍，第一嫌疑是孤儿进程，不要先去查那项测试本身。

### VSCode 三档验证（2026-09-18）

用户要「迅速完成构建、可不跑或只跑核心检测」。查清后发现诉求与配置有一处错位，
以及一个真实缺口。

**先更正一处前提**：用户说「集成的编译会跑全量测试」——**默认构建任务并不跑测试**。
`SliceSoft: Build Runtime (Debug)`（`group.build.isDefault`）= `Fast Build` → `Deploy`，
带 `dependsOrder: "sequence"`；而 `Fast Build` 走 `--build --preset slicesoft-debug`，
该 preset 的 `targets` 只有 `slicesoft_runtime`，它是个 `add_custom_target`，
**只依赖 6 个产品目标**（`slicer_cli` / `rip_reader_test` / `slicer_ui_host_sim` /
`slicer_host_sim` / `slicer_module` / `slicer_worker`），**不含任何测试目标**。
所以「构建不带测试」本来就是默认行为。

**真实缺口是测试侧无档可选**：`testPresets` 原本是 0 个，
而 `tasks.json` 里**没有任何默认测试任务**——「Run Test Task」给不出东西；
唯一那条 ctest 任务指向 legacy 的 `build` 目录且没有 `-j`。

**已补成三档**：

| 档 | testPreset | 项数 | 并行度 | 量级 | 什么时候用 |
|---|---|---|---|---|---|
| 核心 | `slicesoft-debug-core` | **155** | **8** | **实测 12–18 秒**（四次取最小 12.09 秒） | 改完想立刻看一眼 |
| 快集 | `slicesoft-debug-fast` | 262 | 4 | 约 3 分钟 | 提交前 |
| 全量 | `slicesoft-debug-full` | 270 | 4 | 约 16 分钟 | 合入产品线等里程碑 |

`tasks.json` 相应加了 4 条任务：`Core Checks`（**设为默认测试任务**）、
`Build + Core Checks`（快速增量编译后立刻跑核心检测，不部署 Runtime）、
`Fast Subset Checks`、`Full Checks`。

#### 核心档的判据：按「是否起进程」而非按耗时阈值

**第一版我用的是「耗时 >= 2 秒」筛排除名单，那是错的**，两个毛病：

1. 它只是个数字、没有理由——名单一过期，没人知道该怎么重算、为什么是 2 秒。
2. **更糟的是它筛出的集合在并行下不可靠。**

**第 2 条的实测证据**（同一批测试，串行的 final6 对比今天的 `-j4` 快集）：

| 测试 | 串行 | `-j4` | 膨胀 |
|---|---|---|---|
| `hostflow_he01_stl_import` | 0.81 s | **20.09 s** | 25× |
| `hostflow_hb06_slice_job` | 1.32 s | **17.50 s** | 13× |
| `slicer_stage14d07_r2_engine_conformance_test` | 1.47 s | **14.11 s** | 9.6× |
| `slicer_stage14d01_worker_shell_contract_test` | 0.81 s | 9.42 s | 11.6× |

这些测试起子进程或用 Qt offscreen，并行下墙钟膨胀十几到二十几倍，
而它们有**为静默机器校准的内部期限**（F-41 记录在 `HostSliceJobController`）。
于是 `-j4` 下冒出假失败：今天快集多出 `slicer_stage14e04b_capability_coverage_test`
与 `hostflow_ha03_qt_end_to_end` 两项，**单独重跑 6.78s / 7.39s 全部通过**。
**注意这不是 ctest 的 TIMEOUT**（前者无自定义超时、后者 90 秒而实耗 9 秒），是测试自己的内部期限。

**改用「是否起进程」划分，这是有物理原因的边界**：

| | 项数 | 串行耗时 | 占全量 |
|---|---|---|---|
| **纯逻辑（不起进程）** | 144（现 157） | **43.1 秒** | 2.4% |
| 起进程 / Qt | 104 | 1711 秒 | **97.6%** |

纯逻辑里最慢也只有 3.36 秒，且不起进程因而**并行安全**，所以核心档用 `jobs: 8`。
**实测 155 项 / 12–18 秒（四次取最小 12.09 秒）、100% 通过、零假失败**——纯逻辑切分按设计生效。
（单次波动仍有约 1.5 倍，故报区间而非单值；本机短基准波动可达 47%。）

**另排除 2 项继承自合并基 `d28b6451` 的既有失败**
（`stage16c06_bounded_support_shape_unit_tests`、`scene_layer_adapters_unit_tests`）。
理由是可用性而非掩盖：**一个恒显红灯的「十秒看一眼」档会训练人忽略红灯**，
所以这一档做到「好就是绿」；那 2 项在 fast / full 两档里照常出现，债没被藏起来。
它们修好之后应把这两条排除删掉——这句话也写进了 preset 的 `description`。
被排除的家族前缀：`hostflow_` `hostux_` `matvol_` `frame_` `stage16_` `stage14d` `stage14e`
`stage14f` `textured_scene_` `ripflow_` `rip_` `test_spi`。

#### 并行档加了 `--repeat until-pass:2`

fast / full 仍含起进程的测试，`-j4` 下会偶发假失败。自动重试一次即可消除——
**真失败会重复出现、假失败不会**，且 ctest 会在输出里标出重试过的项。
这不是把失败藏起来：需要重试才过的项仍然可见，值得去看一眼。

#### 一个容易被「顺手改掉」的地方

核心档并行度 8、其余档 4、而构建侧是 16。**这三个数字各有理由，不是不一致**：
构建能吃满核心；核心档没有进程竞争所以可以吃一些；而含起进程测试的档受
21 项 `RUN_SERIAL`（占全量 931 秒）与上述内部期限双重约束，j=4 已接近墙钟下界。
**理由已逐条写进每个 testPreset 的 `description` 字段**，就是为了拦住下一个想把它们对齐的人。
### launch.json 判断与 F5 链路实测（2026-09-18）

用户问「9 条 launch 配置都不跑测试，为什么处理时间有时也很久」。逐段实测后答案很清楚。

**先确认前提**：9 条配置的 `preLaunchTask` 链已逐条查过，**一条都不跑测试**。
以用户当前选中的 `Run Packaged Host UI (Release)` 为例：

```
Run Packaged Host UI (Release)
  -> Build Packaged Host UI (Release) -> Build Runtime (Release)
       -> Fast Build (Release)      # 只建 slicesoft_runtime 的 6 个产品目标
       -> Deploy Runtime (Release)  # 增量：按内容哈希 + 大小 + 时间戳判断
  -> 启动 slice_soft_test.exe
```

**零改动时各段实测**（多次取最小）：

| 阶段 | 耗时 |
|---|---|
| Configure（`cmake --preset slicesoft-main`） | **24.6 秒**（三次 26.4 / 27.1 / 24.6） |
| Fast Build（真正零改动） | **8.5 秒** |
| Deploy（增量） | 13.8 秒 |
| 合计 | **约 47 秒** |

**最大一笔是纯浪费**：`Fast Build` 无条件挂着 `dependsOn: Configure Main Build`，
所以每次 F5 都重跑一遍 24.6 秒的配置——**而它比零改动构建本身还慢近 3 倍**。

**为什么它是冗余的（已实测证实）**：cmake 自带 ZERO_CHECK。
故意改动 `CMakeLists.txt` 后只跑 `cmake --build --preset`，它自己打印了
`Configuring done (10.2s) / Generating done (4.3s)`——**`--build` 会按需重配**。
无条件先跑一遍配置，在零改动时纯属白花。

**已去掉** `Fast Build (Debug)` 与 `Fast Build (Release)` 的该依赖，
**F5 从约 47 秒降到实测 28.1 秒（快 40%）**——注意 28.1 是端到端实测，不是分段相加的 22 秒。保留 `Advanced - Build Worker Debug` 那一处未动
（不在 F5 热路径上，也未实测）。边界：新建或删过构建目录时需手动跑一次
`Configure Main Build`，届时 `--build` 会以「找不到 cache」明确报错，不会静默出错。

**一句过期描述是根因**：`Configure Main Build` 的 `detail` 原写「已有 cache 时通常只需约 1-2 秒」，
**实测差 12~25 倍**。那句话大概就是一直没人质疑这条依赖的原因——已按实测改正并写明差距。

**另补两条 launch 配置**（原先的缺口）：
`Quick Run Packaged Host UI (Debug, No Build)`（此前只有 Release 有，不对称）与
`Debug Packaged Host UI (Debug, Core Checks)`（前置任务含核心检测，**检测失败会中止启动**）。
后者之所以可行，正是因为核心档做到了「好就是绿」且只要 12 秒。

**一处未动的既有问题**：9 条配置的 `visualizerFile` 都指向机器特定的绝对路径
（`C:\Users\admin\AppData\Roaming\Code\User\workspaceStorage\<哈希>\...`），
换机器或该哈希变化后失效。属既有配置、可能是单机有意为之，未擅自改，此处留痕。
### launch.json 命名整理与「过时 CLI 配置」核查（2026-09-18）

用户问两件事：配置名能否再优化；有没有不适配新软件的旧 CLI 调用环境、有则删除。

**第二问的答案是：没有，一条都不该删。** 逐条核对了 5 条 CLI/工具配置所传的开关，
在当前源码里**全部仍被识别**：

| 配置 | 开关 | 源码 |
|---|---|---|
| CLI: Slice a config | `--config` | `apps/slicer_cli/main.cpp` ✓ |
| CLI: Inspect model | `--inspect-model` | 同上 ✓ |
| CLI: Preview only | `--preview-only` | 同上 ✓ |
| Tool: RIP reader summary | `--package` `--summary` | `apps/rip_reader_test/main.cpp` ✓ |
| Tool: Worker SPI request | `--spi-request` | `apps/slicer_worker/Main.cpp` ✓ |

`Tool: Worker SPI request` 指向的 `request.json` 当前不存在，但那**不是失效**——
它的前置任务跑 `scripts/Prepare14D08WorkerDebugRequest.py --output ...` 按需生成。
**删掉这些就是删掉能用的工装。**

**查出的唯一真缺陷**：输入变量 `packageDir` 的默认值 `output/NailRgbWhiteVarnishTop2`
**实测不存在**。根因不是它写错了，而是 `output/` 不入库（50 个本地包目录全在 gitignore 下），
**任何硬编码默认都注定过期**。改成空默认 + 说明去哪儿找（`ls output/`，如 `output/S1Probe`）——
留一个按回车就报错的值，比没有默认更坏。

**第一问：已改名并按常用度重排。** 原命名有三处实际妨碍：

1. `SliceSoft 14E: Debug Packaged Host UI (Debug)` —— **Debug 出现两次而含义不同**
   （挂调试器 vs 构建配置）。这是最容易点错的一条。
2. 两套分隔风格并存（`14E:` 与 `Advanced - `），扫一眼分不出组。
3. 每条都以 `SliceSoft` 开头——本仓唯一的项目，纯属占位；而 `14E` / `Advanced`
   是阶段号与模糊分组，挡在了真正要读的部分前面。

新命名把**动作**提到最前、配置放括号，四组一眼可分，并按常用度排序：

```
Run: Host UI (Release, no build)      <- 最快，放第一
Run: Host UI (Release)
Run: Host UI (Debug, no build)
Run: Host UI (Debug)
Debug: Host UI                        <- 只有 Debug 配置能挂调试器，故不再重复写 (Debug)
Debug: Host UI + core checks
CLI: Slice a config
CLI: Inspect model
CLI: Preview only
Tool: RIP reader summary
Tool: Worker SPI request
```

**改名的代价已先核实再动**：这些名字只被 `.specstory/` 的对话记录与本轮新写的本文引用，
没有任务卡或正式文档依赖。改完逐条校验了 `preLaunchTask` 与任务间 `dependsOn`，
**零悬空引用**——这是必须查的，悬空会让 F5 直接失败。

**刻意没动 tasks.json 的标签**：它们被 `preLaunchTask` 与任务间 `dependsOn` 交叉引用
（实测 40 条任务、引用关系完整），改名风险与收益不成比例。要做应单独一轮并连带更新全部引用。
