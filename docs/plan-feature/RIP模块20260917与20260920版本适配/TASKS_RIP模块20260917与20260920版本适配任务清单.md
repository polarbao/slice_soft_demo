# RIP 模块 20260917、20260918 与 20260920 版本适配任务清单

> 文档状态：**COMPLETE / EXTERNAL_VALIDATION_DEFERRED**
> 分支：`feature/codex-ripflow-vendor-20260920`
> 基线：`develop/packaged-slicer@7af5d095`
> 日期：2026-09-20
> 规则：本清单是本次增量适配的状态真源；完成任一卡必须补实际验证和修订记录。

## 任务总览

| 卡片 | 内容 | 状态 | 依赖 |
|---|---|---|---|
| RIPUP-00 | 分支、worktree 和基线隔离 | COMPLETE | 无 |
| RIPUP-01 | 20260917/20260918/20260920 文件、哈希和 CLI 合同盘点 | COMPLETE | RIPUP-00 |
| RIPUP-02 | 组合来源 schema v2 与 fail-closed 解析 | COMPLETE | RIPUP-01 |
| RIPUP-03 | 打包、provenance、manifest 和 CMake 部署更新 | COMPLETE | RIPUP-02 |
| RIPUP-04 | `--verbose`、中文路径及默认日志合同验证 | COMPLETE | RIPUP-03 |
| RIPUP-05 | 真实小包 RIP 与宿主矩阵验证 | COMPLETE | RIPUP-03 |
| RIPUP-06 | RIPFLOW 状态、报告和用户迁移说明更新 | COMPLETE | RIPUP-04、05 |
| RIPUP-07 | RIP 定向回归、差异检查和专项收口 | COMPLETE | RIPUP-06 |

## RIPUP-00 分支和基线隔离

**状态：COMPLETE（2026-09-20）**

**实际结果：** 检测到原工作树位于 `feature/claude-monowrap-panel-guard` 且包含用户未提交修改；未切换或覆盖该工作树。从 `develop/packaged-slicer@7af5d095` 建立独立 worktree 和 `feature/codex-ripflow-vendor-20260920`。

## RIPUP-01 供应方版本盘点

**状态：COMPLETE（2026-09-20）**

**实际结果：** 确认 20260917 是中文路径宽字符加载修复；20260918 CLI 源码与 0917 相同但 EXE/DLL 重新生成；20260920 新增默认 DEBUG 过滤与 `--verbose`，且 DLL 再次变化。头文件和 `tiff.dll` 四版一致，三个新目录均缺 `CmykFiles`。3 层真实输入对比中，0917/0918 两档逐文件一致；0920 的 mode0 一致、mode1 发生变化。

## RIPUP-02 组合来源 schema v2

**状态：COMPLETE（2026-09-20）**

**内容：** 新增 `binaryDirectory` 和 `resourceDirectory`，分别固定 20260920 二进制和 20260909 资源；解析器兼容 v1/v2，非法路径和缺失输入 fail-closed。

**验收：** 新 pin 可解析；新增日期目录不自动切换；缺二进制或资源明确失败；不回退旧资源。

**实际结果：** `source.json` 升级到 v2，固定 0920 二进制与 0909 资源；解析器保留 v1 读取兼容，并由 `TestRipModuleSourcePin.ps1` 覆盖默认、v1、非法 schema、路径逃逸、缺二进制和缺资源。脚本实测 PASS。

## RIPUP-03 打包与部署更新

**状态：COMPLETE（2026-09-20）**

**内容：** 分别复制 3 个二进制和 8 个资源，provenance 记录双来源；模块升至 1.3.0；同步 CMake、Runtime 打包和宿主准入。

**验收：** 11 文件哈希全部通过；宿主根不出现私有 `tiff.dll`；source v1 工程 override 仍可使用。

**实际结果：** 模块提升到 1.3.0，打包器分别复制 0920 的 3 个二进制和 0909 的 8 个资源；`source_provenance.json` v2 记录双来源。CMake、Runtime 部署和宿主准入同步完成，模块自检 11/11 文件 PASS，私有 `tiff.dll` 只位于模块目录；v1 pin 兼容用例 PASS。

## RIPUP-04 路径与日志合同

**状态：COMPLETE（2026-09-20）**

**内容：** 自检要求 `--verbose`；在中文目录运行 CLI；验证宿主命令默认不注入 `--verbose`，默认日志显著少于显式 verbose。

**实际结果：** 中文路径下默认和 `--verbose` 两次运行均 exit 0、输出 3/3 层；默认日志 27 行，verbose 日志 30 行，新增 3 条 `[调试]`。集成测试确认生产命令不注入 `--verbose`。

## RIPUP-05 真实 RIP 验证

**状态：COMPLETE（2026-09-20）**

**内容：** 对项目内小型 Package 运行新版模块，覆盖必要的 ripMode/transparent 代表组合，确认输出、结果合同、输入文件身份和诊断隔离。

**边界：** 本地结果不升级 `EXTERNAL_VALIDATION_DEFERRED`，不等同于打印软件或物理打印验收。

**实际结果：** 同一 3 层 16x16 RGBWSV 输入的两档 `ripMode` x 五档 `transparent` 宿主矩阵 10/10 PASS；结果合同、输入不变和诊断隔离通过。0917/0918 两档输出逐层一致；0920 的 mode0 与 0918 一致、mode1 为 3/3 层哈希变化，已按最新版本重新验证，未将旧 Golden 强加给供应方新实现。

## RIPUP-06 文档同步

**状态：COMPLETE（2026-09-20）**

**内容：** 更新既有 RIPFLOW 任务卡、用户迁移指南、模块 README，并新增本次版本适配报告。

**实际结果：** 已同步 RIPFLOW D-09、模块 README、两份用户说明、专项上下文和 `REPORT_RIPFLOW_D09_20260917至20260920版本适配验证.md`。

## RIPUP-07 回归与收口

**状态：COMPLETE（2026-09-20）**

**内容：** 运行 RIP 定向测试、核心档检查和 `git diff --check`；记录失败集合而非仅记录数量。

**实际结果：** Debug 全目标串行重建 PASS；RIP 定向 CTest 11/11 PASS；合同 `positive=8 / negative=13` PASS；Runtime 模块打包与自检 PASS。核心档观察到 `diagnostics_packaging_tests` 的并发 PowerShell 环境抖动及基线未生成 `production_mode_catalog_unit_tests` 目标，两者均不在 RIP 改动面；按用户授权不继续处理切片软件旁路问题。本卡以 RIP 适配验证为收口边界，外部验收状态不变。

## 修订记录

| 日期 | 版本 | 变更 |
|---|---|---|
| 2026-09-20 | v1.0 | 建立 RIPUP 增量任务清单；完成分支隔离和供应方版本盘点；RIPUP-02 开工 |
| 2026-09-20 | v1.1 | 补入 20260918 中间版本；记录三版二进制、CLI 和小型真实输出差异；RIPUP-02 完成，RIPUP-03 开工 |
| 2026-09-20 | v1.2 | RIPUP-03..07 完成：固定 0920 二进制与 0909 资源，模块 1.3.0、verbose/中文路径、10 档宿主矩阵、Runtime 打包及 RIP 定向回归通过；切片软件旁路问题按用户授权排除 |
