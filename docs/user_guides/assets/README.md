# 使用手册截图资产

本目录保存切片软件使用手册引用的界面截图。截图只用于说明主要操作入口，
功能状态仍以对应分支的当前代码和任务清单为准。

## XYPAD 追加配图（2026-09-09）

`packaged_slicer/21_XY原点补白开关.png` 为 `runtime/slicesoft-xypad/Release` 0.2.446-dev 的真实控件截图，
显示 X/Y 两项均勾选（产品默认均不勾选）。使用独立 `--manual-guide-self-test` 进程，
经 CaptureSliceGuideScreenshots.ps1 打开设置并切换复选框，直接截取“切片参数”区域；未重绘、未改用户工作区。
已查看确认两项完整可见，专用实例正常关闭。图 20 保留作原 X-only T 工艺示例，不替代本图的新控件布局。
