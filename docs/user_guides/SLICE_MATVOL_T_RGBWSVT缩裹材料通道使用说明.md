# SliceSoft RGBWSVT 缩裹材料 T 通道使用说明

> 适用版本：MATVOL-T T-09  
> 状态：显式生产 opt-in  
> 日期：2026-08-26

## 1. 使用边界

RGBWSVT 是独立新版协议，不替换旧 RGBWSV 默认流程。需要 T 通道时，在参考 Host 中显式选择
`host-reference-transfer-channel`，再选择部署目录中的 `_rgbwsvt` 新版工艺。未选择该 Profile 时，
软件继续使用旧 `p0.rgbwsv.2` 六通道协议。

生产准入适用于 Host/Worker 的 Scene 路径。**2026-09-20 起该路径支持一版多件**：
版面上多个可见实例会合成到同一张画布、产出一个包，画幅取整版最大外包围区域。
此前限定「恰好一个可见实例」，多于一个直接 fail closed。零可见实例仍然拒绝。

直接运行 `slicer_cli` 生成的 RGBWSVT 包仍标记为 `rgbwsvt_candidate_unvalidated`，
不能当作已准入生产包——这一条未变。

## 2. 工艺与材质

缩裹区域的识别有**两种模式**，由工艺 JSON 的 `transferChannelPolicy.matchSource` 决定。

**一、`material_diffuse_rgb`（按颜色精确匹配，原有模式）**

- 材质 `01` 是甲片，材质 `02` 是缩裹区域。
- 软件不按材质名或文件名识别缩裹区域；匹配颜色来自所选新版工艺 JSON 的
  `transferChannelPolicy.materialDiffuseRgbValues`。

**二、`whole_model`（整模即缩裹，2026-09-20 新增）**

- **整个模型都是缩裹材料**，不读材质表，`materialDiffuseRgbValues` 必须留空
  （配了颜色即视为配置矛盾，直接拒绝）。
- 供**无 mtl 的纯几何资产**使用——那类资产没有颜色可匹配，原有模式必然解析为空。
- 该模式**不经体积求解**，因此不要求模型流形或无自交。一批在按颜色匹配下因拓扑
  被拒的资产，换整模模式可以正常切片。
- UI 对应预设为「单材料缩裹｜整模 T 实体｜下表面支撑」。
- 支持**一版多件**：多个可见实例合成到同一张画布、产出一个包（见 §1）。

**两种模式共同的语义**

- 缩裹像素**只写 T**，与前六通道互斥——被判为缩裹的像素不会同时出现在 RGB/W/S/V 里。
- 按颜色匹配时，甲片继续使用对应工艺的彩色 RGB、白墨 W 或光油 V 语义；模型没有匹配到
  缩裹区域时 T 全空，甲片按所选新版工艺正常切片。整模模式没有「甲片部分」，也不存在
  「匹配不到」的情形。

需要调整材质 Kd/RGB 时，应新增或修改新版 `_rgbwsvt` 工艺文件，不在 C++ 软件中固化颜色。旧工艺
文件必须保留，不应原地改成七通道。

## 3. 输出合同

```text
Package schema     p0.rgbwsvt.1
Channel order      R G B W S V T
Bit depth          uint8
Polarity           black_is_print
Production status  productionAcceptance=admitted
```

Host/Worker 在切片完成后会再次严格校验 Package。协议错配、缺少 T、准入状态缺失或未知、统计不一致、
多材质同时匹配、缩裹拓扑不合格时均直接失败，不会回退旧协议。

## 4. 当前模型范围

- `03.obj`：当前仓库内显式生产正例，材质 02 可形成闭合 T 体积。
- `08/09.obj`：颜色可识别，但当前材质 02 的开放拓扑不满足体积 Gate，软件会稳定拒绝且不产包。
- 本专项不修复 `08/09.obj`，也不把其拒绝结果计为生产正例。

## 5. 验收检查

切片完成后，应在 Package Review 中确认：协议为 `p0.rgbwsvt.1`、通道数为 7、顺序以 T 结尾、
`productionAcceptance` 为 `admitted`，且缩裹模型的 T 打印像素非零。

**一版多件时另外确认两条**：

- 产出是**一个**包，不是每件一个；画幅为整版最大外包围区域。
- 用同一版面换「单材料光油」切一次作参照，两者的**打印像素几何应当一致**，
  差别只在落到 V 还是 T。这是判断「某一件没被合成进去」最省事的办法——
  件数或位置不对，两版一比即现。

设备和实物打印效果仍应按现场验收流程确认；仓库内通过不等于物理打印或现场 SLA 已通过。

## 6. 修订记录

| 日期 | 版本 | 变更 |
|---|---|---|
| 2026-08-26 | v1.0 | 建立 RGBWSVT 显式生产 opt-in、工艺配置、输出合同和模型边界说明。 |
| 2026-09-20 | v1.1 | 新增 `whole_model` 整模识别模式；Scene 路径由单实例放开为一版多件，画幅取整版最大外包围区域。零可见实例与 CLI 直出包的准入标记未变。 |
