# TASKS-03：缩裹整版多实例

> 专项：MONOWRAP ｜ 建档：2026-09-20 ｜ 依据：[ANALYSIS-04](ANALYSIS-04-缩裹整版多实例的根因与实现面实测.md)
> 目标（用户 2026-09-20 裁定）：**仿照单材料光油 / 白墨的方式切片，产出一个最大外包围区域的整版包**
> 状态：🚧 **已授权开工**（用户 2026-09-20）

## 0. 开工前提

| 项 | 状态 |
| --- | --- |
| 方案文档 | ✅ ANALYSIS-04 |
| 用户确认动工 | ✅ **已取得**（2026-09-20：「可按照你的建议进行后续任务的处理」） |
| §5 的 T 归属语义裁定 | ✅ **甲选**——见下 |
| 门禁放宽授权文档 | ✅ [DECISION-03](DECISION-03-放开单实例护栏的授权.md)（**先于代码写成**，含五条前置条件） |

### T 归属语义的裁定依据

用户 2026-09-20 原话：

> 单缩裹材料的切片处理，可参考 `单光油/白墨` 预设工艺的处理方式。
> **选择该工艺，则默认模型都是一个材料进行配置。**

「整个模型都是一个材料」直接确定了 T 的归属：缩裹像素**就是模型自身**，
不是模型外的附加层，故 T 跟随 `Model` 归属（甲选），不新开归属类。
这同时确认了整模模式对场景内**每一个实例**生效，而非只作用于某一个。

乙选（新开 `TransferWrap` 归属、允许缩裹跨出模型轮廓）**不做**，
留待真出现「缩裹包住模型外一圈」的工艺需求时再议。

> ~~仍保留 ANALYSIS-04 §5 建议的重叠计数。~~ **已作废**：后续核查发现合成器
> 对跨实例模型像素重叠是直接拒绝（`InstanceOverlap`），不存在「悄悄重叠」
> 需要计数的情形。见 ANALYSIS-04 §6c。

## 1. 任务清单

| 编号 | 任务 | 依赖 | 状态 |
| --- | --- | --- | --- |
| MW3-00 | 落基准：当前单实例缩裹产出的字节级快照，作为「行为不变」的判据 | — | ✅ **完成**：10/10 件、4135 产物，`E:\slicesoft-monowrap-mw300\snapshot-before.json` |
| MW3-01 | `SceneInstanceRasterLayer` 加可选 `transfermask`；合成器在唯一的 `WriteOwnedPixel` 调用点顺带写整版 T 掩膜（见 ANALYSIS-04 §6d） | MW3-00 | ✅ **完成**：字节基线 738 产物仍逐字节一致 |
| MW3-02 | 整版 T 掩膜经 `transfermasksink` 与六通道层同批交出 | MW3-01 | ✅ **完成**：2 条用例经双向故障注入证伪 |
| MW3-03 | **反例验证**跨实例 T×T / T×V 触发既有 `InstanceOverlap` / `MaterialConflict`（**无需新写拒绝逻辑**，已核查，见 ANALYSIS-04 §6c） | MW3-02 | ⬜ |
| MW3-04 | 整版七通道层装配：把整版六通道层 + 两张整版掩膜交给**现有** `ComposeRgbwsvtLayer` | MW3-02 | ✅ **完成**：端到端用例经故障注入证伪 |
| MW3-05 | `RunTransferProductionEntry` 改道为 `RunMultiModelProductionService` | MW3-04 | ⬜ |
| MW3-06 | 放开两道 fail-closed 护栏（授权见 [DECISION-03](DECISION-03-放开单实例护栏的授权.md)，**五条前置条件缺一不得落地**） | MW3-05 | ⬜ |
| MW3-07 | 包写入器 / 报告 / 契约 schema 跟随七通道 | MW3-05 | ⬜ |
| MW3-08 | 宿主侧：整版缩裹作业能提交、进度与报告正确 | MW3-06 | ⬜ |
| MW3-09 | **单实例缩裹字节级不变**（对 MW3-00 快照） | MW3-05 | ⬜ |
| MW3-10 | 新用例：12 件整版缩裹合成一个包，T 像素总数 == 各件模型像素之和 | MW3-06 | ⬜ |
| MW3-11 | 反例：T 归属规则被违反时闸门必须变红（证伪） | MW3-03 | ⬜ |
| MW3-12 | 全量档 + 11 例字节级基线 | 全部 | ⬜ |

## 2. 验收判据

1. **整版**：用户那 12 件模型排在一版上，一次切片产出**一个** RGBWSVT 包，
   画幅为整版最大外包围区域，与光油 / 白墨同形。
2. **只写 T**：整版包里 R/G/B/W/S/V 全空，T 像素总数等于 12 件模型像素之和。
   （不必考虑重叠：模型像素重叠时合成器整单拒绝，不会产出。）
3. **单实例不回归**：改动前后单实例缩裹产出**逐字节相同**（MW3-09）。
4. **六通道不受连累**：11 例字节级基线全绿（MW3-12）。v3 方案下六通道合成
   本就零改动，这一条是对该判断的实测确认，而非补救。

## 3. 与 TASKS-01 / TASKS-02 的关系

- TASKS-01 的 MONOWRAP-10 与 MONOWRAP-11 **已于 2026-09-20 各自结清**
  （见 TASKS-01 §3.8、§3.9），TASKS-01 十二项全部完成。
- 因此 MW3-12 的全量档与基线是**在整模单测已在闸门内**的前提下跑的，
  那一跑能证明整模路径，不是空转。
- TASKS-02 的 CFG/UX 各项与本表无依赖。

## 4. 不在本表范围内

- 缩裹跨出模型轮廓（外圈缩裹）——那是乙选归属语义才需要的工艺能力，当前无需求。
- 5 件拓扑超限资产——用户已单独处理。
- `slicer_cli` 直出包的生产准入标记（`rgbwsvt_candidate_unvalidated`）——既有设计，不动。
