# DOC_CHECKLIST_14 对 RIP 侧技术确认清单

> 文档用途：**对外确认函**。请 RIP 负责人（及产品决策者）按 §4 的模板一次性回复。
> 版本：v1.4 ｜ 日期：2026-08-04 ｜ 发起方：切片软件（SliceSoft）
>
> ✅ **两轮问答均已闭合（2026-08-04）。本文自此转为「往来记录档案」。**
> **权威条款已收敛至 `DOC_DECISION_14_S2_RIP接口合同定案.md` —— 实施只看该文。**
> 本文保留全部往来过程（含被否决的候选方案），仅供溯源，不得据以实施。
> 第二轮回复评估见 §4.4。
> 对应任务：`TASKS_14` 的 14A-08 ｜ 阻塞范围：Stage 14 的 14F（三方联调）与 03E-02
> **本文自包含**：读者无需阅读切片项目其他文档即可作答。

---

## 0. 为什么需要这份确认

切片软件、RIP、打印软件三方即将做模块化集成。数据链路是：

```text
切片软件 ──S1──> RIP ──S2──> 打印软件通道化 ──S3──> 打印执行
        p0.rgbwsv.2      rip.ch7.1        12 物理通道
```

**S1 已冻结**（切片输出协议），**S3 已存在**（打印软件既有实现），但 **S2 尚未确认**——即"RIP 吃什么、吐什么"这一段。下面 6 个问题就是 S2 上尚未闭合的点。

在这些问题回复之前，切片侧与打印侧都无法编写与 RIP 对接的代码（会返工）。

---

## 1. 背景速览（作答所需的全部事实）

### 1.1 切片侧输出：`p0.rgbwsv.2`（S1，已冻结，不可改）

```text
目录结构   <packageDir>/manifest.json + layers/layer_%06d.tiff + reports/ + preview/(可选)
通道数     6，固定顺序 R, G, B, W, S, V
位深       8 bit 无符号
存储       stripped（默认，rowsPerStrip 在 manifest 声明）或 tiled → 两者都可能出现
planar     contiguous（交错）
极性       black_is_print → printValue = 0（出墨）、emptyValue = 255（空/不出墨）
背景       模型外区域 6 通道全部 255
DPI        X 与 Y 可不相等（本项目实际使用 X=635 或 720、Y=600）
```

⚠️ **极性最容易搞反**：`0` 是"出墨"，`255` 是"不出墨"。与"值越大墨越多"的直觉相反。

⚠️ **通道语义**：`R/G/B` 是彩色，`W` 白墨，`S` 支撑材料，`V` 光油。**目前 W/S/V 只有 0 或 255 两个值（二值）**，没有中间值。

### 1.2 打印软件侧输入要求：`ChannelSplitter`（S3 上游，硬约束）

打印软件的通道化模块对输入 TIFF 有**硬校验，不满足直接报错**：

```text
samplesPerPixel  必须 ≥ 7      （切片输出只有 6 → 必须由 RIP 补齐）
bitsPerSample    必须 == 8
planarConfig     必须 contiguous（禁 planar）
存储             使用 TIFFReadScanline 逐行读 → 【禁 tiled】
逻辑通道顺序     C, M, Y, K, White, Support, Varnish（前 7 个）
尺寸             同一作业所有层宽高必须完全一致
```

### 1.3 打印软件如何解释像素值（**不对称，重点**）

```text
逻辑通道 0..3（C/M/Y/K）：按阈值映射为墨滴数
    0 → 0 滴 ； 1..119 → 1 滴 ； 120..199 → 2 滴 ； 200..255 → 3 滴

逻辑通道 4..6（White/Support/Varnish）：【取值直接作为墨滴数】，无上界检查
```

### 1.4 墨滴上限按"通道 × 位深"变化（**易错**）

上限 = 该逻辑通道映射到的物理喷头数 × 每喷头最大墨滴数，其中每喷头最大 = `grayBits==2 ? 3 : 1`。

| 逻辑通道 | 对应物理喷头 | 喷头数 | **grayBits=2（默认）上限** | grayBits=1 上限 |
|---|---|---:|---:|---:|
| Cyan / Magenta / Yellow / Black | 各 1 个 | 1 | **3** | 1 |
| **White** | White1, White2 | 2 | **6** ⚠️ | 2 |
| Support | Support1/2/3 | 3 | **9** | 3 |
| Varnish | Varnish1/2/3 | 3 | **9** | 3 |

⚠️ **White 的上限是 6，不是 9。** 若输出 7/8/9，会被下游**静默钳位到 6**，不报错，只是墨量与设计不符。

### 1.5 物理通道顺序（已冻结）

```text
Cyan, Magenta, Yellow, Black, White1, White2,
Support1, Support2, Support3, Varnish1, Varnish2, Varnish3   （共 12 个）
```

### 1.6 材料混合（RIP 需知晓，但不需实现）

打印软件通道化时，逻辑 Support 通道的墨滴会按 **6 : 1** 分配到 `{Support1,2,3}` 与 `{Varnish1,2,3}`——即**约 1/7 的支撑墨滴被路由到光油通道**。该行为默认开启，由打印软件配置控制。

---

## 2. 六个待确认问题

### Q1【最高优先·阻塞编码】W/S/V 的"二值 → 墨滴数"量化，是否由 RIP 承担？

**背景**：切片侧输出的 W/S/V 是二值（`0`=出墨 / `255`=空）；打印软件期望的是 `0..上限` 的墨滴数（§1.3/§1.4）。二者之间缺一步**量化**。

**若直接透传会发生什么**（两个方向都错）：

| 切片输出 | 切片语义 | 被下游解释为 | 结果 |
|---|---|---|---|
| `0` | 出墨 | 0 滴 | 🔴 **白墨完全不出** |
| `255` | 不出墨 | 255 滴 → 钳到上限 | 🔴 **变成满墨** |

即：**语义完全反转，且不会有任何报错**——打出来相当于一张"底片"。

**三个候选方案**：

| 方案 | 承担方 | 评价 |
|---|---|---|
| **A（我方推荐）** | **RIP 量化** | RIP 本就是"连续调 → 设备墨量"的转换器，语义最合理；切片与打印软件零改动 |
| B | 打印软件插一层 `DropQuantizer` | 可行，但增加宿主职责，且多一个接缝要校验 |
| C | 切片侧输出覆盖量 | ❌ 违反 `p0.rgbwsv.2` 冻结协议，本轮不考虑 |

**需要答复**：
1. 是否由 RIP 承担量化？（是 / 否）
2. 若是：量化档位如何配置？（如白墨固定 6 滴、支撑 9 滴、光油 3 滴？还是由材料 Profile 曲线决定？）
3. 是否能保证输出严格落在 §1.4 的**按通道上限**内（尤其 White ≤ 6）？

**若答"否"**：请说明原因，我方将升级为产品决策（改由打印软件插量化层）。

---

### Q2【高优先】固定六通道内，白区信号如何避免与真实黑色及物理材料冲突？

**背景**：目前了解到的做法是——RIP 把普通 RGB 纹理区域当作纹理，把**白色区域的 `W/S/V = 0/0/0`** 当作私有识别信号。

**问题所在**：在 `black_is_print` 极性下，`W/S/V = 0/0/0` 的**物理含义是"白墨、支撑、光油三个通道同时出墨"**。这是一个合法的材料组合值，不是保留值。所以该信号：

- 不属于 `p0.rgbwsv.2` 契约的一部分（是隐式约定）；
- 一旦切片侧在某个合法场景写出该组合，RIP 会误判为白区，且**双方都不会报错**。

**另一个曾被提出的方案仍被否决**：改用
`R/G/B/W/S/V = 0/0/0/255/255/255` 作为白区信号。完整审计表明，早期“5 份配置”只是抽样下限：

```text
109 份配置中，59 份明确允许普通模型像素产生该值；
32 份启用纹理的配置使用有效黑色 fallback；
30 个场景入口中 23 个引用上述配置；
39 张现有贴图中 15 张包含可见纯黑像素。
```

在当前协议下，该值的正常含义是“RGB 纯黑打印、W/S/V 不打印”。删除或替换配置只能减少样例，
不能阻止 OBJ 贴图、MTL、3MF ColorGroup/Texture2D 或未来用户输入产生合法纯黑。详细清单与复现口径见
`DOC_ANALYSIS_14_Q2_RIP白区带内信号与配置冲突审查.md`。

**本轮约束修订**：当前阶段不新增逐层 1-bit sidecar。现有 RIP 基本流程已经完成，新增文件会要求
RIP 重新实现输入、缓存、校验和联调。本轮保持单 TIFF 六通道，优先确认以下两条路径：

| 路径 | 说明 | 当前判断 |
|---|---|---|
| **D 真实材料语义（新增·首选）** | 不透明白 `255/255/255/0/255/255`（W 打底）；透明白 `255/255/255/255/255/0`（仅光油）。**零哨兵、零碰撞、零协议改动** | ✅ **推荐首选** |
| A 兼容既有 `WSV=000` | 作为版本化 `rip_bound_intermediate` 私有合同保留；RIP 须在物理量化前拦截，S/V 不得泄漏 | ✅ 可接受（`WSV=000` 已证不可达）|
| B 全 255 表示白 | 白区写 `255/255/255/255/255/255` | ❌ **NO-GO——与背景字节完全相同**，且触发材料闭环缺陷 |
| C RGB 黑哨兵 | 继续使用 `0/0/0/255/255/255` | ❌ **NO-GO**，除非全输入源转义真实黑色 |
| E 纹理区标记 `WSV=000`+RGB 载色 | 白 `255/255/255/0/0/0`、黑 `0/0/0/0/0/0` | ⚠️ 可行但三处代价：S/V 泄漏风险、占用 W 真实语义、需 composer 主动写出当前不可达值 |

**路径 D 为何最优（详见 `DOC_ANALYSIS_14_Q2` §2.2.3）**：

```text
"白"在 RGBWSV 里本来就有物理正解，区别只在【白由什么材料构成】：
  不透明白 = 无彩色墨 + 白墨打底        → 255/255/255/0/255/255
  透明白   = 无彩色墨、无白墨 + 仅光油  → 255/255/255/255/255/0
两者互不相同、不与纯黑纹理碰撞、不与背景碰撞，且每个字节都是【真实材料指令】，
不存在"需要被下游剥离的假值"——这正是哨兵方案的根本风险来源。
```

若切片侧能确定材料构成，直接写 W=0 或 V=0，RIP 无需猜测；若无法确定，统一按不透明白写 `255/255/255/0/255/255`，再由 RIP Profile 决定是否抑制 W 转为透明——**两种做法都不需要哨兵，也不需要 sidecar**。

**Stage 15 项目内验证证据（2026-08-04）**：显式不透明白 Profile 的 W 载体路径已经通过
自动 Gate，但尚未取得外部 RIP 和实物工艺证据。

```text
F-01：真实小马物语模型切片 PASS；纯白载体/W 打印像素均为 150581；
F-04：像素差异仅发生在 W(idx 3)，R/G/B/S/V 差异为 0；
F-02：无严格纯白输入时新旧生产 TIFF 逐层 SHA-256 等价；
项目内 strict Reader：PASS；既有 Golden 28/28 不变；Quick CI：PASS；
Stage 15 未修改本仓库 RIP 源码。
```

这组证据支持路径 D 中的**显式不透明白**分支：切片侧可直接输出
`255/255/255/0/255/255`，项目内 Reader 按普通 W 通道读取即可。请 RIP 侧继续确认目标实现
是否也把它作为普通白墨材料处理。该证据不回答“同一全 RGB Package 能否由 RIP Profile
切换透明/不透明白”，因此原 Q2/Q3 和 12G-TCWS 继续保留。

**🆕 支持路径 A 的新证据（2026-08-03 代码级核实）**：`W/S/V = 0/0/0` 在当前 composer 中**结构性不可达**——

```text
S 通道（base+4）全仓库仅三处写入：
  slicer.cpp:2436  legacy 模型路径 → 显式写 background.value = 255
  slicer.cpp:2839  role=Support   → 写 0，但该 switch case 只写 S 就 return
  slicer.cpp:3125  support 分支   → 位于 else-if 链第三段，只对【非模型像素】生效
且 policy 路径 write_material_pixel()（:2798-2802）写 base+0,1,2,3,5，【完全跳过 base+4】。

⇒ 模型像素要 S=0 只能走 role=Support，而该路径下 W、V 必为 255；
  policy 路径可得 W=0、V=0，但 S 必为 255；
  非模型像素可得 S=0，但 W 必为 255。
⇒ 三者不可能在同一像素同时为 0。
```

即：**既有 RIP 白区信号是"由构造保证安全"，不是碰巧没撞上。** 这与候选值 `0/0/0/255/255/255` 恰好相反（后者是高频合法内容）。

因此我方建议：**保留既有 `WSV=000`，不改协议字节布局**，只补两条保护把隐式约定变成受保护的显式合同：

```text
保护一 Writer 断言：写出前扫描，若出现 W==0 && S==0 && V==0 → fail-closed
        报 PM-SLICER-CONTRACT-0060，不允许发布该包
        （可与既有通道统计扫描合并，零额外遍历开销）
保护二 manifest 显式声明：新增只读字段（不改通道布局、不改像素语义）
        "ripBoundIntermediate": { "whiteRegionSentinel": "WSV=000",
                                  "contractId": "rip_bound_intermediate.1",
                                  "guaranteedUnreachableByComposer": true }
```

完整推导见 `DOC_ANALYSIS_14_Q2_RIP白区带内信号与配置冲突审查.md` §2.1。

**需要答复**：
1. 当前 RIP 实际识别的完整六通道白区字节值是什么？
2. `WSV=000` 的识别发生在 W/S/V 墨滴量化之前还是之后？S=0、V=0 是否可能进入真实支撑/光油？
3. 两个 RIP Profile 对同一白区像素的逐通道输出映射分别是什么？
4. 是否接受 `W=0, S=255, V=255` 作为白区材料信号，由 Profile 决定使用或抑制 W？
5. 若坚持 RGB 黑哨兵，是否接受所有真实纯黑统一转义、manifest 版本化和未知版本 fail-closed？

---

### Q3【高优先·产品决策】白色的"语义类型"由谁定义、如何传递？

**背景**：表达"这里出白墨"其实已有正解——`W=0`（白墨通道打印）。但据了解，**同一份全 RGB 六通道包，RIP 可以生成"透明白（transparent knockout）"或"不透明白（opaque white）"两种不同结果**。

也就是说白色存在一个**语义类型维度**，而 `p0.rgbwsv.2` 里目前没有地方表达它。

**三条可能的路**：

| 方案 | 做法 | 适用前提 |
|---|---|---|
| ① 由 Profile 决定 | 整个作业统一一种白（RIP 按 Profile 走），切片侧不管 | **同一层内不需要混用两种白** |
| ② 由 manifest 声明 | 切片侧在包级声明本作业的白色类型 | 同上，但归属更清晰 |
| ③ 逐像素区分 | 当前六通道内需保留/转义一个语义值；否则留待后续协议扩展 | **同一层内需要混用两种白** |

**需要答复（此项主要请产品/工艺确认）**：
1. 实际工艺上，**同一层内是否需要同时存在"不透明白"和"透明白"**？（需要 / 不需要）
2. 若不需要 → 采用 ① 或 ②？由谁持有该设置？
3. 若需要 → 请说明区分规则（按材质名？按区域？按纹理内容？）。本轮不新增 sidecar；无法在固定六通道内无歧义表达时，转入独立协议决策。

---

### Q4【中】RIP 是否支持 PackBits 压缩输入？

**背景**：切片侧已实现可选 TIFF 压缩（`output.tiffCompression.algorithm` = `none` | `packbits`），**默认 `none`**，manifest 中声明 `tiff.compression`，严格 Reader 校验一致性。当前判定为 `NO_GO_DEFAULT`，原因就是目标 RIP 是否支持未确认。

**收益**：若支持，可显著降低包体积与磁盘 I/O（层数多时明显）。

**需要答复**：
1. RIP 读取 TIFF 时是否支持 `Compression = PackBits (32773)`？（支持 / 不支持）
2. 若支持，是否有性能顾虑（解压开销 vs I/O 节省）？
3. 是否希望默认开启，还是保持默认不压缩、按需开启？

---

### Q5【中】`grayBits` 的请求路径请固化

**背景**：`grayBits`（1 或 2）决定 §1.4 的墨滴上限，因此 **RIP 必须知道它**。但打印软件侧的接口文档中，请求 JSON 示例里没有出现该字段，只在 `profileKeys` 列表中提到 `device.grayBits`。

**需要答复（此项由打印侧与 RIP 侧共同约定即可，不需产品拍板）**：
1. `grayBits` 通过哪个字段路径传给 RIP？（建议 `profile.device.grayBits`）
2. 若请求中未提供该字段，RIP 的行为是？（建议：**fail-closed 报错**，不要默认猜 2）

---

### Q6【中】RIP 输出能否回写 `dropRange`，并保证取值在按通道上限内？

**背景**：打印软件侧会在 RIP 输出后加一道校验器（S2 校验），其中一条判据是"抽样检查 W/S/V 取值是否在合法范围内"。若 RIP 能在输出 manifest 中声明实际使用的 `dropRange`，校验会更可靠且可自解释。

**需要答复**：
1. RIP 输出的 manifest 中能否包含 `dropRange: { min, max }`（或按通道给出上限）？
2. 能否保证输出的 W/S/V 严格 ≤ §1.4 的按通道上限？
3. RIP 输出的组织形式：每层一个多通道 TIFF（推荐，命名如 `rip_%06d.tif`），还是每层每通道一个文件？（**两种模式不可混用**）

---

## 3. 各问题的影响范围

| 问题 | 阻塞什么 | 不答的后果 |
|---|---|---|
| **Q1** | RIP 编码、打印软件 `RipService`、S2 校验 | 🔴 无法开工；若猜错方向，打出"底片" |
| **Q2** | 白区处理实现、协议是否需扩展 | 🔴 隐式耦合长期存在，随时可能静默出错 |
| **Q3** | 白色语义能力设计 | 🟡 可先按①推进，但后期若需混用需返工 |
| **Q4** | 03E-02 能否从 `NO_GO_DEFAULT` 转 GO | 🟡 不影响主链路，只影响体积与 I/O |
| **Q5** | S2 校验判据、RIP 钳位正确性 | 🟡 可能出现白墨量静默错误 |
| **Q6** | S2 校验器实现方式 | 🟡 校验只能靠抽样，可靠性下降 |

---

## 4. 第一轮回复（已于 2026-08-04 回填）

```text
【回复方】：___产品经理/架构_____  【日期】：20260804

Q1 W/S/V 墨滴量化
  1) 由 RIP 承担量化？        [✅] 是    [ ] 否
  2) 若是，档位配置方式：白墨 6 滴 / 支撑 9 滴 / 光油 9 滴
     （示例：白墨 6 滴 / 支撑 9 滴 / 光油 3 滴，或"由材料 Profile 曲线决定"）
  3) 能保证输出在按通道上限内（White ≤ 6）？  [ ✅] 是  [ ] 否
  4) 若选"否"的原因：________________________________________

Q2 白区识别
  1) 当前实际识别的完整 RGBWSV 字节值：_______________________
  2) 识别时序：[ ] W/S/V 量化前  [ ] W/S/V 量化后  [ ] 其他：____
  3) WSV=000 中 S/V 会进入真实支撑/光油？ [ ] 会  [ ] 不会
  4) 透明 Profile 的逐通道映射：______________________________
  5) 不透明 Profile 的逐通道映射：____________________________
  6) 接受 W=0、S/V=255，由 Profile 使用或抑制 W？ [ ] 接受 [ ] 不接受
  7) 若坚持 RGB 黑哨兵，接受真实黑色全链路转义与版本化？ [ ] 接受 [✅ ] 不接受
  8) 15阶段补充了全实体RGB配置中关于白色纹理部分处理，可基于这个阶段的相关文档进行回顾和处理

Q3 白色语义类型
  1) 同一层内需要混用不透明白与透明白？     [ ] 需要  [✅] 不需要
  2) 若不需要，采用：[ ] ① Profile 决定  [ ] ② manifest 声明
     该设置由谁持有：可以由1、2的不同文件中中部署相同字段，在字段中进行表示RIP阶段时不透明还是透明
  3) 若需要，区分规则：______________________________________

Q4 PackBits 压缩
  1) 支持 Compression=PackBits？   [✅] 支持  [ ] 不支持
  2) 性能顾虑：_优先使用libtiff库进行处理，并且采用压缩处理
  3) 建议默认：[✅] 默认开启  [ ] 默认不压缩、按需开启

Q5 grayBits 传递
  1) 字段路径：profile.device.grayBits，这个值时根据你的建议进行设置
  2) 缺失时行为：[ ✅] fail-closed 报错  [ ] 其他：______________

Q6 输出声明与组织
  1) manifest 可含 dropRange？     [ ] 可  [ ] 不可
  2) 保证 W/S/V ≤ 按通道上限？     [✅ ] 是  [ ] 否
  3) 输出组织：[ ] 每层一个多通道 TIFF  [ ] 每层每通道一个文件

【其他补充或反向提问】：
1. Q2中在今天针对`全实体RGB配置`中关于白色纹理部分做了重新定义，判断这些形式是否可以完成Q2问题的回复。
2. 回答中未回复的问题，可以按照你的建议值进行操作
____________________________________________________________
____________________________________________________________
```

---

## 4.1 第一轮回复评估（切片侧，2026-08-04）

### 4.1.1 逐题状态

| 题 | 状态 | 说明 |
|---|---|---|
| **Q1** | 🟡 **部分闭合** | 方案 A 已定（RIP 量化），但缺**极性映射方向**与 **grayBits=1 档位** → R1 / R2 |
| **Q2** | 🔴 **未闭合** | 2.1–2.6 未答。这些是关于贵方既有实现的**客观事实**，切片侧无法代答 → 已压缩为单选 R3 |
| **Q3** | 🟡 **部分闭合** | 3.1「同层不需混用」是关键解锁；但 3.2 的"两处部署相同字段"构成双真源 → R4 |
| **Q4** | 🟡 **部分闭合** | 支持 PackBits 已确认；"默认开启"与切片侧现状存在联动代价 → R5 |
| **Q5** | ✅ **已闭合** | 字段路径与 fail-closed 行为均已明确，无需追问 |
| **Q6** | ✅ **由我方代答闭合** | 见 §4.3 |

### 4.1.2 本轮回复已解锁的事项

```text
Q1=方案A     → 切片侧与打印软件【零改动】，Stage 14 的 14F 三方联调具备开工条件
Q2.7 不接受   → 路径 C（RGB 黑哨兵 + 全链路转义）【永久排除】，不再讨论
Q3.1 不需混用 → 12G-TCWS 可【保持冻结】，p0.rgbwsv.3 协议扩展【不需要】（最大解锁）
Q4 支持       → 03E-02 具备从 NO_GO_DEFAULT 转 GO 的条件（仍需 R5 确认落地节奏）
Q5            → S2 校验判据可据此编码
```

### 4.1.3 关于「Stage 15 是否已回答 Q2」（回应第一轮补充问题 1）

**结论：Stage 15 回答了切片侧的一半，回答不了 RIP 侧的另一半。**

| 维度 | Stage 15 是否回答 |
|---|---|
| 切片侧如何表达"需要打印的白" | ✅ 已回答：`255/255/255/0/255/255`，真实材料语义，零哨兵，有自动 Gate 证据 |
| 贵方既有 RIP 现在识别的是哪个字节值 | ❌ 无法回答 —— 这是贵方实现的事实 |
| 识别发生在 W/S/V 量化【之前还是之后】 | ❌ 无法回答 —— 决定 `S=0`/`V=0` 会否泄漏成真实支撑墨与光油 |
| 透明 / 不透明两个 Profile 的逐通道映射 | ❌ 无法回答 |
| 同一全 RGB Package 由 Profile 切换透明/不透明 | ❌ **明确不在 Stage 15 范围**（见 `REPORT_15` §8），那是 Q3 的问题域 |

因此 Q2 仍需贵方回复，但**问法可以大幅简化**——见 R3。

---

## 4.2 第二轮追问（请直接在本段作答）

> 只剩 6 项。🔴 = 阻塞编码，必须回复；🟡 = 不阻塞但影响成本，建议回复。

### 🔴 R1 · Q1 补充：请给出极性映射表

**为什么必须要**：本文 §1.3 已说明，打印软件对逻辑通道 4..6 是**取值直接作为墨滴数**。若 RIP 直接透传切片的二值语义，会出现 `0`(出墨)→0 滴、`255`(空)→钳到上限，**语义完全反转且两端都不报错**。第一轮给出了档位（白 6 / 支 9 / 光 9），但未说明 `0` 与 `255` 各自映射到哪个值。

````text
请填写（grayBits=2 情形）：

  W = 0   （切片语义：出墨）→ ______ 滴        W = 255 （切片语义：空）→ ______ 滴
  S = 0   （切片语义：出墨）→ ______ 滴        S = 255 （切片语义：空）→ ______ 滴
  V = 0   （切片语义：出墨）→ ______ 滴        V = 255 （切片语义：空）→ ______ 滴

以下是在打印软件中关于RIP的定义，是否可以解决你的问题
### 8.2.1 🔴 墨滴上限按通道 × 按位深变化（v1.1 重要更正）

**v1.0 曾笼统写作「W/S/V 统一 0–9」，这是错的。** 核实 `ApplyTiffDropsToBuffers()`（`ChannelSplitter.cpp:648-703`）后的实际逻辑：

```cpp
maxDropPerHead = (ClampGrayBits(grayBits) == 2) ? 3 : 1;   // ClampGrayBits(g) = (g==1 ? 1 : 2)
totalDrops     = std::min(totalDrops, targetCount * maxDropPerHead);
```

`targetCount` = 该逻辑通道映射到的**物理通道数**（`MakeDefaultTiffRoutes()`, `:550-564`）：

| 逻辑通道    | 物理通道       | 数量 | **grayBits=2 上限（默认）** | grayBits=1 上限 |
| ----------- | -------------- | ---: | --------------------------: | --------------: |
| 0–3 C/M/Y/K | 各 1 个        |    1 |                       **3** |               1 |
| **4 White** | White1, White2 |    2 |                     **6** ⚠️ |               2 |
| 5 Support   | Support1/2/3   |    3 |                       **9** |               3 |
| 6 Varnish   | Varnish1/2/3   |    3 |                       **9** |               3 |

**关键后果**：

- **White 的上限是 6，不是 9。** 在默认 2-bit 模式下，RIP 若输出白墨 7/8/9，会被**静默钳位到 6**，不报错，只是墨量与设计不符
- `grayBits = 1` 时全部上限降为 1/2/3/3
- 因此 RIP **必须从请求中读取 `grayBits`**（`profileKeys` 需含 `device.grayBits`），并自行钳位到正确上限，不要依赖下游静默钳位
````

### 🔴 R2 · Q1 补充：grayBits=1 时的档位

**为什么必须要**：第一轮的「白 6 / 支 9 / 光 9」是 grayBits=2 的上限值。**在 grayBits=1 下这三项全部超限**（见 §1.4）。Q5 已确认 RIP 能通过 `profile.device.grayBits` 拿到该值，因此档位应当是它的函数。

| 逻辑通道 | grayBits=2（第一轮已答） | **grayBits=1（待答）** | grayBits=1 物理上限 |
|---|---:|---:|---:|
| White | 6 | 2 | **2** |
| Support | 9 |                      3 | **3** |
| Varnish | 9 | 3 | **3** |

### 🔴 R3 · Q2 收敛为单选：`WSV=000` 保留还是废弃？

**这一问替代原 Q2.1 ~ Q2.6。** 请先做这个选择，再决定是否需要回答后续细节。

```text
[✅] 路径 D —— 废弃 WSV=000 私有信号，统一改用 W=0 真实材料语义

    → 原 Q2.1 ~ Q2.6 全部作废，Q2 当场闭合。
      只需再确认一句：
      RIP 是否把 255/255/255/0/255/255 当作【普通白墨材料】处理，
      无需任何特殊识别逻辑？        [✅] 是    [ ] 否，原因：____________

[ ] 路径 A —— 保留既有 WSV=000 私有信号

    → 需回答原 §2 中的 Q2.1 ~ Q2.6（六个小问）。
      同时我方将补两条保护，把隐式约定变为受保护的显式合同：
        · Writer 断言：写出前扫描，出现 W==0 && S==0 && V==0 即 fail-closed
        · manifest 增加只读字段 ripBoundIntermediate（不改通道布局与像素语义）
      是否接受这两条保护？          [ ] 接受  [ ] 不接受，原因：__________
```

> 参考：`W/S/V = 0/0/0` 在当前 composer 中**结构性不可达**（§2 Q2 已给代码级证据），
> 因此路径 A 是安全的；路径 D 则连"需要被下游剥离的假值"都不存在。两条都可行，请贵方按实现成本选择。

### 🟡 R4 · Q3 补充：两处部署同一字段会造成双真源

第一轮答复为「可以由 ①②的不同文件中部署相同字段」。同一信息存在两个来源，**长期必然出现不一致**，必须先定权威与冲突处置。

```text
1) 权威源：
   [✅] manifest 为权威，Profile 仅提供默认值      ← 我方建议
   [ ] Profile 为权威，manifest 仅作记录
   [ ] 其他：__________________________________

2) 两处取值不一致时：
   [✅] fail-closed 报错，不猜测                    ← 我方建议
   [ ] 以权威源为准并告警
   [ ] 其他：__________________________________

3) 字段命名建议 whiteSemantics: "opaque" | "transparent"
   是否可接受？ [✅] 可接受   [ ] 建议改为：______________________
```

### 🟡 R5 · Q4 补充：「默认开启压缩」的联动代价确认

第一轮答复为「优先 libtiff + 默认开启压缩」。切片侧现状与之存在落差：

```text
· output.tiffCompression 当前默认 none；
· 03D 决策为 GO_OPTIONAL —— LibTIFF 是【可选】后端，默认仍是手写 writer；
· 改为默认开启将使【全部既有 golden TIFF 的 SHA-256 失效】，需重新固化基线，
  且很可能需同期把 LibTIFF 切为默认后端。
```

```text
1) 能否接受分两步：首版仍「默认不压缩、按需开启」，「默认开启」作为独立决策后续推进？
   [✅] 可以接受                    ← 我方建议
   [ ] 不可以，需首版即默认开启

2) 若需首版即默认开启，是否接受我方同期重新固化 golden 基线
   （约需一个独立验收轮次）？      [✅] 接受   [ ] 不接受
```

### 🟡 R6 · Q1 补充：与 6:1 材料混合的交互

§1.6 规定逻辑 Support 墨滴按 **6 : 1** 分流至 `{Support1,2,3}` 与 `{Varnish1,2,3}`。若 Support 量化到 9（满值）**且** Varnish 亦量化到 9（满值），分流进入光油通道的支撑墨滴与光油自身墨滴如何处理？

```text
1) 二者是否叠加？
   [ ] 叠加   [ ] 不叠加（光油通道只取其一）   [✅] 由打印软件配置决定   [ ] 其他：______

2) 若叠加且超过物理上限 9，行为是？
   [✅] 钳位到 9   [ ] 报错   [ ] 未定义/需进一步确认
```

---

## 4.3 我方代答项（如无异议，无需回复）

第一轮补充说明「未回复的问题可按建议值处理」。据此我方将 Q6 按以下取值定稿：

| 项 | 取值 | 依据 |
|---|---|---|
| Q6.1 manifest 含 `dropRange` | **可** | 采纳建议值 |
| Q6.3 输出组织 | **每层一个多通道 TIFF**（命名 `rip_%06d.tif`） | **非偏好，而是硬约束** |

> ⚠️ Q6.3 需特别说明：这不是"采纳建议"，而是被 S3 唯一确定。打印软件 `ChannelSplitter`
> 要求 `samplesPerPixel ≥ 7` 且使用 `TIFFReadScanline` **逐行**读取（禁 tiled）——
> 「每层每通道一个文件」的形式**根本无法通过该校验**。除非贵方对此有异议，本项就此定稿。

**如对以上两项有异议，请在此注明**：`________________________________________`

---

## 4.4 第二轮回复评估与结项（切片侧，2026-08-04）

### 4.4.1 逐项判定

| 项 | 判定 | 结论 |
|---|---|---|
| **R1** 极性映射表 | ⚠️ **重分类，非未答** | 见 §4.4.3 |
| **R2** grayBits=1 档位 | ✅ 闭合 | 白 2 / 支 3 / 光 3 —— 与物理上限精确一致 |
| **R3** 路径选择 | ✅ **闭合（本轮最关键）** | **选路径 D**，并确认 `255/255/255/0/255/255` 按普通白墨材料处理 |
| **R4** 双真源 | ✅ 闭合 | manifest 权威 / 不一致 fail-closed / `whiteSemantics` 字段名可接受 |
| **R5** 压缩节奏 | ✅ 闭合 | 两项均勾选＝两种节奏都可接受；我方取「分两步」（风险较低） |
| **R6** 6:1 混合 | ✅ 闭合 | 由打印软件配置决定；超限钳位到 9 |

### 4.4.2 六问最终状态

```text
Q1  ✅ 闭合（档位 + 上限；映射方向转双边，见 §4.4.3）
Q2  ✅ 闭合 —— 路径 D 定案，原 Q2.1~Q2.6 全部作废
Q3  ✅ 闭合 —— 同层不混用；manifest 权威 + whiteSemantics 字段
Q4  ✅ 闭合 —— 支持 PackBits；落地分两步
Q5  ✅ 闭合
Q6  ✅ 闭合 —— 我方代答无异议
```

### 4.4.3 R1 重分类：转 RIP ↔ 打印软件双边事项

对方指出「切片软件似乎不需要知道具体信息」——**该判断成立**：

```text
· 切片输出的 W/S/V 是二值字节（0 / 255），由 p0.rgbwsv.2 冻结，不因映射方向改变；
· 量化由 RIP 承担（Q1 方案 A）；
· 消费方是打印软件的 ChannelSplitter 与 S2 校验器，均不在切片仓库内。
⇒ 切片侧既不持有也不需要该信息，R1 不构成切片侧的未答项。
```

但该问题**必须有人认领** —— 它是链路上唯一「搞错也不报错」的失效模式。故不删除，
改记入合同定案文档 §3.1，附我方代拟值，由 RIP 侧与打印软件侧双边确认后回填。

**不阻塞切片侧任何工作；阻塞 RIP 实现与 S2 校验器编码。**

### 4.4.4 结项动作

| 动作 | 产物 |
|---|---|
| 条款收敛 | 新建 `DOC_DECISION_14_S2_RIP接口合同定案.md`（权威） |
| 作废清理 | `DOC_ANALYSIS_14_Q2` 降级为推导档案；§2.1.4 两条保护标记禁止实现 |
| 新工作项 | **N1** manifest 新增 `whiteSemantics`（此前无任务卡覆盖）|
| 状态变更 | **N2** 03E-02 由 `NO_GO_DEFAULT` → **GO（按需开启）** |

---

## 5. 我方承诺（便于对方评估工作量）

```text
· S1（p0.rgbwsv.2）协议保持冻结，不会在本轮变更通道数、顺序、位深或极性；
· manifest 中会诚实声明 storage / rowsPerStrip / tiled / polarity / printValue / emptyValue，
  RIP 可直接读取，无需假设；
· 当前阶段保持单 TIFF 六通道，不新增逐层 sidecar 文件；
· 未经双方确认，不会把合法纯黑 `0/0/0/255/255/255` 静默改成白区哨兵；
· 我方现有严格包校验器（23 个错误码 + 逐层 6 通道 checksum）可提供给 RIP 侧做输入自检；
· 若 Q3 需要协议扩展，我方按独立决策流程评估 p0.rgbwsv.3，不在本轮擅自变更。
```

---

## 6. 修订记录

| 日期 | 版本 | 变更 |
|---|---|---|
| 2026-08-03 | v1.0 | 首版。自包含背景（S1 协议、S3 硬约束、值解释不对称、按通道墨滴上限、物理通道序、材料混合）+ 六问 + 影响范围 + 可填写回复模板 + 我方承诺 |
| 2026-08-03 | v1.1 | 深化 Q2：按当前阶段约束撤回 per-layer sidecar 推荐；将早期 5 份配置抽样扩展为 109 份配置、30 个场景和 39 张贴图的完整审计；明确 59 份直接冲突配置、32 份黑 fallback 配置及真实纯黑输入风险；改为确认既有 WSV=000 拦截证据与 W-only Profile 路径 |
| 2026-08-04 | v1.2 | 回填 Stage 15 显式不透明白 W 载体的项目内自动 Gate 证据；保留目标 RIP、实物 G7 和同包透明/不透明复用确认项 |
| 2026-08-04 | v1.4 | **两轮问答全部闭合，本文转为往来记录档案。** 回填第二轮回复并评估（§4.4）：R2/R3/R4/R5/R6 闭合，R1 重分类为 RIP↔打印软件双边事项（切片侧不持有该信息）；Q2 由路径 D 定案，原 Q2.1~Q2.6 作废；权威条款收敛至 `DOC_DECISION_14_S2_RIP接口合同定案.md`；产生新工作项 N1（manifest `whiteSemantics`）与 N2（03E-02 转 GO） |
| 2026-08-04 | v1.3 | 回填第一轮回复并评估：Q5 闭合、Q6 由我方代答闭合、Q1/Q3/Q4 部分闭合、Q2 未闭合；新增 §4.1 逐题状态与已解锁事项（Q3.1 使 12G-TCWS 保持冻结、p0.rgbwsv.3 不需要；Q2.7 永久排除路径 C）；明确 Stage 15 只回答切片侧、无法代答 RIP 侧事实；新增 §4.2 第二轮追问 6 项（R1 极性映射表、R2 grayBits=1 档位、R3 将 Q2.1~2.6 收敛为 D/A 单选、R4 双真源权威、R5 默认压缩联动代价、R6 6:1 混合交互）；新增 §4.3 我方代答项并说明 Q6.3 为 S3 硬约束唯一确定 |
