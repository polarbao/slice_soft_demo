# DOC PREP 13D 工作台布局收口准备

> 文档状态：PREPARED / 13D-01 READY
> 版本：v1.0
> 日期：2026-07-28

## 1. 开发前置

13D 的需求、目标信息架构、组件边界和验证视口已经明确，但不得在 13C-05 前开始代码迁移：

```text
13B-08 冻结顶部 SceneActionBar 和场景作业状态；
13C-05 冻结“生产预览/诊断预览”的最终一级入口；
现有 UI self-test 与 Smoke 全部通过；
截取默认、三模型、配置、生产预览和诊断 Dock 五类基线截图。
```

## 2. 迁移清单

```text
建立 JobActionBar 顶层容器；
建立 ContextInspector 并包装现有 panel；
迁移模型列表/变换/排版/预检，不重写 controller；
把工艺对比并入 DiagnosticsDock；
把路径和兼容入口移入 ProjectToolsDock；
版本化保存/恢复布局；
删除确认无引用的重复壳层；
完成四视口截图和 UI Smoke。
```

## 3. 开发 Gate

在 13D-01 开始前必须再次核对：

```text
13B-08 最终组件名称；
13C-05 PreviewWorkspace 最终一级页；
MainWindow 当前工作树无跨任务未提交 UI 修改；
布局迁移不触碰 slicer_core；
所有旧入口在迁移矩阵中有目标位置。
```

如果任何入口没有目标位置，停止删除，只允许保留在“项目与高级工具”。

## 4. 验证矩阵

| 维度 | 必测 |
|---|---|
| 视口 | 1280x720、1440x900、1920x1080 |
| 缩放 | 100%、150% |
| 场景 | 空、单模型、三模型、22 实例摘要 |
| 状态 | idle、blocked、slicing、completed、failed |
| 恢复 | 新安装、合法状态、旧版本状态、损坏状态 |
| 能力 | 模型编辑、切片、预览、报告、材料闭环、曲线、对比、日志 |

## 5. 准备结论

13D 已具备总体准备，不缺 PRD/DEV/DEMO/TASKS。13C-05 已于 2026-07-28 完成并冻结
“生产预览/诊断预览”一级入口，因此 `13D-01 READY FOR DEVELOPMENT`。13D-02..04 继续按前一任务
通过后逐项解锁，不得跨任务抢先修改 MainWindow 布局。

2026-07-29 已补齐四个原子准备入口：

```text
DOC_PREP_13D_01_顶部作业栏准备.md；
DOC_PREP_13D_02_单一ContextInspector准备.md；
DOC_PREP_13D_03_项目区与诊断Dock准备.md；
DOC_PREP_13D_04_响应式与阶段收口准备.md。
```

四个任务的输入、迁移映射、禁止边界和 Smoke 验收均已明确；后续准备工作完整，但执行状态仍严格
按 `13D-01 -> 13D-02 -> 13D-03 -> 13D-04` 解锁。
