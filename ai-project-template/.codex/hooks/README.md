# Codex Hooks

Hooks are disabled by default in `.codex/config.toml`:

```toml
[features]
hooks = false
```

Enable hooks only after reviewing exact script scope, failure behavior, trust
flow, and rollback path.
