# 12E-08C-R2-03 边界环修复完成

> 日期：2026-07-20
> 状态：R2-03 COMPLETE / R2-04 READY

## 已完成

新增默认关闭的 simple boundary-loop repair。只有 incidence=1、每点 degree=2 且一入一出、简单、平面、
严格凸、全预算内、完整相交证据可用的闭环才进入候选。新面只允许
`inherit_uniform_material_no_uv`，并通过 `generatedTriangleMappings[]` 与 source mapping 分离。

候选填补后重新检查 boundary 精确减少、组件不变，以及 degenerate/non-manifold/duplicate/winding/
self-intersection 不恶化。任一失败丢弃整个 R2-03 candidate。

## 证据

generated 缺顶 box 补 2 面后 strict PASS；non-planar、budget、UV、branching fixture 稳定 blocked。
四个 required case 双运行稳定：两个有 boundary 的 OBJ 因 sampled self-intersection evidence 拒绝填面；
`meigui_fudiao` 和闭合 3MF 无 boundary，分别保持 manual/no-op strict PASS。

## 固定边界

repair 默认关闭；OpenVDB optional/OFF；legacy 不调用 repair；不写生产 package/TIFF；
`p0.rgbwsv.2`、RGBWSV、uint8、`black_is_print` 不变。R2-03 不是通用 remesher。

## 下一任务

`12E-08C-R2-04 Post-Repair Strict 与 Attribute Guard`。准备文档：

`docs/slice/DOC/DOC_PREP_12E_08C_R2_04_PostStrictAttributeGuard准备.md`

R3 与 12E-08D 继续 BLOCKED。
