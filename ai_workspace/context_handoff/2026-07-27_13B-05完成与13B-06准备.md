# 13B-05 完成与 13B-06 准备交接

> 日期：2026-07-27
> 分支：`feature/12e-08c-mesh-repair`
> 状态：13B-05 FIXTURE COMPLETE / 13B-06 READY FOR FIXTURE DEVELOPMENT

## 已完成

```text
13B-04A 已实现全部可见模型统一 +Z 纹理俯视和追加后的 11x2 自动排版；
SceneRasterTypes 建立共享 Grid、实例 Raster、层、错误和统计合同；
Legacy adapter 复用 run_slicer callback，不写 package，并应用已准入 ModelInstance transform；
Legacy 校验配置 modelPath 与 sourceTransformIdentity；
Global adapter 包装 writer-ready layer，并把畸形层转换为稳定错误；
SceneCollision admission 保留独立 transform revision/hash；
Orchestrator 拒绝 stale、重复 identity、混合引擎、非整数 offset、负 Z 和 extent 溢出；
SceneLayerComposer 以 Model > OuterVarnish > Support > Empty 进行联合层合成；
V-only model 和 model-owned V/outer V 已分离；
13B-05 不写 TIFF、manifest、report 或 package。
```

## 验证

```text
6/6 定向 CTest PASS；
Debug 全量构建 PASS；
scripts/run_ci_quick.ps1 PASS；
Legacy adapter package 目录快照无变化；
代码复审 P1 已修复，P2 资源异常和 adapter/final closure 分层作为后续残余。
```

## 固定边界

```text
p0.rgbwsv.2 / R G B W S V / uint8 / black_is_print 不变；
Legacy 默认；
Global/OpenVDB 显式 opt-in 且默认关闭；
同一场景只允许一种 effectivePipelineMode；
正式设备 buildVolume/origin/axes 未冻结，production GO 仍阻断；
docs/claude/ 是无关未跟踪目录，不读取、不提交。
```

## 下一步

先补齐 13B-06 独立 PREP/PROMPT，至少冻结共享 writer 输入、单 package 原子发布、manifest
兼容、scene/instance report、失败无伪成功 package 和 RIP strict 验收；准备通过后才开始代码。
