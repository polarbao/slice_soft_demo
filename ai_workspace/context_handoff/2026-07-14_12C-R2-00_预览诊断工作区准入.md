# 12C-R2-00 预览与诊断工作区准入交接

> 日期：2026-07-14
> 状态：COMPLETE / R2-01 READY

## 1. 准入事实

```text
12C-R0 fresh build 与 smoke 已通过；
12C-R1 Profile、设置、generated config、帮助元数据已完成；
R2 只修改 Qt UI，不需要 slicer_core API、依赖或协议变更；
当前可直接执行 R2-01 PreviewWorkspace。
```

## 2. 冻结共享层契约

```text
真实 layerIndex 是唯一共享标识；
生产层检查优先提供规范层范围；
overlay/raw preview 是稀疏数据；
目标层缺图时保持目标 layerIndex 并明确显示缺失；
禁止最近层、图片序号和跨层 RGB/W/S/V 兜底；
模式切换不得改变共享 layerIndex。
```

## 3. 验证夹具

`output/UiSmokeOverlayRgbwv` 同时包含连续生产 TIFF 和从后续层开始的 preview，可验证：

```text
有 preview 的同层同步；
生产层存在但 preview 缺失；
缺失时不跳到后续有图层；
三种模式切换保持共享层号。
```

## 4. 下一任务

```text
12C-R2-01 PreviewWorkspace 与共享层状态
```

实施细节与验证命令见 `docs/slice/DOC/DOC_CHECKLIST_12C_R2_预览诊断工作区准入.md`。
