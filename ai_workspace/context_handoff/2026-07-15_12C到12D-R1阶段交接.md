# 12C 到 12D-R1 阶段交接

> 日期：2026-07-15
> 状态：CURRENT / 12D-R1 READY

## 1. 已完成事实

```text
12C-R0/R1/R2 全部完成；
REPORT_12C_Qt工作台当前状态.md 已生成；
build-12c-ui-r2-final fresh configure/build 通过；
Run12CUiClosure.ps1 完整 UI Smoke 通过；
CTest 6/6 通过；
12D-R0 文档、schema、fixture matrix、任务和执行提示均已存在。
```

## 2. 12C 冻结边界

```text
Profile + SliceSettingsModel + generated effective config 链路保留；
PreviewWorkspace 使用真实 layerIndex，不允许跨层兜底；
ReportPanel/ChannelChartPanel/LogPanel 由 DiagnosticsDock 唯一承载；
12D-R3 的 UI 展示复用 ReportPanel/DiagnosticsDock；
不重写 12C 工作台结构。
```

## 3. 12D 安全边界

```text
TIFF inferred 只能 candidate/confidence，不得 production pass；
semantic masks exact 才能作为生产闭环证据；
repair 默认关闭；
第一批只允许显式 1px repair；
2px 及以上只报告；
外部背景保持 RGBWSV 全 255；
固定协议 p0.rgbwsv.2 / R G B W S V / uint8 / black_is_print 不变。
```

## 4. 下一原子任务

```text
12D-02 MaterialClosureConfig
```

只新增 `materialClosure` 配置段及解析/默认值/校验，默认 `enabled=true`、`mode=diagnostic`、`repair.enabled=false`。本任务不提前实现报告、gap detector 或 repair。
