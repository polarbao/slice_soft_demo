# 12E-08C-R3-01A 完整自相交证据完成

> 日期：2026-07-21
> 分支：`feature/12e-08c-mesh-repair`
> 状态：R3-01A COMPLETE / R3-02 READY / 12E-08D BLOCKED

## 1. 当前目标与约束

R3-01A 用确定性完整证据替代真实大网格的 sampled self-intersection 结论。该任务只做分析，不执行 repair，
不写生产 TIFF/package，不修改 legacy、Qt、OpenVDB 默认值或 RGBWSV 协议。

## 2. 已完成实现

```text
MeshCompleteSelfIntersectionAnalyzer：确定性 AABB BVH + 现有 narrow-phase；
共享顶点 pair 排除、候选排序/去重、pair SHA-256；
complete/confirmed/coplanar/touching/budget-resource blocked 状态；
显式 5000000 candidate 预算与峰值内存/耗时；
MeshRepairOptions/Result/report/hash/eligibility/preflight/evidence validator 接入；
mesh_repair_preflight 的 --analyze-r3-01a CLI；
unit/contract/preflight/CLI tests；
四 required case 双运行证据脚本。
```

## 3. 真实模型事实

```text
nai_you_new：236181 tested，8409 confirmed；
aishen_fudiao：491365 tested，19270 confirmed，20 coplanar；
meigui_fudiao：346104 tested，5592 confirmed；
Texture2D 3MF：8 tested，0 confirmed/coplanar/touching。
```

四 case 均 `complete=true`，无 budget blocked，双运行稳定。三个 OBJ 继续
`rejected_self_intersection`；闭合 3MF 为 `strict_pass_no_repair`。

## 4. 决策与被拒绝方案

```text
confirmed/coplanar 继续 fail-fast；
不通过提高旧 sampled cap 冒充完整证据；
不使用 OpenVDB 体素化替代 triangle/UV/material 语义；
不在 R3-02 发明通用自相交重建；
任务证据完成与 production Gate PASS 分离；
12E-08D 继续 BLOCKED。
```

## 5. 验证证据

```text
定向 CTest：5/5 PASS；
Debug 全量 build：PASS；
Debug 全量 CTest：36/36 PASS；
Qt --self-test：startup、experimental-report-summary PASS；
真实模型脚本：4/4 complete，4/4 repeatability PASS；
git diff --check：无 whitespace error，仅换行提示。
```

Quick CI 已运行但未通过：legacy golden `material_process_top2` 期望 `widthPx=48`、实际 `226`。R3-01A
不修改该模型的缩放、姿态或 writer；该项作为既有 baseline blocker 保留。

## 6. 主要文件

```text
src/slicer_core/geometry/repair/MeshCompleteSelfIntersectionAnalyzer.*
src/slicer_core/geometry/repair/MeshRepairPreflight.cpp
src/slicer_core/geometry/repair/MeshRepairTypes.h
src/slicer_core/diagnostics/MeshRepairReport.cpp
apps/mesh_repair_preflight/main.cpp
tests/unit/mesh_complete_self_intersection_analyzer/main.cpp
scripts/run_12e_08c_r3_01a_complete_self_intersection.ps1
docs/slice/DOC/DOC_EXEC_12E_08C_R3_01A_完整自相交证据结果.md
docs/slice/DOC/DOC_PREP_12E_08C_R3_02_真实模型RepairMatrix准备.md
```

## 7. 工作树状态

R3-01A 代码、测试、脚本和文档仍未提交。没有 `.specstory` 修改，没有生产输出应进入提交。

## 8. 下一任务

执行 `12E-08C-R3-02 真实模型 Repair Matrix`。按专用准备文档运行 strict-no-repair、保守 repair、evidence
validator 和 non-manifold classification lane；confirmed/coplanar case 不允许 mutation。R3-02 完成后再判断
R3-03 是否 READY。
