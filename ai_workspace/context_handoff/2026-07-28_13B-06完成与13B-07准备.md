# 2026-07-28 13B-06 完成与 13B-07 准备

## 当前状态

```text
分支：feature/12e-08c-mesh-repair
13B-04A：COMPLETE，多模型纹理 +Z 俯视和追加自动排版已提交
13B-05：FIXTURE COMPLETE，全局 Raster 与联合内存层已提交
13B-06：FIXTURE COMPLETE / PRODUCTION INPUT OPEN
下一任务：13B-07 真实模型矩阵与收口 PREPARATION
```

## 13B-06 已实现

```text
一个 scene 发布一个 package；
每个全局 layerIndex 一个 RGBWSV TIFF；
manifest 保持 p0.rgbwsv.2，只增加可选 scene/reports.scene；
新增 slicesoft.multimodel_scene_summary.13b.1；
新增 slicesoft.multimodel_scene_report.13b.1；
scene report 与 TIFF/manifest 同一 staging 事务发布；
fixture 使用 functional_fixture_admitted，productionReady=false；
旧单模型 writer 不提供 scene extension 时保持兼容；
scene/admission/transform/buildVolume/compose stats/实际像素闭环 fail-closed；
JSON write/flush/close 和持久化复读校验；
共享 writer backup 清理不再回滚已成功发布的新 package。
```

## 验证

```text
定向 CTest 4/4 PASS；
Debug 全量构建 PASS；
fixture RIP strict PASS：p0.rgbwsv.2、8 bit、RGBWSV、635x600、warnings=0；
Quick CI PASS，包含 UI self-test 和 overlay-load-real。
```

## 固定边界

```text
schema=p0.rgbwsv.2；
channelOrder=R G B W S V；
uint8 / black_is_print / 0 print / 255 empty；
Legacy 默认，Global/OpenVDB 显式 opt-in；
13B-06 不重新切片、不反读 TIFF、不做 Qt 一键多模型联合切片；
功能 fixture PASS 不等于正式设备 production GO。
```

## 下一步

13B-07 需要补齐独立 PREP/PROMPT 后再进入真实模型功能矩阵。功能开发不被外部输入阻断，但以下
输入继续阻断 Stage 13B production GO：

```text
设备 buildVolume；
设备原点和 X/Y 轴向；
22 实例正式性能预算/验收阈值。
```

`docs/claude/` 是用户未跟踪目录，本轮未读取、未修改、不得提交。
