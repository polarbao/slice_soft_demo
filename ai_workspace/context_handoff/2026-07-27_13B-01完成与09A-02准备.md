# Context Handoff

## Project

- 名称：SliceSoft UV 工业喷墨 3D 打印切片软件
- 仓库：`polarbao/slice_soft_demo`
- 分支：`feature/12e-08c-mesh-repair`
- 当前主线：13A-01、13B-01 COMPLETE；scene-aware 12E-09A-02 READY
- 技术栈：C++20、Qt 5.15 Widgets、CMake、Windows x64/MSVC、OpenVDB optional/OFF

## Confirmed facts

- A 级代码证据：`ModelTransform`、`ModelInstance`、`MultiModelScene`、`ResourceScope` 和
  `SceneEffectiveConfig` 已实现。
- 13B scene schema：`slicesoft.multimodel_scene.13b.1`。
- Scene Effective Config schema：`slicesoft.scene_effective_config.13b.1`。
- 场景 P0 材料规则：`scene_profile_only`；不同 Profile fail-closed。
- Build Volume 可表达 unresolved/fixture/device_profile；unresolved 允许 draft，禁止 production。
- 提交 `357bf6b` 已推送至远端当前分支。
- 实际验证：Debug 全量构建 PASS；CTest 60/60 PASS；Qt self-test PASS；Quick CI PASS。
- 09A-02 的 PRD/DEV/DEMO/PREP/PROMPT 已补齐，但代码尚未开始。

## Important constraints

- Qt 只能进入 `apps/slicer_debug_ui`；09A-02 core 不依赖 Qt。
- `p0.rgbwsv.2`、R G B W S V、uint8、black_is_print 不变。
- Legacy 保持默认；OpenVDB 默认关闭；禁止 silent fallback。
- 09A-02 不写 production package/TIFF，不覆盖 Profile、fixture、模型或 scene draft。
- `docs/claude/` 是无关未跟踪目录，不纳入本轮提交。

## Decisions made

- `effectiveTransform = derivedLayoutTransform * requestedTransform`。
- 等价变换按仿射矩阵容差验证，不以镜像字段表现形式作为唯一依据。
- scene/transform revision 必须位于 JSON 精确整数范围。
- 09A-02 同时支持 `single_model` 和 `scene`。
- scene 诊断必须绑定 current modelId/instanceId/sceneRevision/transformRevision。
- 09A-02 输出为
  `output/ui_sessions/<session>/slice_config.diagnostic.effective.json`，与 09B production config 和
  13B scene config 分离。

## Open questions

- 正式设备 buildVolume、原点和 X/Y 轴方向仍未提供；阻断 13B-04 production。
- 22 实例正式性能预算仍未提供；阻断 13B-07 production GO。
- 多实例不同 MaterialProcessProfile 的产品规则未确认；P0 固定 scene_profile_only。
- 09A-03..06、13A-02..05、13B-02..07、13C-01..05 尚未实现。

## Next actions

1. 用户授权后执行 `12E-09A-02 Diagnostic Effective Config`。
2. 先写 `diagnostic_effective_config_unit_tests` 并验证 RED。
3. 实现 single_model/scene identity、requested/derived/effective、hash、atomic write、stale/cancel。
4. 回归 `production_effective_config_unit_tests` 与 `multimodel_scene_contract_unit_tests`。
5. 生成 09A-02 状态报告并停止，不夹带 09A-03 Qt 控件。
