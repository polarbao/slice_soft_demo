# 12E-08C-R2-01 保守清理完成

> 日期：2026-07-20
> 状态：R2-01 COMPLETE / R2-02 READY

## 已完成

- 新增显式 `MeshRepairService`，只执行 degenerate 和同属性同向 exact duplicate cleanup。
- adapter 保留被过滤退化面的 source triangle id。
- report 新增确定性 `sourceMappings[]`，candidate/operation/post hash 可重复。
- 属性冲突与 opposite duplicate 不自动删除；confirmed self-intersection 保持 fail-fast。
- 真实模型每 case 两次证据通过；两个 OBJ 只补齐 adapter degenerate provenance，三个 OBJ 仍为 manual。

## 当前事实

```text
nai_you_new：1 degenerate operation，post boundary=113；
aishen_fudiao：1 degenerate operation，post boundary=3/non-manifold=59/opposite=2；
meigui_fudiao：0 operation，post non-manifold=10940/opposite=7192；
Texture2D 3MF：0 operation，strict_pass_no_repair。
```

## 下一任务

`12E-08C-R2-02 Vertex Weld、Winding 与组件守门`。准备文档：
`docs/slice/DOC/DOC_PREP_12E_08C_R2_02_VertexWeldWindingComponentGuard准备.md`。

## 验证

```text
cmake --build build --config Debug：PASS；
ctest --test-dir build -C Debug --output-on-failure：28/28 PASS；
Qt startup / experimental-report-summary self-test：PASS；
真实模型 cleanup evidence：4/4 case，各重复两次，稳定比较 PASS；
scripts/run_ci_quick.ps1：FAIL，既有 material_process_top2 widthPx golden 48/226 差异。
```

Quick CI 失败与 R1 已记录的基线 blocker 相同，不得写成全量 CI 通过，也不属于 R2-01 修改面。

## 边界

不写生产 package，不修改 legacy/RGBWSV/OpenVDB 默认设置；12E-08D 继续 BLOCKED。
