# 12C-R1-04 设置项中文帮助元数据交接

> 日期：2026-07-14
> 状态：COMPLETE

## 1. 已完成

```text
新增 HelpTextProvider，集中提供 21 个设置项的中文帮助元数据；
固定字段为 title/description/affects/default/productionSafety/docPath；
QuickConfigPanel 的模型填充、支撑、光油、preview、OpenVDB tooltip 改为读取统一元数据；
新增 SettingHelpPanel，并接入配置页“设置说明”页签；
新增 setting-help-metadata UI smoke；
用户手册、DEV、任务清单和 12C 启动状态已同步。
```

## 2. 固定边界

```text
未修改 slicer_core 或切片算法；
未修改 p0.rgbwsv.2、R G B W S V、uint8 或 black_is_print；
OpenVDB 保持默认关闭、utility/candidate、productionReplacementAllowed=false；
OpenVDB 帮助明确不写生产 RGBWSV；
未删除或改写 regression fixture。
```

## 3. 验证入口

```powershell
cmake --build build-12c-ui --config Debug --target slicer_debug_ui
.\build-12c-ui\apps\slicer_debug_ui\Debug\slicer_debug_ui.exe --self-test
.\build-12c-ui\apps\slicer_debug_ui\Debug\slicer_debug_ui.exe --ui-smoke-test --case setting-help-metadata
.\build-12c-ui\apps\slicer_debug_ui\Debug\slicer_debug_ui.exe --ui-smoke-test --case slice-settings-model
.\build-12c-ui\apps\slicer_debug_ui\Debug\slicer_debug_ui.exe --ui-smoke-test --case generated-effective-config
```

## 4. 下一任务

```text
12C-R2-01 PreviewWorkspace 与共享层状态
```

R2-01 只在现有 `LayerPreviewPanel`、`PreviewOverlayPanel`、`PreviewPanel` 之上建立模式协调与共享真实 `layerIndex`，不得重写底层数据提供者，也不得跨层兜底 RGB/W/S/V。
