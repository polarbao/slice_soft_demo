# 12B-R2 到 12C-R0 阶段交接

> 日期：2026-07-10
> 项目：slice_soft_demo
> 当前分支：main，后续任务开始前必须再次确认

## Compact Context

### Goal

结束 12B 性能与 OpenVDB 定位专项，从 `12C-R0-01 Qt/MSVC Fresh Build Lane` 开始 Qt 工作台产品化收口。

### Current facts

```text
12A 当前 P0/P1 材料、支撑和光油语义基本完成；
12B-R0/R1/R2 已完成；
OpenVDB 定位为 optional、disabled-by-default SDF utility candidate；
legacy slicer_cli 仍是生产路径；
12C 已有 PRD/DEV/DEMO/Decision/Roadmap/Tasks/Prompt/Entry Report；
现有 UI binary 的 scenario-registry smoke 可运行；
fresh Qt UI build 被 Qt 5.15.2 / MSVC 19.51 兼容问题阻断。
```

当前工作树包含尚未提交的两组变更：

```text
12B-R2-06/07：OpenVDB SDF utility report 原型、单测、脚本、CMake 和最终报告；
12C-00：阶段文档、上下文入口和准入记录。
```

不得清理、回退或覆盖这些变更。

### Decisions

```text
OpenVDB 不替换 legacy production slicer；
OpenVDB utility 不写生产 RGBWSV TIFF/package；
12C 不修改切片算法和 RGBWSV 协议；
12C 先 R0 build gate，再 R1 Profile/settings，再 R2 preview/diagnostics；
普通用户默认四类稳定 Profile；
UI 运行时自动生成 session effective config，不覆盖 fixture template；
DiagnosticsDock 默认位于底部并折叠；
12D 只允许后续只读接入，不阻断 12C-R0/R1。
```

### Current State

12C 文档和上下文准备完成。`ScenarioRegistry`、`ConfigDocument`、`QuickConfigPanel`、三个预览 panel、报告/曲线/日志 panel 和 UI smoke 基础可复用。

### Target State

用户通过稳定 Profile 和中文设置完成切片；generated effective config 参与本次运行；三种预览共享真实 `layerIndex`；诊断内容不遮挡主预览；OpenVDB 始终显示为 utility/candidate。

### Historical State

09P/09B OpenVDB surface-shell 和 07/11 UI 阶段是历史能力来源，不代表当前生产替代结论。直接选择大量 fixture JSON 和顶级报告/曲线 tab 是历史调试交互。

### Pending Confirmation

R0 没有产品需求待确认。R0-01 需要基于实际构建证据比较兼容工具链、项目内最小 shim 和 Qt patch/LTS 升级路线；依赖升级或修改第三方 Qt 安装目录前必须停止并等待用户确认。

### Validation status

本轮已验证：

```text
OpenVDB OFF utility unit test：1/1 PASS；
OpenVDB ON utility unit test：1/1 PASS；
OFF/ON utility report validator：PASS；
scenario-registry smoke：PASS，default=11 fixture=7 advanced=6；
git diff --check：PASS，只有行尾转换警告。
```

未通过/未执行：

```text
fresh slicer_debug_ui build：BLOCKED，属于 R0-01；
12C-R1/R2 UI smoke：尚未实施，不应运行或宣称通过。
```

### Key files and read order

```text
AGENTS.md
.agents/AGENTS.md
docs/slice/REPORT/REPORT_12B_R2_OpenVDB_SDFUtility当前状态.md
docs/slice/DOC/DOC_CHECKLIST_12C_阶段准入与上下文完整性.md
docs/slice/DOC/DOC_DECISION_12C_R0_R1_R2_Qt工作台阶段拆分.md
docs/slice/DOC/DOC_DECISION_12C_UI产品默认值与交互冻结.md
docs/slice/PRD/PRD_12C_Qt_UI配置预览工作台收口.md
docs/slice/DEV/DEV_12C_Qt_UI配置预览工作台设计.md
docs/slice/DEMO/DEMO_12C_Qt_UI配置预览验证方案.md
docs/codex_task/current/TASKS_12C_Qt_UI配置预览任务清单.md
docs/codex_task/current/CODEX_PROMPT_12C_Qt工作台收口执行指令.md
```

### Risks / open questions

```text
Qt 5.15.2 与当前 MSVC 的兼容修复路线尚未由 R0-01 实测决定；
工作树未提交，后续提交必须按 12B-R2 和 12C 文档边界分组；
不得让旧 binary smoke 掩盖 fresh build blocker；
不得在 12C 中顺带实现 12D 或提升 OpenVDB 生产定位。
```

### Next step

只执行 `Task 12C-R0-01 Qt/MSVC Fresh Build Lane`：复现错误、比较三条路线、输出决策、实现最小修复并从新 build dir 构建 `slicer_debug_ui`。

推荐下一条指令：

```text
请读取 12C 阶段交接、决策和任务清单，只执行 12C-R0-01 Qt/MSVC Fresh Build Lane；验证通过后按项目模板提交，不执行 R0-02。
```

本文不包含令牌、凭据、私有外部数据路径或其他敏感信息。
