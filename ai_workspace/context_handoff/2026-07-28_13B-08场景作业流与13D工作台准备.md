# Context Handoff

## Project

- 名称：SliceSoft 正式切片软件
- 仓库/分支：`slice_soft_demo` / `feature/12e-08c-mesh-repair`
- 当前主线：Stage 13 多模型场景与 TIFF 原生预览
- 技术栈：C++20、Qt 5.15 Widgets、CMake、Windows x64/MSVC、可选 OpenVDB

## Confirmed facts

- 原 Stage 13 近程范围 17 个任务，13A-01..05、13B-01..07、13C-01/02 已完成，共 14/17。
- 当前 UI 中，SceneDocument 有实例时旧 `run_slicer_button_` 被主动禁用。
- 旧 `OnImportModelAndSlice()` 会重新选择单个文件，不消费当前多模型场景。
- `OnImportModelPreview()` 使用单文件对话框，当前没有批量队列。
- 13B-05..07 已存在联合内存层、单 Package writer 和 1/11/12/22 功能矩阵，但缺 Qt 产品接线。
- 模型页右侧上下文 panel 与主窗口最右参数/预检/诊断 panel 同时常驻；诊断还与底部 Dock 重复。
- 当前工作树另有未提交的 13C-03 局部代码改动，不能与本专项文档混合或覆盖。

## Important constraints

- Qt 不进入 `slicer_core`。
- 不重新启用旧单模型入口冒充当前场景切片。
- 不修改 `p0.rgbwsv.2`、`R G B W S V`、uint8、`black_is_print`。
- 不允许 Global 静默回退 Legacy。
- 设备 buildVolume/轴向和 22 实例预算未关闭，不能宣称 production GO。
- 13D 布局开发等待 13C-05，避免 PreviewWorkspace 导航二次返工。

## Decisions made

- 新增 `13B-08`：批量导入、显式 scene CLI、Qt“切片当前场景”和真实模型收口。
- 新增 `13D`：顶部作业栏、单一 Context Inspector、项目工具折叠和统一 DiagnosticsDock。
- 推荐顺序：`13B-08-01..04 -> 13C-03..05 -> 13D-01..04 -> 12E-09A-03..06 -> 12E-10A..D`。
- 批量导入 P0 使用串行队列、总容量 22、部分失败继续、批次结束一次自动排版。
- 工作台主流程为：批量导入 -> 排版/变换 -> 场景预检 -> 切片当前场景 -> 单 Package -> TIFF 生产预览。

## Open questions

- 用户已于 2026-07-28 批准 `DOC_DECISION_13B_08` 并授权开始 13B-08-01。
- 正式设备 buildVolume、原点/X/Y 轴向和 22 实例生产预算仍待输入。
- 13B-08-02 开发前需复核 Global 多模型生产准入是否仍保持阻断。

## Next actions

1. 执行 `13B-08-01`，按 PREP/PROMPT 完成批量导入和主动作占位。
2. 13B-08-02/03/04 的任务级 PREP/PROMPT 已补齐，按前序 Gate 依次执行。
3. 13B-08 每个原子任务独立验证、报告和提交。
4. 13B-08 收口后恢复 13C-03；13C-05 后再开始 13D。
