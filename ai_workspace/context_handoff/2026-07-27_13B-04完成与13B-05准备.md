# 13B-04 完成与 13B-05 准备

> 日期：2026-07-27
> 分支：`feature/12e-08c-mesh-repair`
> 当前提交：`db6a2cf`

## 当前事实

```text
13A-01..05、13B-01..04、13B-04A、scene-aware 12E-09A-02 COMPLETE；
Stage 13 近程实现完成 9/17；
13B-04 SceneCollisionService 已通过 fixture 功能 Gate；
正式设备 buildVolume/origin/axes 仍 OPEN，production GO 未关闭；
下一任务为 13B-05 全局 Raster 与联合层合成；
13B-05 独立 PREP/PROMPT 已补齐，可进入 fixture 开发。
```

## 13B-04A 插入增强

```text
俯视画布统一绘制全部 visible 实例；
SceneViewGeometry 保留 Z/UV/材质显示资源；
Qt 显示贴图或 diffuse RGB，大网格使用显示 LOD；
追加导入后自动执行当前 11x2 规则排版；
该增强不改变生产 RGBWSV，也不替代 13B-05。
```

## 13B-04 代码

```text
src/slicer_core/layout/SceneCollisionService.h/.cpp；
tests/unit/scene_collision_admission/Main.cpp；
CMake target/test scene_collision_admission_unit_tests。
```

## 13B-04 验证

```text
定向 CTest 5/5 PASS；
scene-grid-layout UI Smoke PASS；
scripts/run_ci_quick.ps1 PASS；
git diff --check PASS。
```

## 13B-05 关键约束

```text
先提取公共内存 layer contract，不复制 slicer.cpp 私有 Legacy 算法；
不从 TIFF 反读；
共享 XY/Z grid 只接受整数像素/层 offset；
模型 ownership > 外侧光油 > 支撑 > Empty；
实例模型重叠和材料冲突 fail-closed；
本任务不写 TIFF/package，13B-06 才负责单包输出；
RGBWSV/TIFF 协议、Legacy 默认、OpenVDB optional/OFF 不变。
```

## 工作树边界

`docs/claude/` 为用户未跟踪目录，不读取、不修改、不暂存、不提交。
