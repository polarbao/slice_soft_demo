# 12C-R1-01 Profile 元数据收口交接

> 日期：2026-07-13
> 状态：COMPLETE
> 下一原子任务：12C-R1-02 SliceSettingsModel

## 1. 已完成

```text
samples/scenarios/slicer_scenarios.json 升级为 slice_soft.scenarios.2；
ScenarioRegistry 增量解析 displayName、inputFormats、materialCapabilities、productionSafety、docPath；
普通用户默认 Profile 精确收口为四个冻结类型；
默认 Profile 为 textured_nail_rgb_white_lower_support；
历史场景保留并转入 advanced/fixture；
hidden 场景不会因“显示高级/测试场景”而暴露；
Profile tooltip 展示输入格式、材料能力、生产安全和文档路径；
scenario-registry smoke 固化四 Profile 集和元数据完整性。
```

## 2. 稳定 Profile

```text
textured_nail_rgb_white_lower_support
textured_nail_rgb_varnish_lower_support
single_material_relief
production_rgb_inspection
```

前三个生产 Profile 标记为 `production`，生产 RGB 检查标记为 `diagnostic`。OpenVDB 未进入普通 Profile，仍保持 optional utility/candidate 和默认关闭。

## 3. 验证证据

```powershell
cmake --build build-12c-ui --config Debug --target slicer_debug_ui
.\build-12c-ui\apps\slicer_debug_ui\Debug\slicer_debug_ui.exe --ui-smoke-test --case scenario-registry
```

结果：

```text
PASS scenario-registry default=4 fixture=7 advanced=17
```

## 4. 下一任务边界

R1-02 建立独立于 QWidget 的 `SliceSettingsModel` DTO，覆盖模型、输出、层高、模型填充、支撑 placement/internal void、表面/外侧光油、preview 和 engine role。

当前白墨与光油彩色 Profile 复用同一基础模板。R1-02/R1-03 必须通过 Profile 默认设置和 generated effective config 形成实际差异，不能直接修改或覆盖模板 JSON。

## 5. 未改变边界

```text
未修改 slicer_core 或切片算法；
未修改 p0.rgbwsv.2、RGBWSV、uint8、black_is_print；
未删除回归 fixture；
未默认启用 OpenVDB；
未实现 12D material closure 算法。
```
