# 2026-07-23 12E-09B-01 完成与 09C 准备

## Current State

```text
12E-09B-01 COMPLETE；
当前生产主线下一任务：12E-09B-02 Production Effective Config；
12E-09A-01 COMPLETE，09A-02..06 为独立 diagnostic UI 支线；
12E-09C X/Y DPI 专项 PREPARATION COMPLETE / WAIT 09B-06；
12E-10 等待 09B、09C 和 09A-05。
```

## 09B-01 Implementation

```text
ProductionModeCatalog 定义 Legacy、Global restricted、Global material parity；
Legacy 默认，Global 显式 opt-in；
未知 Profile fail-closed；
能力锁定版本 slicesoft.ui.production_capability.12e_09b.1；
中文模式/Profile 名已进入只读目录；
ProductionModeUiDto 默认 output=false、fallback=false、资源/测量未评估；
无 Qt 控件、OpenVDB 类型、拓扑规则或 TIFF 写入。
```

验证：

```text
production_mode_catalog_unit_tests PASS；
slice_pipeline_router_unit_tests PASS；
global_surface_shell_production_pipeline_unit_tests PASS；
Qt self-test PASS。
```

## 09C Decision

用户要求 Qt 开放 X/Y DPI，产品新默认为：

```text
dpiX=635；
dpiY=600；
X pitch=0.040000 mm；
Y pitch=0.042333... mm。
```

该改动必须独立于 09B，原因是当前 Core validator、RIP Reader 和一键配置仍固定 600/600，
且会影响 raster 尺寸、外侧光油离散化、preview 物理纵横比、TIFF 数据量、golden 和性能基线。

兼容决策：

```text
显式 600/600 继续有效；
历史 fixture/golden 保持显式 600/600；
省略值和新 UI session 才采用 635/600；
Legacy/Global 共用同一 DPI；
不改变 p0.rgbwsv.2、RGBWSV、uint8、black_is_print。
```

## 09A Decision

09A 必须保留在开发序列中，但不阻断 09B。推荐单贡献者顺序：

```text
09B-02..06 -> 09C-01..06 -> 09A-02..06 -> 12E-10
```

09A-05 是 12E-10A 的明确前置。

## Next Actions

```text
1. 用户授权后执行 12E-09B-02；
2. 依次完成 09B-03..06；
3. 09B-06 后从 09C-01 开始 DPI 专项；
4. 完成 09A-02..06；
5. 执行 12E-10 最终矩阵和阶段收口。
```

## Dirty State Note

工作树包含用户新增模型、既有文档修改和本轮 09B/09C 文件。不要删除、回滚或把 `.specstory`
纳入同步；提交仅在用户明确要求时进行。
