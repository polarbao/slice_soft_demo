# 12E-08C-R2-02 拓扑守门完成

> 日期：2026-07-20
> 状态：R2-02 COMPLETE / R2-03 READY

## 已完成

新增显式、默认关闭的受约束 vertex weld 与 local winding operation set。weld 只允许同一原始
edge-connected component 内、显式阈值内的顶点归并，并在候选上重新验证退化、组件、拓扑和完整
self-intersection。winding 只接受无冲突且翻转集合唯一的共享边 parity 解，并同步交换 per-corner UV。

报告新增 `allowVertexWeld`、`allowWindingRepair` 和 `vertexMappings[]`；CLI 新增
`--execute-r2-02`。所有 blocked candidate 均丢弃，不写 production package/TIFF。

## 证据

generated fixture 覆盖 safe weld、跨组件近邻、退化阻断、唯一 winding 和 non-orientable 歧义。
`nai_you_new`、`aishen_fudiao`、`meigui_fudiao` 与闭合 Texture2D 3MF 均执行两次并通过稳定投影。
三个 OBJ 无新增 weld/flip 并继续 manual；闭合 3MF 保持 no-op strict PASS。

## 固定边界

OpenVDB optional/OFF；legacy 不调用 repair；`p0.rgbwsv.2`、RGBWSV、uint8、`black_is_print` 不变；
R2 不拥有生产写包权限。真实 OBJ 的 sampled self-intersection 仍不能作为 post-strict PASS。

## 下一任务

`12E-08C-R2-03 Boundary Loop Repair`。准备文档：

`docs/slice/DOC/DOC_PREP_12E_08C_R2_03_BoundaryLoopRepair准备.md`

只允许简单、平面、严格凸、预算内且属性策略唯一的 boundary loop。12E-08D 继续 BLOCKED。
