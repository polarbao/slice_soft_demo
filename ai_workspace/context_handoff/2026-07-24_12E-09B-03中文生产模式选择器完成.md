# 12E-09B-03 中文生产模式选择器完成

> 日期：2026-07-24
> 当前生产主线下一任务：12E-09B-04

## 已完成

```text
新增“传统切片 / 全局纹理壳层”中文产品模式选择器；
Legacy 保持默认，Global 必须显式选择；
Global 只显示 restricted/material-parity 两个能力目录 Profile；
显示能力范围、准入/过期、阻断和高资源开销提示；
Profile 锁定普通材料、支撑和光油控件，并通过 tooltip 说明原因；
普通配置页不再显示 OpenVDB backend 开关；
独立 OpenVDB 诊断/候选按钮未删除，仍不属于产品模式。
```

## 验证

```text
cmake --build build --config Debug --target slicer_debug_ui PASS；
production-mode-selector UI smoke PASS：
  1280x720；
  1440x900；
  1920x1080；
slicer_debug_ui --self-test PASS；
production_mode_catalog_unit_tests PASS；
production_effective_config_unit_tests PASS。
```

## 固定边界

```text
未修改 p0.rgbwsv.2；
未修改 R G B W S V / uint8 / black_is_print；
未改变 Legacy 默认；
未把 OpenVDB 作为第三种产品模式；
未提前实现 09B-04 一键路由或 09C DPI。
```

## 下一步

执行 09B-04：让现有一键切片读取当前产品模式/Profile 和 session Effective Config，复用现有预检与
ProcessRunner，阻断 stale/invalid Global，禁止静默 fallback，并确保失败时不加载旧 package。
