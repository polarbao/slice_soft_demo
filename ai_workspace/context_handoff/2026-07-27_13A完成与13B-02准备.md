# 13A 完成与 13B-02 准备

> 日期：2026-07-27
> 当前状态：13A-01..05 COMPLETE / M13-1 CANDIDATE PASS
> 下一任务：13B-02 READY FOR DEVELOPMENT

## 已完成事实

13A 已形成单模型切片前工作区闭环：

```text
不可变源模型和实例变换；
+Z 俯视、选择、X/Y 平移、绕 Z 旋转和统一缩放；
原点居中、重置、mirrorX/mirrorY；
镜像后的 winding/UV 修正；
source/transformed 双预检；
Legacy/Global 独立准入；
session scene/effective config 原子保存和回读；
三窗口 UI Smoke、真实 OBJ/3MF 正向资产和反向 fixture。
```

收口验证提交为 `0a1169e`。13A 状态报告：
`docs/slice/REPORT/REPORT_13A_模型俯视工作区与实例变换当前状态.md`。

## 固定边界

```text
blocked 模型可查看和调整，但生产 fail-closed；
scene effective config 尚未被生产 slicer_cli 消费；
Legacy 默认，OpenVDB 显式候选且默认关闭；
不修改 p0.rgbwsv.2、RGBWSV、uint8、black_is_print；
不自动修复 confirmed self-intersection；
不允许 silent fallback。
```

## 13B-02 准入

13B-01 已提供 `MultiModelScene`、`ModelSource`、`ResourceScope` 和 Scene Effective Config；
13A 已提供 `SceneDocument`、`SceneSelectionModel`、`SceneModelRepository`、`ModelTopViewLoader`、
`SceneTransformController`、`ModelTransformPanel` 和 transformed preflight。13B-02 必须扩展这些
现有 API，不得创建第二套平行 scene store。

执行真源：

```text
docs/slice/DOC/DOC_PREP_13B_02_模型列表与实例操作准备.md
docs/codex_task/current/CODEX_PROMPT_13B_02_模型列表与实例操作执行指令.md
```

13B-02 仅实现 1..22 实例列表和场景草稿操作，不实现 11x2 排版、碰撞、联合切片或生产 package。

## 后续准备

13B-03 已有全阶段通用准备，待 13B-02 实际 API 完成后生成独立 PREP/PROMPT 并校正文件所有权；
13C-01 技术上可独立执行，但单贡献者顺序保持模型场景 UI 优先。
