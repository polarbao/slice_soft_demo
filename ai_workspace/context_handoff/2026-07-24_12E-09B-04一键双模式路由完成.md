# 12E-09B-04 一键双模式路由完成

> 日期：2026-07-24
> 当前生产主线下一任务：12E-09B-05

## 已完成

```text
“导入模型并切片”和“运行切片”均读取当前产品模式与 Global Profile；
Legacy 继续使用当前传统 Profile；
Global 使用能力目录映射的只读 canonical Profile 源，生成独立 session Effective Config；
两种产品模式共用 ModelPreflightController、SlicePreflightCoordinator、ProcessRunner 和 slicer_cli；
Global production 走普通 slicer_cli 的 08D production pipeline，不依赖独立 OpenVDB 诊断 backend；
模式、Profile 或 ConfigDocument 变化会使旧准入/session 过期；
ProductionSliceRunSession 只允许成功的当前进程加载当前 package；
失败、阻断、非法 Profile 和重试均不会加载旧 package；
fallbackApplied 固定为 false，Global 不会自动改跑 Legacy。
```

## Profile 源

```text
restricted:
  samples/configs/texture_fill_partition/global_production_xiao_ma_white_fill.json

material parity:
  samples/configs/texture_fill_partition/global_production_xiao_ma_material_parity.json
```

源 fixture 只读；模型、输出、DPI、modelTransform、autoOrient、background 和 preview 等允许项只进入 session
copy。Global 材料、支撑、光油和 closure 合同仍由 Profile capability lock 恢复。

## 验证

```text
Debug 全量 build PASS；
Debug CTest 53/53 PASS；
production_slice_route_process_tests PASS；
model-preflight-one-click-gate PASS：
  clean Global production admitted；
  topology blocked；
  writer 启动前必须完成准入；
model-preflight-lifecycle PASS；
production-mode-selector PASS；
slicer_debug_ui --self-test PASS；
run_ci_quick.ps1 PASS。
```

## 固定边界

```text
Legacy 仍是默认；
Global 必须显式 opt-in；
无静默 fallback；
OpenVDB 不作为第三种产品模式；
未修改 p0.rgbwsv.2、R G B W S V、uint8、black_is_print；
未提前实现 09B-05 结果身份展示或 09C DPI。
```

## 下一步

执行 09B-05：校验并加载当前 session package，显示 requested/effective mode、productionOutputWritten、
fallback=false、本次实际耗时/峰值内存和 Global 高开销提示，并验证 preview/report 与 package 同源。
