# 12C-R0-02 UI Smoke 基线交接

> 日期：2026-07-12
> 当前分支：main

## Compact Context

### Goal

使用 R0 fresh build lane 的二进制完成 UI self-test、场景注册、层预览、真实叠加预览和配置编辑器 Smoke 基线。

### Current facts

```text
12C-R0-01 已完成；
12C-R0-02 已完成；
build-12c-ui fresh slicer_cli 可生成两类 UI fixture；
fresh slicer_debug_ui 的全部 R0-02 case 通过；
12C-R0-03 尚未执行。
```

### Validation evidence

```text
fresh slicer_cli build：PASS；
UiSmokeLayerPreview fixture：PASS，48 x 24 x 25；
UiSmokeOverlayRgbwv fixture：PASS，48 x 24 x 25；
--self-test：PASS startup / experimental-report-summary；
scenario-registry：PASS default=11 fixture=7 advanced=6；
layer-preview-load：PASS layers=25；
overlay-load-real：PASS images=47；
save-as-config：PASS。
```

### Dependency audit

`D:\vcpkg-openvdb` 仍被 `build-openvdb-09p`、09B 系列 build cache 以及当前 12B-R2 OpenVDB 验证文档直接引用，约占 5.48 GiB。本机默认 `VCPKG_ROOT` 已指向 `D:\Program Files Tools\vcpkg`，但尚未迁移这些独立 OpenVDB lane，因此本轮未删除该目录。

### Decisions

R0-02 只建立基线，不修改 UI 行为、切片算法或协议。生成的 `output/UiSmoke*` 和配置编辑器输出仅为本地验证产物。

### Current State

fresh Qt UI build 与必需 Smoke 均可复现。R0 尚缺布局和组件复用基线。

### Target State

完成 R0-03 的组件职责审查和 1440x900、1280x720、1024x768 布局证据后，才解除 R1 准入阻塞。

### Risk Points

承载 E/F 分区的 SSD 在 2026-07-12 00:20 出现过 NTFS Event 50/140；本任务前未发现更新事件。若再次发生，应立即停止仓库写入。

### Next step

只执行 `12C-R0-03 布局与组件复用基线`，不提前进入 R1。
