# 12E-08C-R3-02 真实模型矩阵完成

> 日期：2026-07-21
> 分支：`feature/12e-08c-mesh-repair`
> 状态：R3-02 COMPLETE / R3-03 READY NON-PRODUCTION / 12E-08D BLOCKED

## 已完成

新增 `--execute-r3-02` 诊断 preset、CTest、expectation golden 和真实模型 repair matrix 脚本。四个 required
case 均执行 strict/no-repair 与 conservative-repair 两条 lane，每条两次，稳定投影一致。

## 关键证据

```text
nai_you_new：8409 confirmed，fail-fast，无 mutation；
aishen_fudiao：19270 confirmed + 20 coplanar，59 non-manifold，无 mutation；
meigui_fudiao：5592 confirmed，10940 non-manifold，无 mutation；
Texture2D 3MF：complete no intersection，no-op strict PASS，validator/attribute PASS。
```

任务证据 4/4 完整，production Gate 0/4 通过。三个 OBJ 的 blocked 是完整几何证据，不是分析预算不足。

## 安全边界

```text
repair 默认关闭；
OpenVDB OFF；
没有通用 self-intersection repair 或 fan split；
不写 TIFF、preview 或 production package；
legacy 与 RGBWSV 协议不变；
12E-08D 继续 BLOCKED。
```

## 已运行验证

```text
定向 CTest：3/3 PASS；
R3-02 真实模型矩阵：PASS，约 226.2 秒；
完整 Debug build：PASS；全量 Debug CTest：37/37 PASS；Qt self-test：2 项 PASS；
Quick CI：已知 material_process_top2 widthPx=48/226 blocker，未记录为通过。
```

## 下一任务

执行 `12E-08C-R3-03 Release Core 与 Legacy Regression`。只允许非生产 Release 证据：三个 OBJ 的 global
core 必须 `skipped_due_topology`，闭合 3MF 可执行 partition/texture/raster/full closure；同时验证 legacy、
repair-disabled TIFF invariant 和 RIP strict。R3-03 不得写 global production package。
