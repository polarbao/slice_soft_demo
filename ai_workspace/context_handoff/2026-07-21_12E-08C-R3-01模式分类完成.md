# 12E-08C-R3-01 模式分类完成

## 当前事实

```text
branch=feature/12e-08c-mesh-repair；
R1 COMPLETE；
R2 COMPLETE；
R3-01 COMPLETE；
R3-01A READY；
12E-08D BLOCKED。
```

## 已实现

新增只读 `MeshNonManifoldPatternClassifier`，以 canonical edge incidence 和排除 non-manifold edge 后的
residual component 证据，稳定分类 duplicate exporter、separable local edge fan、overlapping
component、mixed winding fan、attribute-conflicting fan 和 unclassified。分类器只有在每个 residual
fan group 都为反向边对、无重复面且无属性冲突时，才给出 unique fan split 可行证据。

`MeshRepairOptions` 新增默认关闭的 `classifyNonManifoldPatterns`，并纳入 options hash；report 新增
`nonManifoldAnalysis`；CLI 新增 `--classify-r3-01`。该路径不修改 mesh，不写生产包。

## 真实证据

```text
nai_you_new：0 条 non-manifold edge，not_present；
aishen_fudiao：59 条，duplicate exporter=2，attribute conflict=57；
meigui_fudiao：10940 条，duplicate exporter=10935，attribute conflict=5；
Texture2D 3MF：0 条，not_present；
4/4 case 双运行稳定；
0 个真实 case 满足全局 unique fan split。
```

证据摘要：
`output/benchmarks/12e_08c_r3_01_non_manifold/non_manifold_summary.json`。

## 下一步

执行 12E-08C-R3-01A 完整自相交证据。用确定性空间索引完整枚举 required real model
candidate pair，复用当前 narrow-phase；小 fixture 与 O(N^2) 对照，sampled 不得计为 strict PASS。

## 不得改变

repair 默认 OFF；legacy/TIFF/Qt 不接入；OpenVDB optional/OFF；RGBWSV 协议不变；
12E-08D 不得提前解锁。
