# 13A-02 完成与 13A-03 准备

> 日期：2026-07-27
> 当前分支：`feature/12e-08c-mesh-repair`
> 代码提交：`cc6e40d`
> 当前任务：13A-03 READY FOR DEVELOPMENT

## 已完成事实

```text
13A-01 ModelTransform/ModelInstance：COMPLETE；
13B-01 MultiModelScene/Scene Effective Config：COMPLETE；
12E-09A-02 scene-aware Diagnostic Effective Config：COMPLETE；
13A-02 SceneViewGeometry 和 Qt 模型俯视：COMPLETE。
```

13A-02 新增：

```text
无 Qt SceneViewGeometry +Z 投影合同；
source/effective bbox、identity/revision/hash 和纹理提示；
SceneDocument、SceneSelectionModel；
generation-aware/cancellable ModelTopViewLoader；
QPainter ModelTopViewWidget；
独立“导入模型预览”，不启动 slicer_cli；
scene_view_geometry_unit_tests；
model-top-view UI Smoke。
```

验证：

```text
定向 CTest 4/4 PASS；
Qt self-test PASS；
model-top-view UI Smoke PASS；
Debug 全量构建 PASS；
Quick CI PASS。
```

## 固定边界

```text
+X 右、+Y 上、单位 mm；
Z 落台和 autoOrient 沿用生产逻辑；
core 不依赖 Qt；
模型导入/投影不在 UI 主线程；
只有最新 generation/revision 可发布；
blocked 可查看但不等于生产 PASS；
Legacy 仍默认，OpenVDB 仍默认关闭；
生产 p0.rgbwsv.2/RGBWSV/uint8/black_is_print 未修改。
```

## 下一任务 13A-03

目标：

```text
单模型 X/Y、rotateZ、uniformScale；
场景原点居中和 identity reset；
SceneDocument 持有 ModelInstance/revision/dirty/stale；
只读 SceneModel repository，避免每次编辑重导入；
SceneTransformController；
异步重投影；
单实例 MultiModelScene/effective config 原子保存、回读和回滚；
model-top-view-transform UI Smoke。
```

执行真源：

```text
docs/slice/DOC/DOC_PREP_13A_03_选择与精确变换准备.md
docs/codex_task/current/CODEX_PROMPT_13A_03_选择与精确变换执行指令.md
```

## 后续准备度

```text
13A-04：专项 PREP 已完成，WAIT 13A-03；重点是 mirror 和 transformed preflight；
13A-05：通用准备已存在，执行文档等待 13A-04 API；
13B-02：技术依赖已满足，专项 PREP 已完成，但单贡献者顺序排在 13A-05 后；
13C-01：READY，固定顺序排在 scene UI/13B 波次之后；
设备 buildVolume/轴方向仍阻断 13B production，不阻断 13A-03。
```

## 工作树注意

`docs/claude/` 是无关未跟踪目录，不纳入提交。
