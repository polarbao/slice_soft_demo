# 12E-08C-R1-04 真实模型 Baseline 完成

> 日期：2026-07-20
> 状态：R1 COMPLETE / R2-01 READY

## 已完成

新增只读 `MeshRepairPreflight`、`mesh_repair_preflight` 和四 case 双运行脚本。三个真实 OBJ 均输出
`manual_repair_required`，闭合 Texture2D 3MF 输出 `strict_pass_no_repair`；全部 case 的 config/source/
geometry/attribute/stable evidence hash 可重复，且不执行 repair、不写 production package。

## 关键事实

```text
nai_you_new：V=58924，T=117705，C=10，boundary=113，degenerate=1；
aishen_fudiao：V=42193，T=84533，C=10，boundary=3，nonManifold=59，oppositeDuplicate=2，degenerate=1；
meigui_fudiao：V=34722，T=76926，C=2，nonManifold=10940，oppositeDuplicate=7192；
3MF checker：V=8，T=12，C=1，strict_pass_no_repair。
```

三个 OBJ 的自相交检查均为 sampled，不是 confirmed。R3 在真实模型 matrix 前新增 R3-01A 完整自相交证据任务。

## 下一任务

`12E-08C-R2-01 Degenerate/Duplicate Cleanup`。必须把 adapter 已过滤退化面的行为纳入 operation/source
mapping；aishen/meigui 的 duplicate 全为 opposite duplicate，不得按 exact duplicate 静默删除。

## 边界

R2-02..04、R3 与 12E-08D 继续阻断。OpenVDB optional/OFF、legacy 和 RGBWSV 协议未变。
