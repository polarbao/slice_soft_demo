# 13B-07 功能矩阵完成与 13C-01 准备

> 日期：2026-07-28
> 分支：`feature/12e-08c-mesh-repair`
> 当前任务：13B-07 已完成；等待用户授权 13C-01

## 1. 当前状态

```text
13A-01..05：COMPLETE；
13B-01..07：FUNCTIONAL COMPLETE；
13B production：INPUT_OPEN；
13C-01：READY / NEXT；
Stage 13 近程：12/17；
Legacy 默认，OpenVDB/Global 仍显式 opt-in；
12G-TCWS：FROZEN。
```

13B-07 已通过 Debug/Release 的 1/11/12/22 实例、真实 OBJ、Texture2D 3MF、平移 Raster 复用、
联合层、单 package、每全局层单 TIFF 和 RIP strict 功能矩阵。

## 2. 关键实现

```text
apps/multi_model_scene_matrix/Main.cpp；
scripts/run_13b_07_real_model_matrix.ps1；
src/slicer_core/pipeline/TranslatedSceneRasterReuse.*；
src/slicer_core/reports/MultiModelSceneMatrixReport.*；
samples/configs/scene/13b_07_*；
docs/slice/REPORT/REPORT_13B_07_真实模型矩阵与阶段收口当前状态.md。
```

功能 Profile 固定为 127x127 DPI、0.20 mm 层厚、preview OFF 和
`modelFill.material=white/modelFill.scope=all_model`。`all_model` 用于保证纯白纹理像素仍由 W
通道承载可打印模型材料。

## 3. 验证证据

```text
定向 CTest：7/7 PASS；
Debug 矩阵：5 个正向 PASS，5 个负向按预期 BLOCKED；
Release 矩阵：5 个正向 PASS，5 个负向按预期 BLOCKED；
全部正向 package：RIP strict PASS，warnings=0；
Quick CI：PASS。
```

Release 22 实例功能 fixture：

```text
Grid=1661x416x29；
两个唯一模型只调用两次本地层生产器；
20 个重复实例使用纯平移 Raster 复用；
slice=343.80 ms；
compose=387.50 ms；
write=3655.26 ms；
rip=2880.23 ms；
total=14611.34 ms；
process peak=243830784 bytes。
```

该数据不是 635x600 DPI 设备 SLA。

## 4. 不变量与未决项

```text
p0.rgbwsv.2 / RGBWSV / uint8 / black_is_print 未修改；
每个场景仍只允许一个 pipeline mode；
不允许 silent fallback；
功能 fixture 不等于 production；
设备 buildVolume、原点、X/Y 轴向仍 OPEN；
22 实例正式性能预算仍 OPEN；
aishen/meigui/titian strict PASS 仍为 0/3；
Global/OpenVDB 多模型 production 矩阵未运行。
```

## 5. 下一步

下一原子任务是 `13C-01 TiffLayerSource 与 LRU`。开始前按顺序读取：

```text
AGENTS.md；
docs/slice/REPORT/REPORT_13B_07_真实模型矩阵与阶段收口当前状态.md；
docs/slice/DOC/DOC_PREP_13C_01_TIFFLayerSource与Cache准备.md；
docs/slice/PRD/PRD_13C_RGBWSV_TIFF原生统一预览.md；
docs/slice/DEV/DEV_13C_TIFFLayerSource与统一材料合成设计.md；
docs/slice/DEMO/DEMO_13C_TIFF原生统一预览验证方案.md；
docs/codex_task/current/TASKS_13_模型场景排版联合切片与TIFF预览任务清单.md。
```

13C-01 只建立 manifest/TIFF 权威层源、异步解码、取消/stale 和默认 5 层 LRU，不提前实现
13C-02 合成器或 13C-03 UI 统一。

## 6. 工作区边界

`docs/claude/` 是用户要求排除的未跟踪目录，不得加入提交。本文不包含凭据、令牌或私密数据。
