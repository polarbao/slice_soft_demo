# 12C-R0-03 布局组件基线交接

> 日期：2026-07-12
> 当前分支：main

## Compact Context

### Goal

完成现有 Qt 布局、组件职责和复用边界基线，关闭 12C-R0 并为 R1 Profile/Settings 准入。

### Current facts

```text
12C-R0-01 fresh build：COMPLETE；
12C-R0-02 UI smoke：COMPLETE；
12C-R0-03 layout/component baseline：COMPLETE；
12C-R0：COMPLETE；
12C-R1-01：NEXT。
```

### Layout evidence

当前三列 minimum width 在 margin/handle 前已为 1300 px，左侧无滚动长表单和常驻日志共同造成窗口最小高度过大。1440x900、1280x720、1024x768 本地采样均无法完整满足目标尺寸；窄窗口右侧诊断区被裁切。

### Reuse decisions

`ScenarioRegistry`、`ConfigDocument`、三个 preview panel、ReportPanel、ChannelChartPanel 和 LogPanel 全部复用。R1 不重写 preview；R2 只增加 PreviewWorkspace/DiagnosticsDock 协调层。

### vcpkg audit

共享根 `D:\Program Files Tools\vcpkg` 可继续服务普通 manifest 项目，但不能直接服务当前 OpenVDB lane。fresh OpenVDB configure 实际运行 19 分钟后在 `hwloc:x64-windows` 失败，错误为路径被拆成 `/d/Program` 和 `Files`。没有从 `D:\vcpkg-openvdb` 复制 installed/packages/buildtrees；OpenVDB 后续仍使用无空格根。

若未来统一环境，先为项目锁定 vcpkg builtin-baseline，再建立无空格共享根或无空格别名，并从全新 build dir 验证；不得原地切换现有 CMake cache。

### Concurrent worktree note

本任务期间出现用户新增的 `DOC_RESEARCH_PrismSlicer功能处理策略与SliceSoft对照.md` 及其 `docs/slice/README.md` 索引改动。它们不属于本任务，不得纳入 12C-R0-03 提交。

### Risk Points

承载 E/F 分区的 SSD 曾在 2026-07-12 00:20 记录 NTFS Event 50/140。若出现更新事件，应停止仓库写入。

### Next step

只执行 `12C-R1-01 Profile Metadata 收口`，不提前执行 R1-02 或 R2。
