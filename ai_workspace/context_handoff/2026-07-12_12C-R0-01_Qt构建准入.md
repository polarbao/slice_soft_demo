# 12C-R0-01 Qt 构建准入交接

> 日期：2026-07-12
> 当前分支：main

## Compact Context

### Goal

在不升级 Qt、不修改 Qt 安装目录的前提下，使 Qt 5.15.2 UI 可使用 MSVC 19.51 从 fresh build dir 构建。

### Current facts

```text
12C-R0-01 已完成；
Qt stdext 编译错误已复现并修复；
build-12c-ui fresh configure/build PASS；
fresh UI --self-test PASS；
PowerShell 与 VS Code 构建入口已建立；
12C-R0-02/R0-03 尚未执行。
```

### Decisions

选择项目内 `Qt515MsvcCompatibility.h` target-scoped shim。当前不固定旧 MSVC 工具集，也不升级 Qt。

### Current State

fresh Qt UI build gate 已通过。完整 UI smoke 和布局基线仍待 R0-02/R0-03。

### Target State

完成 R0-02 fresh binary smoke 与 R0-03 布局/组件复用基线后，才进入 R1 Profile/Settings。

### Historical State

旧 `build/apps/slicer_debug_ui` binary 只作历史基线。NTFS/Disk 事件在本轮 00:20 仍有记录，但之后 `git fsck` 和最终 fresh build 均通过；该存储风险尚不能视为永久消除。

### Pending Confirmation

R0-02 无产品决策待确认。若再次出现 NTFS Event 50/140 或 Disk Event 51，应停止写入并优先处理存储设备。

### Validation status

```text
git fsck --full --no-progress：exit 0，仅 dangling objects；
Configure12CQtUi.ps1 fresh build：PASS，259.8 s；
fresh slicer_debug_ui --self-test：PASS startup / experimental-report-summary；
tasks.json / launch.json JSON parse：PASS。
```

### Next step

只执行 `12C-R0-02 UI Self-Test 与 Smoke 基线`，使用 `build-12c-ui` fresh binary，不执行 R0-03。
