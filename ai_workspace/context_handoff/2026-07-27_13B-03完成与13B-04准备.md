# 13B-03 完成与 13B-04 准备

> 日期：2026-07-27
> 分支：`feature/12e-08c-mesh-repair`
> 最新实现提交：`aef10c1 feat(13B-03): 建立11x2规则排版与Qt操作闭环`
> 当前任务：13B-04 fixture 幅面、碰撞和逐实例准入

## 1. 当前事实

```text
13A-01..05、13B-01..03、scene-aware 12E-09A-02 COMPLETE；
Stage 13 近程完成 8/17；
13B-03 定向 CTest 6/6、Qt self-test、multi-model-list、scene-grid-layout 和 Quick CI PASS；
工作树中 docs/claude/ 是无关未跟踪目录，不纳入提交；
Legacy 默认、OpenVDB optional/OFF、RGBWSV/TIFF 固定协议不变。
```

## 2. 13B-03 交付

```text
GridLayoutPolicy 无 Qt 核心；
1..11 列、1..2 行、最多 22 个实例；
20.00/30.00 mm 默认边到边净距和 0.01 mm UI 步进；
row_major、隐藏占位、锁定保持和冲突 fail-closed；
SceneDocument 原子 apply/restore；
requested/derived/effective transform 保存和回读；
模型工作区中文排版页；
三窗口 UI Smoke。
```

## 3. 13B-04 已冻结边界

```text
只实现 fixture 功能 Gate；
正式设备 buildVolume/origin/axes 仍 OPEN；
FunctionalFixture 必须使用显式 Fixture buildVolume；
Production 遇到 Fixture 或 Unresolved 必须 fail-closed；
可见实例参与幅面、碰撞和 admission，隐藏实例只保留身份；
AABB 快筛后使用投影三角形精确检测；
不自动避让，不实现联合 Raster/TIFF/package。
```

## 4. 当前入口

```text
docs/slice/REPORT/REPORT_13B_03_11x2规则排版当前状态.md
docs/slice/DOC/DOC_PREP_13B_04_幅面碰撞与逐实例准入准备.md
docs/codex_task/current/CODEX_PROMPT_13B_04_幅面碰撞与逐实例准入执行指令.md
```

## 5. 验证基线

```powershell
cmake --build build --config Debug --target scene_collision_admission_unit_tests slicer_debug_ui
ctest --test-dir build -C Debug -R "^(scene_collision_admission_unit_tests|grid_layout_policy_unit_tests|scene_document_unit_tests|scene_transform_controller_unit_tests|transformed_model_preflight_unit_tests)$" --output-on-failure
.\scripts\run_ci_quick.ps1
git diff --check
```

## 6. 未决

```text
设备 buildVolume.widthMm/heightMm；
设备原点和 X/Y 方向；
22 实例生产性能预算；
13B-05 联合 Raster 尚未开始；
13C TIFF 原生预览尚未开始。
```
