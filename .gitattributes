# 行尾：仓内与检出一律 LF。
#
# 仓内 blob 本就是纯 LF（普查：LF 3035、CRLF 0）。问题出在**检出**这一层——
# 原文件没有 text=auto，检出行为完全由各人的 core.autocrlf 决定（本机系统级为 true），
# 于是盘上长出 1295 个 CRLF、1561 个 LF 和 176 个单文件内混合。
# 详见 docs/plan-feature/文件行尾统一/。
#
# 选 lf 而非 crlf 的理由：仓内本就如此，选 crlf 等于反向改 3035 个文件；
# 且下方那条冻结样本先例用的就是 eol=lf，同一个仓里两种 eol 取值是给后来人埋坑。
* text=auto eol=lf

# 二进制：不做任何行尾转换
*.pdf binary
*.png binary
*.jpg binary
*.jpeg binary
*.ico binary
*.tif binary
*.tiff binary
*.dll binary
*.exe binary
*.lib binary
*.pdb binary
*.zip binary
*.ttf binary

# 模型资产：钉住现状，检出即仓内字节。
# 仓内它们已是 LF，本规则与上面的默认规则效果相同；单列出来是因为这批文件的字节会进
# sourceHash，显式 -text 能让后来人一眼看出「这里从不转换」，不必再查一遍哈希是否签入。
*.obj -text
*.mtl -text
*.stl -text

# 按精确 SHA-256 字节校验的冻结样本：单独钉住（既有条目，保持不变）
samples/configs/3mf/three_mf_texture2d_checker.json text eol=lf
