{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://slicesoft.local/contracts/file_contract_v1.request.schema.json",
  "title": "SliceSoft worker request",
  "type": "object",
  "required": [
    "contract",
    "major",
    "minor",
    "jobId",
    "correlationId",
    "capability",
    "timeoutMs"
  ],
  "properties": {
    "contract": { "const": "file_contract" },
    "major": { "const": 1 },
    "minor": { "type": "integer", "enum": [0, 1] },
    "jobId": {
      "type": "string",
      "pattern": "^[A-Za-z0-9][A-Za-z0-9_.-]{0,127}$"
    },
    "correlationId": {
      "type": "string",
      "minLength": 1,
      "maxLength": 128
    },
    "capability": {
      "type": "string",
      "enum": [
        "slice.rgbwsv",
        "slice.rgbwsvt",
        "geometry.preflight.full",
        "geometry.repair"
      ]
    },
    "timeoutMs": {
      "type": "integer",
      "minimum": 1000,
      "maximum": 86400000
    },
    "sceneHash": {
      "type": "string",
      "pattern": "^sha256:[0-9a-fA-F]{8,64}$"
    },
    "scene": { "type": "object" },
    "profile": { "type": "object" },
    "input": { "type": "object" },
    "output": {
      "type": "object",
      "properties": {
        "contract": {
          "type": "string",
          "enum": ["p0.rgbwsv.2", "p0.rgbwsvt.1"]
        },
        "packageDir": { "type": "string", "minLength": 1 }
      },
      "additionalProperties": true
    }
  },
  "allOf": [
    {
      "if": {
        "properties": { "capability": { "const": "slice.rgbwsv" } },
        "required": ["capability"]
      },
      "then": {
        "required": ["sceneHash", "scene", "profile", "output"],
        "properties": {
          "minor": { "const": 0 },
          "output": {
            "required": ["contract", "packageDir"],
            "properties": {
              "contract": { "const": "p0.rgbwsv.2" }
            }
          }
        }
      }
    },
    {
      "if": {
        "properties": { "capability": { "const": "slice.rgbwsvt" } },
        "required": ["capability"]
      },
      "then": {
        "required": ["sceneHash", "scene", "profile", "output"],
        "properties": {
          "minor": { "const": 1 },
          "output": {
            "required": ["contract", "packageDir"],
            "properties": {
              "contract": { "const": "p0.rgbwsvt.1" }
            }
          }
        }
      }
    },
    {
      "if": {
        "properties": {
          "capability": {
            "enum": ["geometry.preflight.full", "geometry.repair"]
          }
        },
        "required": ["capability"]
      },
      "then": {
        "required": ["input"],
        "properties": { "minor": { "const": 0 } }
      }
    }
  ],
  "additionalProperties": true
}
